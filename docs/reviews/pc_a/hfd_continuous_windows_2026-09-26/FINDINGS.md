# 审查发现、修复和保留失败

1. 预审的合成人物框没有覆盖物体，却断言应有角色候选，出现 1 failed / 68 passed。将夹具人物框改成明确的双人-物体交叠，仍保留唯一几何关联；没有放宽生产关联器或角色规则。日志 `evidence/initial/round1.log`。这是测试预期错误，不是实际角色精度问题的修复。
2. mypy 发现新 `load_runtime_windows` 的结果列表缺少类型标注，已在正式冻结前补齐；不改变行为。
3. 首次完整冻结执行中，两审 178 / 25、mypy 367、Ruff、真实 337 试次来源重建及新进程复核均通过，但首个真实 MediaPipe 初始化因沙箱缺少 macOS 图形服务而 SIGABRT（子进程退出 -6，总执行器 1）。CPU delegate 已显式指定，仍触发原生 Metal 图服务创建。`evidence/final-01/real-frontend.log` 保留完整堆栈，不列为真实矩阵成功。通过已授权本机执行初始化同一固定模型后恢复；没有改依赖、模型、科学阈值或绕过源校验。
4. 自审补充缺口：最初完整时钟重封测试只证明结构准入和原源拒绝。加强为固定真摘要在模型启动前拒绝；主动误用自报假摘要时，继续执行全部窗口、手物测量、轨迹、角色候选、完整候选及最终 core/ledger/action 检查；诊断文件确实可生成，语义发布/记忆/动作仍 0。再恢复原始包并从来源重建成功。追加每窗首帧均 NEW_UNVERIFIED、后续可关联，避免跨窗口继承状态。加强后重新从第一审开始，旧 25 项不冒充最终覆盖。

这些检查只支持本轮适配器和诊断路径的局部结论。完整伪造可产生貌似有效的诊断输出，因此“有候选”“字段完整”“哈希一致”都不能成为来源或科研准确率的证明。调用者必须携带独立源重建得到的固定 runtime 摘要；前端自报结果不授予原生语义权限。

实际数据的角色/手物限制和下一主项由 REPORT 的真实结果另列；未新增独立人工标注，也不把测试数当成统一闭环证据。

真实模型日志另有 `NORM_RECT without IMAGE_DIMENSIONS` 的投影提示。保留该原生日志，没有据此假称已建立实际手部坐标误差界；当前结果是待校准的图像几何测量。完整手物语义、真实接触/释放及机器人角色尚未闭合。
