# 窗口二：修复后独立验收准备与精确交付复核

2026-09-12；**PARTIAL**。电脑 A/macOS arm64 本机独立复核，**不是 Windows/WSL 的 B0/B1**。只新增独立工具、测试、矩阵和报告；不修改 B 专属比较模块、旧77项、W1编排器、W3核心、科学配置或历史工件。本轮未生成任何新比较包，没有重新跑旧77项，没有自动合并。

结论：**局部诊断可靠，完整机制、公平性和科学收益尚未建立。** 已公布的 W3 模型恢复可在其局部依赖范围接受；W1 缓存缺口在被审核生产源码仍复现，其正在进行的修复不能预先签收。

## 1. 远端、分支和被审查版本

开工 `git fetch origin --prune` 首次因 FETCH_HEAD 沙箱写权限失败；使用批准的 Git 权限后 fetch 成功。按顺序完整读取远端协作四文件、SNAPSHOTS、结构二 README、`97e1d2c1e83a4532e2b3bab3bf87cc6a2927f274` 的 R6 REPORT，以及 W2 最新 handoff contract。主工作区仍是旧 checkpoint 且存在未跟踪文件，均未动用或覆盖。

| 对象 | 本轮绑定的完整 SHA | 意义 |
|---|---|---|
| 远端集成基线 / 本任务 base | `bdec3ee21b7db361e390496d97ff2eb30390dc6c` | 尚不是已审查整合后的完整统一系统 |
| 本轮工具代码 | `8bdd5b2a7d045865d3a72b83a97d34ef822a3760`（完整 SHA 见 validation_summary.json） | 独立验收工具/测试，不含被审核实现改动 |
| W2 基线 | `de2c04c3e690dba674850af9c4f7e126dbab910e` | 保全与历史包读取；不重复旧77项 |
| W1 实际受测实现 | `b6202bec015679461b06056571bd47a5446045b8` | 精确复现缓存反例 |
| W1 后续远端状态 | `2ef1a20354fdffd627aba8480f2dccd54ef1a729` | 只登记缓存修复开工；实际 src/tests/tools/apps/configs 未变，不能称已修复 |
| W3 R6 | `1bd513f51ab7e54a7290870a5f34b524254d55c6` | 原粒子反例、公开 CORRECT 正负路径、模型恢复前对照 |
| 期间新推送 W3 R7 批次一 | `8607a751cc482661baefa127968b4bc0758ec1d8` | 模型原件恢复，已另建独立目录实跑受影响50项 |
| R6 独立审核与实际攻击归档 | `97e1d2c1e83a4532e2b3bab3bf87cc6a2927f274` | 原始反例 helper、31个实际包及其逐包 CLI 结果 |

本任务分支 `codex/pc-a-w2-acceptance-prep-20260912`，目录 `/private/tmp/cpswm-pc-a-w2-acceptance-prep-20260912`；开工 STATUS 已提交并推送。目标目录分别为 `/private/tmp/cpswm-pc-a-w2-review-target-{w1,w2,w3,w3-r7}-20260912`，均是精确提交的独立审核副本，**不是统一目录**。没有读取活动窗口未提交实现来填补缺口。

`targets_and_preservation.json` 逐文件绑定受测源码、测试、配置、工具和工件；Git diff 与相应 SHA 一致。W2 原889项保全对象全部匹配，包含4个 v5包文件、401源码绑定和原77项测试/攻击构造器。之前77项和31类攻击的独立复核资格仍只对应其实际版本。

## 2. 本轮真实执行结果

所有完整命令、cwd、环境覆盖、起止时间、退出码分别在同目录 `*.command.json`，stdout/stderr 全文保留；下面每项均区分本轮运行与读历史记录。

| 工作 | 本轮结果 | 证据及边界 |
|---|---|---|
| 新检查器单元回归 | **71 passed** | `checker_tests_final.xml`；31种无操作拒绝+31种指定依赖合法变异+来源/逐包边界。不是原77项，不是生产能力测试 |
| R6 31类实际归档攻击 | **31/31 指定依赖非空改变** | `archived_mutation_values.json`，每项有选择器、首个真实差异值、前后摘要、来源一致性、历史真实 CLI 拒绝路径。**本轮读取历史逐包结果，没有重新执行31类正式重放** |
| W1原项目缓存反例 | 干净正例完成；unchecked与timestamp旧代码仍完成；同源码干净负例正确失败 | `w1_cache_results.json` 与四个 `cache_*` journal；原编排器/runtime未改 |
| W1 pytest断言缓存 | 种子通过；陈旧断言仍完成；清缓存同失败源码正确失败 | `w1_cached_test_results.json` 与三个 journal；不是用 batch exit 0 当修复通过 |
| W3原始公开边界脚本 | 合法 control / 极端alpha成功，总质量1；外部地点、未绑定投影、权重溢出明确拒绝且无状态/账本/默认动作副作用；原非法CORRECT完整回滚 | `w3_original_boundary.stdout.log`；原脚本字节来自审核提交，未改 |
| 独立新CORRECT夹具 | **合法同位置、合法异位置各真实提交；非法异位置完整回滚** | `w3_correction_records.stdout.log`：公开请求、贡献地点/质量、ledger记录、hybrid alpha、行动分布与身份均留存；重复请求不重复提交 |
| W3恢复前受影响组 | **33 passed / 7 failed / 10 errors，exit 1** | `w3_model_before.*`，缺模型/相关绑定导致，保留原失败 |
| W3 `8607a751…`恢复后同组 | **50 passed，exit 0** | `w3_model_restored.*`，确切新SHA独立运行；不是引用修复方50项总结 |

71项初跑与最终代码复查各有日志，不相加成142。CORRECT 首版已有正负三路径通过；为了补实际贡献记录，首次扩展导出因 ndarray 不能直接JSON序列化而 exit 1，保留 `w3_correction_final.stderr.log`。只修独立观察脚本，显式导出地点/owner质量和真实ledger/alpha，再运行得到上述最终结果。该失败是本窗口导出缺陷，不归因于W3生产实现。

W1环境 `uv sync --frozen --offline --extra dev` 初次因本机缓存权限 exit 2，批准访问缓存后成功；不是依赖版本随意变更。Python 3.13.5，W1使用其新审核环境；W3使用主仓库原生Python并明确指向相应快照src。50项与最终检查器使用新的 `PYTHONPYCACHEPREFIX`；保留 W1 故意构造恶意缓存的真实负例。单纯 `-B` / `PYTHONDONTWRITEBYTECODE` **不阻止读取旧缓存**，旧交接模板不能据此声称测试执行来源已经可信。本轮没有改 W2 已通过的 CLI 来源保护。

并行执行仅用于正确性检查，不从本轮耗时比较推导性能优势。部分审核脚本复用了交付中的历史构造器/叶层实现；本轮独立的是断言与观察，不是独立数学模型或训练历史托管证明。

### 2.1 W3模型恢复的精确接受边界

新工件 `artifacts/project_two_v04_development/structure_two_neural_amortized_model_v0_1.json` 与历史 `505e723170c29f8707ee19d113a5b8646680c5f6` 的 Git blob **逐字节相同**，本机计算 SHA-256 为 `5be8a0c5c87b2272049ed6e283e63feab12bcaa97b1f7b91ecaec69ad8986478`。没有自行补模型、重训练或改来源字段。恢复前后独立50项对照支持本批次解决缺失工件依赖。

这不证明原始训练托管独立、不证明更丰富七轴神经模型、真实投影消费或默认完整主干已实现。W3 STATUS 明确后续主干工作仍进行中。R6安全拒绝未绑定posterior projection是有效边界修复，不能升级为该消费能力已交付。

### 2.2 CORRECT的实际后果

夹具通过 `_legacy_history(12)` 公开生产历史形成非空贡献，不给P5额外预热，不注入私有状态或授权。合法案例取已存在合格父贡献；非法案例使用原反例的不合格历史目标。私有状态仅只读观察。

- 同位置纠正：旧贡献被撤回、新贡献以合法质量与来源提交，ledger lineage变化；alpha和行动分布保持一致。
- 异位置纠正：owner_mass `0.19836201405759452` 的贡献改到新位置；实际加权hybrid alpha在两位置间转移约 `0.3061142192246828`（不是将owner_mass与加权统计混为一项），行动分布随之改变；**top-1位置未变化，未执行环境动作**。不能将后验敏感性写成搜索/放回执行成功。
- 不合法纠正：`ValueError: CORRECT not admitted by CCRR; transaction rolled back`；原贡献仍在，新观察不存在，完整公开状态摘要、实际ledger、alpha、行动分布及Hybrid/CCRR/CF/particle workspace对象身份保持；不是“任何异常都算通过”。

这些只在普通生产lane局部成立。不能从有合法CORRECT推出direct P5默认连续流有非空长期学习，不能据其替换v5的原历史结论。

## 3. 独立肯定输出与覆盖矩阵

机器可读 [acceptance_matrix.json](acceptance_matrix.json) 包含20行，分为工程回归、真实非空能力、公平性、科学收益。**每行均列肯定输出、必要可信输入、真实生产者、消费者、必须产生的实际后果、合法正例、攻击/应不变项与当前状态。** 它是验收规格，不是自报布尔值即可授权的证书。

- 工程 E01–E03：实际运行源码与断言身份、生成→验证→消费顺序、完整伪造逐包判定。
- 能力 C01–C09：H/R/I/C/Z/r/V和三个RB blocks进入默认生产、稳定长期习惯、CIAV正负验证、多人物/未知/相似实例、真实阶段变化与复活、合法/非法CORRECT、完整修订生命周期、真实执行反馈、长期贡献污染。
- 公平 F01–F07：地图/支持权限、缺owner先验、实际特征消费、独立调参访问时序、容量与在线预算、真实SEARCH读出、共享CIAV预算。
- 科学 S01：冻结PCT与guardrails、0.02信号门、已授权的确认数据；当前NOT_ESTABLISHED。

完整能力正路径必须在**同一实际默认系统的连续历史**形成非空链：合法输入→联合状态/三RB→七算子消费→后验→唯一长期账本→已执行行动→世界后果→后续修订。逐算子被调用、两个哈希不同、普通reference写入、prepared接口安全拒绝，均不足以替代这条链。污染按真实长期贡献质量测，不能用当前位置错率代理；近似后验与重放采用已冻结容差，不能看完结果再选容差。所有场景保留普通例和强基线，不制造P5优势。

## 4. 旧“缺陷应存在”断言的迁移计划

本轮不修改旧源码上的预期。详尽函数清单保留在 W2 handoff contract，本轮新增可执行 `probe_correction.py` 验证了其中一部分修复后的真实正负路径。

| 旧断言/消费者 | 修复后应改为 | 当前操作 |
|---|---|---|
| dynamic `test_current_production_revision_defect_is_not_promoted_to_success`、`boundary_probes` 要求 old/new均未提交 | 合格父项+合法纠正真实提交；非法请求完整回滚。区分直达拒绝与公开request允许的隔离/延期状态；重复无双计数 | 已有独立公开直达3例；B在实际统一源码更新消费者模式，不在旧W2强行改绿 |
| `test_real_long_term_positive_is_not_misattributed_to_direct_p5`、same-location fullP5测试要求 committed=0 | 保留reference不能冒充P5；默认P5需按真实资格达到必要非空条件，且长期贡献对后验/行动有可辨识作用 | 默认完整主干未满足；不要移植普通reference或强制grant |
| AMG native SEARCH / learned joint 扰动被丢弃、逐步SEARCH相同 | 合法方法后验经冻结公共解码器有敏感性；审查adapter是否保留真实预测，不擅改共同解码合同 | 原历史缺陷仍有效；权限/接口科研选择待确认 |
| full/fast相同、execution=MISSING | 预声明会区分长期贡献的合法连续输入，实际读出和环境后果可观察；未触发仍报MISSING | 未强制动作差异、不做局部替代执行器 |
| ordered_role未消费、旧验证消费者必须早读失败 | 更新后检查实际允许字段消费/相关干预；验证真值仍在动作提交后开放，旧实现不再被当永久oracle | 不改B模块；移交回归要求 |
| 缺owner不同点质量、未来支持集存在 | 按用户明确权限/先验规则检验，保留原始及开发共同规则两套结果 | 不默认选因果支持/共同先验；重验17步归因只是未来对账起点 |

`corrected_memory=True` 在合法纠正已经提交的新版本可能是无操作。检查器必须先拒绝这种“攻击”，再由B按新实际账本/资格状态构造真正改变结论的变异。`action_consequence=PASSED` 同理。不能删除攻击意图、保留旧source过期、或硬编码85/102/999来宣称修复后验证充分。

## 5. 31类完整伪造的当前证据与未来运行合同

本轮不重构历史包；从审核提交的 `w2_actual_attack_packages.tar.gz` **在内存只读检查**，不解压覆盖旧目录、不重训练。每类选择器直接指向参与结论的字段：记忆贡献、后验/动作/真值、步骤/episode顺序和数量、输入/选择/划分、来源/schema、归因、支持/owner/cost、消费记录、公平声明及动态后果。结果包含可查看的前/后实际差异值，而非只说两个哈希不同。

31中 `source_binding` 本来就是明确的来源元数据攻击，应因来源异常拒绝；其余30项的archive源码绑定与合法参考一致。R6保留的三批CLI各有合法参考MATCH、攻击逐包REJECTED；新检查器逐包核验了这些**历史记录**，并拒绝missing/duplicate、合法参考失败、攻击通过、缺错误原因、弱验证或对语义攻击仅靠旧源码不匹配的结果。不能用攻击批次整体exit 1替代逐包检查。日志JSON自身不是可信新鲜重放输入，`check_cli_batch` 只做记录检查，不能签发认证。

正式新运行必须：

1. A公布已审查的实际统一SHA，B消费者已按真实新行为更新；固定解释器、依赖、源码及测试加载身份、配置与全部输出域。
2. 全新目录生成主比较与归因，两个真实CLI完整重放，合法参考先通过；不能重写v5/source字段。
3. 基于该**新包**构造31类意图及新增依赖/状态机攻击；逐项 `mutation_witness` 指定结论依赖必须真的改变，无操作立即失败，不进入攻击计数；同步重算所有可自算依赖。
4. 单包/混合批次经真实CLI拒绝；分开统计语义拒绝与明确来源攻击。相同新包在每批保留合法正例，不能仅靠旧绑定被拒。
5. 源码与输入包前后不变；真实攻击包在临时目录清理前归档，保存其文件索引、摘要、完整命令/环境/加载身份、stdout/stderr/JUnit、逐包错误位置。此处本轮没有执行，保持BLOCKED。

重算还须含主比较1920步对账、归因四类分解/17差额与共同规则、动态非空机制场景、来源保护、全部受影响回归及W1实际生产者—消费者链。旧v5与R6归档是历史参考，不能作为新统一版本的验收包。

## 6. 公平性：冻结约束、未决项和最小区分实验

| 项 | 冻结/当前边界 | 候选与最小区分实验（未执行，未代用户决定） |
|---|---|---|
| 公共地图/候选支持 | 当前D0缺catalog时全episode支持含未来；科学权限尚未明确 | 明确公开完整地图 vs 仅因果前缀及未知桶；固定观察与方法，加入合法未来首次出现位置对照，记录实际支持来源/时间 |
| 缺owner先验 | 原17步差额来自默认规则；共同先验仅开发对照 | 明确共享无信息先验 vs保留方法原生先验并显式报告；同一缺owner前缀追踪真实posterior/动作，不选对P5有利规则 |
| 真实特征与训练 | 共同合法证据、训练限定train、validation先行动后真值、各臂独立选择现有网格 | 逐字段实际消费/值、fit次数、模型选择访问轨迹；目标/网格如欲改变需新决策，不能让弱learned成为不公平的唯一证据 |
| SEARCH | 共同类型化解码器、平局语义已冻结；当前adapter共同detected读出掩盖原生差异 | 在相同观测/支持/解码器下，合法方法posterior跨决策边界；若需要改变方法接口，先明确合同，替代读出单列development-only |
| CIAV及动作 | 共享候选/结果/时间成本/隐私、提交先于真值，不擅改预算 | 记录真实预算消费及动作提交/执行/反馈；零观测成本不解释成真实净效用 |
| 模型容量/训练与在线计算 | 当前配置预算可以逐项实测，但信息匹配不等于等算力 | 固定参数量、fit数、运算量、cache/warmup、实际硬件/重复次数/测量范围；若要同参数或同在线预算择一比较须用户明确。并行窗口墙钟不用作优越证据 |
| 长期账本/行为 | CCRR资格、RGRC唯一写撤权、拒绝原子性按已冻结合同执行 | 稳定/变化/迟到纠正的同历史三臂，真实非空贡献及错误质量；分别量后验、top1、执行成功和世界状态，不强制非零或替换生产主干 |
| 最终科学效用 | PCT主效用、guardrail、0.02信号门已有冻结协议 | 不重新选指标、不重开已打开D0；正式七算子消融与密封确认等待门和授权，不启动router calibration |

## 7. 使用说明、交接与阻塞

运行新夹具使用 Python 3.13，不依赖某个假定统一目录：

```sh
# HISTORICAL_BUNDLE为de2c04c...中成功v5；REPO包含指定审核Git对象；OUT必须全新。
"$PY" tools/pc_a_w2_acceptance/inspect_archived_mutations.py \
  --repository "$REPO" --historical-reference "$HISTORICAL_BUNDLE" --output "$OUT"
# ROOT必须为明确审查的W3实际源码；这不是三臂实验。
PYTHONPATH="$ROOT/src" "$PY" tools/pc_a_w2_acceptance/probe_correction.py --root "$ROOT"
"$PY" -m pytest -q -o addopts= tests/pc_a_w2_acceptance/test_mutation_witness.py
```

验收运行须使用全新缓存与输出域，不能只依赖上述最短调用获得执行来源认证；完整本轮参数参照各 `.command.json`。`run_recorded.py` 排他创建该次日志，记录真实exit；新失败必须新label留存，不能覆盖原记录。它不是统一编排器或科学门。W1原缓存探针复制了独立环境并制造普通Git夹具，修改仅在一次性夹具，不修改审核目标文件。

当前阻塞：集成分支没有公布修复后审查统一SHA；B专属比较消费者未按修复后的CORRECT/default-path更新；W1缓存修复只有开工状态；W3完整默认联合主干、生命周期与行动闭环仍进行中；B0/B1尚无本窗口可代签的Windows/WSL结果。新科研权限/先验/预算选择仍交用户。**不因局部实跑通过启动正式新比较。**

交给A：复核并整合各独立交付、公布统一SHA，完善W1控制器/worker实际代码身份，按相应前置门闭合生产路径。交给B：保留旧77意图并更新旧缺陷快照，使用本矩阵/变异见证，生成新的真实参考包和实际攻击归档，独立复核实际统一源码。PR只交接本窗口工具/证据/STATUS，不自动合并任何窗口。

失效清单：一旦实际源码或消费者变化，旧主比较/归因、source验证正例、完整伪造矩阵、动态导出、数值对账、依赖这些产物的统一receipt/checkpoint都不能继承“当前版本通过”。历史字节保持不变；按生产者顺序在新输出域完整重算，而不是只改哈希/manifest。工程回归、局部非空能力、公平和科学结论必须继续分开。
