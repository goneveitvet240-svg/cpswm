# 窗口一：实际入口身份与历史报告跨目录复核

本轮从 `816a88b242c24b9c6ace6021bba23e4b5b8b526e` 继续，工作树为 `/private/tmp/cpswm-s2-evidence-repair-window1`，分支为 `codex/s2-evidence-repair-window1-20260911`。独立审核报告已完整阅读。仅修本窗口，不合并、不推送，不签发消融授权。

## 实际入口身份

根因是旧启动器只把磁盘入口纳入来源清单，没有比较正在执行的入口代码。修改前在独立副本中将旧诊断入口编译为 unchecked pyc，恢复精确磁盘源码后，`python -m apps.evaluation_runner.probe_structure_two_execution_source` 仍执行旧标记，同时取得 `frozen-source-compile-no-pyc@1`。文件入口没有旧标记。完整原始输出与子命令退出码在新证据目录 `validation_logs/before_reproductions_complete.stdout.log.gz`。

`structure_two_source_bootstrap.py` 现在在安装正式 guard 前，检查调用帧确为支持列表中的模块顶层、实际文件位置与模块 `__file__` 相符，且完整代码对象（包括嵌套函数、常量与控制流）等于对冻结字节的重新编译。同时核对正在执行的启动器代码自身，避免启动器加载后、冻结前的漂移。已有 guard 再次被入口使用时仍核对；后续 require 和安全发布前后继续检查全源码清单与实际导入实例。

来源策略升级为 `frozen-source-and-entry-compile@2`。支持正常文件、模块 `-m`、执行同一真实入口代码的 runpy 及 multiprocessing spawn。`-c` 直接自行建立来源、外来文件以及不匹配的旧缓存明确拒绝；`-c` 通过 runpy 执行真实支持入口时，仍必须通过该入口完整代码核对。程序不删除 pycache；正确缓存可以通过，带错误实现的缓存不能取得当前身份。仅更改编译缓存中的文件名但代码完全相同，不代表执行了不同实现；CPython 会在正常加载时修正缓存文件名，负例必须真的包含不同代码。

原测试中的临时 `-c` 启动改为执行真实受支持诊断入口，后续攻击与断言保留；根 conftest 明确关闭仅对自身的 pytest assertion rewriting，确保启动代码能精确核对，测试模块的断言重写不变。诊断入口增加 `--spawn`，只观察子进程实际来源和默认语义，不运行新科学实验。

这仍以可信解释器、标准库和进程完整性为边界。代码对象核对不是外部执行认证、签名或独立托管；不抵御任意进程内 monkeypatch、伪造执行帧、解释器/操作系统控制。通过受支持入口启动后，调用者的任意外部代码也不会因此被纳入被验证实现。

## 历史报告跨目录复核

修改前真实跨目录历史 `--verify` 完成 11 条 Git 字节核验与 1 次指定失败完整重放，随后因启动器堆栈根目录差异退出 1。原脚本只正则替换历史解包目录，遗漏工作树内启动器路径。此失败不否定此前数值结果或真实性，不据此指控既有实验造假。

新报告格式为 `structure-two-historical-local-git-audit@0.3`。生成时只规范化已知 Python 栈帧文件路径以及已知 ImportError 导入来源尾部路径：当前启动器用 `<audit-source>/apps/...`，历史文件用 `<historical-source:完整commit>/src/...`。保留完整 traceback、行号、函数、代码摘录、异常类型、错误正文、缺失符号与历史 commit。无关路径原样保留。验证阶段不再对调用方报告作宽泛路径替换，采用严格规范 JSON 比较，整数 1 与布尔 true 也不同。

报告另绑定完整审计执行源码清单，生成前后核对。历史子进程只从真实 Git 源码快照编译旧依赖，不安装可签发当前身份的正式 guard；没有把新依赖塞进旧版本。11 条固定历史身份映射、安全发布与数值完整相等检查不变。`4103bea` 仍须完整重算；最早 `557ff9c` 的 RegimeStage 导入失败仍保留。Git 字节相等、源码可恢复与数值重放完成分别统计，本地 Git 仍不是首次执行、首次未见或独立托管证明。

## 版本与保全

新五类结果只发布到 `benchmarks/structure_two/evidence_entry_portability_2026_09_12/current_v0_4/`；历史报告到同目录上级 `historical_source_audit_v0_3.json`。新工程链位于 `benchmarks/structure_two/engineering_trust_checkpoint_2026_09_12_v0_3/`。

科学协议、阈值、种子、正式基线、长期提交规则与七算子算法均不变。v0.4 是证据产物版本，保留 `confirmatory=false`、`previously_unseen_established=false`、`first_execution_established=false`，D0 仍为开启后重放。旧 v0.2、v0.3、11 份原始记录、旧历史报告、旧检查点和回执保留原字节。可变的 P0 当前路径和 v0.5 兼容性审核在新目录中另存本轮已审核原字节；210 项保全清单逐项绑定 Git 816a88b。

## 验证与交接

补修源码与测试提交：`05c78af2a38e07e0171c5352bf4ab9b9da3124d2`。修正后的原 79 项加新增 36 项整组运行于 2026-09-11 18:29:09 至 18:53:24 UTC，实际 **115 passed，0 failed / skipped / xfailed**，pytest 计时 1455.54 秒。源码随后保持固定。新增 36 项包括 15 个文件/模块/runpy/spawn 与合法缓存正例、6 个错误入口缓存负例、3 个无资格入口/执行期间漂移负例、9 个正式入口逐一核验、1 个启动器冻结前漂移、1 个旧 v0.3 保全/拒绝冒充以及 1 个真实跨目录完整历史回放与成组对抗验证。


跨目录逐字验证另行完成于 18:33:40 至 18:56:01 UTC：13 个真实子进程中，目录 A 完整生成、目录 B 完整验证均 exit 0；9 个报告字段反例、1 个真实源码替换、1 个重哈希历史数值替换均按预期 exit 1。换源反例仍实际完成 4103bea 全量重算且数值匹配，只因审计来源不同而拒绝。参考封存报告为 `cross_root_reference_report_v0_3.json`，文件 SHA-256 为 `5d07ca09774950f25bd6968a57ce7cb3fdd400ee94a9420082de34a3ee15129d`。这些计数属于同一跨目录回归的详细执行记录，不另加进 115 项 pytest 总数。

五类 v0.4 正式生成（每份发布前另作完整重算）以及存盘后的统一复核均 exit 0。五份科学字段与已审核 v0.3 严格相等，仅来源、证据上下文与随之变化的外层摘要不同。11 份历史记录保全，4103bea 完整重算相等，557ff9c 的真实 RegimeStage 导入失败保留；本工作树新历史报告与独立目录参考报告字节完全相同。210 项保全核验通过。

实际工程审计的 10 个命令均 exit 0：P0 对抗测试 **121 passed**；完整核心测试 **3944 passed、1 skipped、1 xfailed**（1616.38 秒）；mypy 检查 310 个源码文件通过，Ruff lint/format、compileall、冻结离线 uv 环境检查与 git diff 检查通过。核心命令按既有依赖顺序暂不运行依赖新检查点自身的测试文件，后者在生成新检查点后单独完整运行。沙箱受限的原用例在宿主权限下使用同一工作树和虚拟环境复核，得到 **1 passed、1 xfailed、0 skipped**（14.77 秒）；没有改断言，RLS 的既有 xfail 保留，不将其当作方法有效。

本轮 P0 manifest SHA-256 为 `c87357cee49ff7a712f615773ed66b422cd2e506a810483a412898e0d155af12`，工程审计回执内容摘要为 `9b7aa855ac1c9d4f39c888b4372019dc1b899b4b128b032cf43066bbec236338`。审计前后 P0 与真实环境指纹均相等；环境指纹为 `bba78a7a15b6c7549995038d89482580b105b702aeaa1775b5e5118eb60b563d`，使用本工作树 `.venv/bin/python` 的 Python 3.13.5。执行源码清单摘要为 `d845c1216cd7ddb5295c6e311dcec5ccc826c3d4990788478c8bb448a2dea1a4`，全 src 与环境锁的生产装配摘要为 `914b77705a89e389b8800d56ba33e17bb54222b8e5924930ef87374e69abd1b8`。没有伪造工作目录或环境指纹。

本报告在新检查点生成前固定；实际命令、退出码、完成时间、日志摘要和最终统计在同目录 `validation_logs/commands.jsonl` 及封存闭环报告中记录。不得以中断、部分完成或快速当前性复核冒充完整重算。正式顺序是原回归和新增回归、五类真实生成及新鲜重算、历史来源及失败重放、P0、实际审计矩阵、强制 fresh checkpoint、当前性与检查点对抗回归。

主要重叠位置是共用启动器、历史审核入口、P5 五个 DEFAULT_OUTPUT、证据版本模块、根 conftest、P0/工程回执/检查点生成器和相关测试。没有修改窗口二、三的比较或算子接线。新的来源代码使旧 v0.3 与旧检查点失去“对应当前源码”的身份，它们仍是对应旧版本的历史证据；不能改旧摘要使其冒充当前。

三个窗口整合后，必须在统一源码与实际匹配环境上重新生成五类当前产物、历史审计、窗口二和三各自必要的验证、P0、真实工程审计回执及完整 fresh checkpoint。不得拼接三个分支的绿色结果，不授予方法有效、科学门通过、七算子全面有效或穷尽 CPSWM 审计的结论。
