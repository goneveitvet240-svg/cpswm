# 第二审实际反例与修复

修前生产代码来自PR43 f72413fdd55bb3324ca064c02ba1fd8507aba54b（HFD仍为PR42修后实现）。审查脚本98111ce885ffbe899161626e1bb87739e651a26e首次12失败/7通过，但其中1例是AF_UNIX绝对路径过长的夹具搭建失败，不能算生产缺陷。改用临时目录内短相对socket地址，冻结eb64e65287660f5366ac7d48e6cd42c33cfccfe6，生产源码未改，沙箱内复跑仍12失败/7通过，但socket被EPERM阻断，实际11个行为反例＋1个环境失败。随后在该固定历史SHA的独立只读检出中，明确校验导入路径并在获准创建本地socket的环境重跑，才获得12个实际反例/7通过，见second-before-local-socket.log。

1. 完整合法包额外FIFO和Unix socket被普通文件清单忽略（2例）。修复以lstat明确区分普通文件/目录，拒绝其他类型，保留软/硬链接拒绝。
2. 最后check_members返回后重写已核验原始数组或添加额外文件，创建和verify均仍成功（4例）。修复最终内容校验后的第二次身份清单比较，创建失败不留下接受manifest，合法新目录重试与修复后的原包核验继续可用。这是受控校验过程的漂移检测，不宣称能防御恶意操作系统或任意时刻的外部改写。
3. 复数时钟被float转换静默丢虚部，无符号相减回绕使相机/力时钟倒退仍被接受（3例）。改为明确实数数值dtype、直接相邻比较单调性，不隐式更正原数据。
4. 字符串力、复数力、非对象metadata使整批非预期异常退出，未按既定隔离合同保留可用试次（3例）。先验证数值dtype和JSON对象，明确ValueError进入原始保留/分母不变的隔离路径。

另外7例原已通过，覆盖硬/软链接、完全重封的隔离提升/不同有效视频交换/科学权限提升，以及重复JSON键和真实解码计数错配，合法恢复后重新核验。修后c10a311430ff68ecce4aea0eaa6b3039e2f88974的final-01首审32通过，二审18通过/1个socket沙箱EPERM；该轮未验收。final-02在允许同一临时本地socket夹具的环境从首审完整重跑，无Python变更；双审及真实结果以最终回执为准。
