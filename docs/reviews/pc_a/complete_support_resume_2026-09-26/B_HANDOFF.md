# B 独立复核入口（A 完整矩阵已封存，B 未复核）

本页不登记 B 通过。受审生产/测试/工具 Python 源码为 `7c19637791e45dbf28e041beb3b98bc9acd92038`；后续文档提交不改变该源码。B 应从 GitHub 获取完整 SHA，在自己的独立 `codex/pc-b-*` 分支/目录复核，并记录系统、Python/PyTorch版本及CPU线程数。共享集成不在本轮自动合并范围。

## 所需资料

1. `uv.lock` 对应的 Python 3.13.5 与 dev/perception/hand-perception 依赖，重建本机虚拟环境，不复制A的macOS环境或工作树.git指针。
2. 原始三臂 fixture 权重：`output/joint-audit-next-20260925/round2/training`，原权重和训练来源以检查点manifest核对；此处没有新优化步数。
3. 已登记四段 `bimanual-phase-four-clips-20260920` 原视频和 `fasterrcnn_resnet50_fpn_v2_coco-dd69338a.pth`。来源、SHA及1920×960去答案裁剪仍由原策略检查，不可替换成随手找的片段。
4. 原第三段失败的完整支持/context工件：`output/full-support-compute-20260926/final-03/video-0003`。这是实际216→783输入的失败恢复检查根，不应用小夹具替代。
5. A完整命令回执、source-before/after、全矩阵结果及大工件逐文件SHA清单。大工件位于A主仓被忽略的output；Git中只有精简报告。资料不齐时明确记缺失，不能把单元测试通过写成真实矩阵复现。

## 两种环境的执行方式

A的 `run_frozen_validation.py` 使用 `.venv/bin/python`、mypy、ruff 的macOS路径。Windows不能原样运行此包装器，也不能因此绕过某条检查。可按 `final-04/commands.json` 的相同顺序逐条执行：将解释器替换为本机 `.venv/Scripts/python.exe`，mypy/ruff用本机 `python -m mypy` / `python -m ruff`，把ROOT、main原资料根和output换成本机绝对路径。其余参数、测试列表、4帧、3臂、65,536显式配置、旧32,768默认配置及1e-5容差全部保持。

逐条顺序为：第一审及相关测试 → 第二审及既有完整伪造 → mypy/Ruff → 显式配置派生 → 三臂303密集/分块对照 → 默认配置派生 → 实际原失败拒绝/非空历史回滚/后续RNG → 四视频各全三臂。每条保留退出码和日志；任意一条失败表示整个固定矩阵未通过。运行前后保存实际源码SHA及工作树差异；不得在运行时修改源码。

## 需要独立挑战的有后果路径

- 全部键值、完整支持及原始权重是否保持，条件概率是否符合完整归一化；质量1不是充分条件。
- 完整重封检查点/历史/权限、实际已加载自有代码和缓存对应关系漂移是否被拒绝。
- 已有合法历史后的资源失败，完整快照和下一次随机采样是否保持；同请求合法重试是否可用。
- 四段中的失败或缺失是否被总入口显式保留；自然语义/粒子发布/动作是否仍为0。

跨硬件/PyTorch版本不要求与A逐位一致；应在各自同一执行环境比较在线和恢复，并独立执行既定数值容差。该复核仍不证明自然准确率、校准、长时在线可扩展性或科学收益。完整统一框架与未定科研选择不变。
