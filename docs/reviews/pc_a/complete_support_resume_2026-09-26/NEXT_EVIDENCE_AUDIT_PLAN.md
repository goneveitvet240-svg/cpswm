# 完成本轮后再执行的证据入口第二审计划

此文件仅为静态阅读所得计划，不是运行结果，也不表示已开始下一轮实现。源文件 full_hfd_training.py 在本轮冻结期间不修改。

1. 保留正常/失败/隔离三类案例的完整分母，验证重封清单不能把隔离试次提升为作者正常结果或精确接触真值；验证后合法原包可继续核验。
2. 检查硬链接、目录/文件符号链接、特殊文件与不在清单中的成员；单独核对返回成功前后的目录和成员身份，避免校验期间后写漂移仍留下可用接受清单。当前packet_inventory只返回普通文件，特殊文件是否被明确拒绝需要实际攻击验证。
3. 检查视频与标签跨试次交换、JSON重复键、实际解码帧数、非数值数组和元数据错配。应保留原始问题并隔离或明确失败，不删除不利样本。
4. 原始作者归档完成全包校验后才进入337试次处理；从原始归档重新生成所有接受行和清单，不能只检查包自己的自洽摘要。新真实兼容性修复须重冻后顺序重做双审。
5. 检查在线数据类型和下游权限：作者标注只进入evaluator工件，原生发布、完整提议训练就绪、身份/位姿校准和精确接触/释放真值保持false；A工程审核与B独立验收分开。

下载准备保留所有373旧分片及首次SSL失败，续传不重新索要用户标注，也不访问作者validation/test分区。作者正常结果目录90项与实际可用正常视频的数量分别记录。
