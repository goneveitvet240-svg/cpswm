# 独立审核一：监督对齐诊断组件

审核日期：2026-09-20。固定源码 SHA：`a9ea12aa7f234a84a57539bdc47ffea734b16ea4`；基线 `65cf2bfecfd1a2969f6c6ad77246c34f24797899`。
审核者为本任务独立子代理；未修改生产代码、未提交，不代表电脑 B。

## 结论

在本轮组件范围内未发现阻断问题。结论限于“固定输入的评价侧结构诊断、拒绝不安全数组、不把对齐候选升级成真值”。不证明发布版时间/身份对应已解决，也不证明自然语义生产或 P5 闭环成立。

## 独立执行证据

- `.venv/bin/python -m pytest -q tests/test_supervision_alignment.py`：12 项通过，0 失败。
- 从三份实际清单重新计算 SHA，独立运行 `tools/audit_core4d_supervision_alignment.py` 全部两个序列。新输出 `/private/tmp/cpswm-review1-alignment-a9ea12`，`summary.json` 源码 SHA 为上述固定版本，状态 `UNVERIFIED_ALIGNMENT`、接受帧链接为空、语义转移为 0。
- 额外手工攻击检查共 5 类：超额 NPY shape、负数 shape、object dtype、截断数值 payload 均被拒绝；具有一致尺寸、7 列及在范围内索引的伪造输入仍输出空 accepted_frame_links，各候选 accepted=false。这些检查不计入上述 12 项 pytest 数量。
- 对两份原始位姿数组另用旋转矩阵奇异值是否接近 1 的方法核查，未复用工具的正交性残差实现。023 异常行为零；024 异常零基行精确为 130、131、132、133、136、180、181，与独立运行输出一致。未改写或修复原文件。
- 实际分割形状分别为 `[60,1080,1920]` 与 `[70,1080,1920]`；两个序列的 event_labels、identity_labels 和 accepted_frame_links 都为空。

实际三份清单 SHA：

- raw：`f949b5bc8ac6c0297fded51db9eb4e3d525cf3499931daa596c0765b222026e0`
- motion：`fef29ac8945da8e4818c3d791cda627c56ea775432ea4da577a6fbdc72eb55e9`
- mask：`6ce8f10d991ff8c8daf6c2aaeed19520ae78781d547f648c525723cd05aca064`

## 审核依据和边界

成员唯一性、解析后路径位于指定 lane、文件字节摘要及大小均检查；原始视频由核验后的内存字节写入临时副本再解码。分割 ZIP 限定单个 `arr_0.npy` 和解压大小，NPY 先验查 dtype/shape/实际字节长度且禁用 pickle。没有作者标签回流运行时的路径。

时间戳以整数先相减，播放 PTS 单独保留；对齐表列数匹配只报告结构，未赋予列语义。边界支持为探索分数，源码无接受阈值及标签生产出口；静态边缘、高分候选和缺失分割值均未转为角色或接触证据。位姿错误保留并报告，未静默投影到旋转群。

哈希只确认指定清单和字节的一致性，不证明作者真实性；全量档案 LFS 摘要、相机时钟标定、身份协议和独立逐帧事件真值仍未验证。本工具记录 Git HEAD 及两份实际源码文件摘要，不是整个依赖环境的可重现构建证明。现有合法/拒绝测试和本次攻击检查不是任意 ZIP/NPY 全面的模糊测试。以上限制不影响本轮保守的未验证结论。

## 修复后固定版本复验（替代上面的初版通过结论）

最终固定 SHA：`3534e9796bb2aae99b594a330671a1328ddf5ccf`。第二独立审核发现初版 Sobel 同时平滑时间轴；本轮第一审核未发现该问题，初版边界评分不能用作最终结果。以下为独立复验，不将初版无阻断结论当作最终接受依据。

- 审查 a9ea12→3534e9 完整实现及测试差异，确认 Sobel 仅接收单帧二维数组；ffmpeg 明确 `-fps_mode passthrough`。
- 重新执行 `tests/test_supervision_alignment.py`：18 项通过，0 失败（包含新增 6 个参数化用例；不是 12+18 个不同用例）。
- 额外固定随机数的 5 帧 × 3 张掩码实验：替换第 0 帧后其余帧分数逐位一致；分别调用单帧与批处理最大差异 4.44e-16，属于归约布局的浮点舍入，1e-14 绝对容差比较通过。
- 独立以 float16、float32、float64、longdouble 各构建一个单位矩阵和一个该类型最大有限值矩阵：合法行通过、异常行均被标记、严格 JSON 序列化成功、原数组逐元素保持不变。float64 溢出诊断不输出 inf/NaN 数字字段。
- 使用相同三份已绑定清单，在最终 SHA 独立重跑真实双序列；输出 `/private/tmp/cpswm-review1-alignment-3534e9`。`summary.json` 源码 SHA 正确，状态仍 UNVERIFIED_ALIGNMENT；两段所有事件标签、身份标签及已接受链接为空。023 非刚体行为 0，024 仍为同一 7 行。

最终组件范围未发现未解决的阻断问题。边界分数仅探索信息，初版分数撤回，不允许混入最终表格或评价。时间/身份对齐与逐帧事件真值仍未成立；上述审核仍不代表 B 复核、自然 P5 验收或科学收益。
