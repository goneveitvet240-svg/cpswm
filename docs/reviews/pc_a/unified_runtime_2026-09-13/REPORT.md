# 统一运行候选：源码合流、真实输入入口与启动修复

本批目标来自用户：来源明确、真实输入驱动、能连续修订并影响行动的统一运行版本。**当前交付仍是候选工程进展，完整目标未完成，不能签收统一运行或科学收益。**

## 源码与接收边界

- A 基线：`05836759cd96b89011efca71d3e9dc3ab393ce1b`，含最新 Unity 构建、真实采集、G1 数据边界、原生联合消费者和 R7 主干。
- B 五边界：`c366cad0a92593bb2c4c44db7e8d3408a2b5cc9c`，在该完整 SHA 独立复核后接收，见 B_REVIEW.md。
- W1：`21870b0bcd6c23d43518a27fcc1c4b538b3912b7`；W2：`de2c04c3e690dba674850af9c4f7e126dbab910e`。仅汇入候选，不将旧证书移植为新源码验收。旧快照清单分别保存在 docs/collaboration/component_snapshots/。
- 最新候选代码：`06366579a1a72a46b465194b6e1bc2b6c27a3e87`。主干/库代码仍与回归 SHA `01d44d58a691d4144ffaaa3c23b9aa414115d977` 相同；两版本在 src/tests/tools/apps/configs/artifacts 范围仅 CLI 修复与其新测试有差异。后续报告提交不修改该代码；每次执行的文件摘要、命令、环境和源码前后比较独立存档。
- 分支：`codex/pc-a-unified-runtime-20260913`。共享集成分支仍为 `codex/dual-pc-handoff-20260912`，未替换或自动合并。

## 本批实际实现

1. 新增原始 RGB-D 到既有 M05 ObservationEnvelope 的适配器：核验外部保留的捕获回执摘要、真实传感字节摘要、run 身份、类型和因果时间边界；方法侧只获得像素/深度及观测身份，不导出 evaluator 元数据。不可变字节与独立重建的 envelope 防止验证后修改。外部摘要的真实性仍须由实际采集所有者负责，schema 不自证来源。
2. 新增固定 Git SHA 的归档回放入口。实际从既有 ProcTHOR run_07 读取 36 次已释放捕获，导出 72 个 RGB/depth 观测。保留逻辑到达序列及实际导入时间。这是已发生的模拟器原始传感记录回放，不是新在线模拟器运行、真实世界相机观测、语义识别或长期学习。
3. 修复独立启动循环导入：原先直接首先导入生产主干/粒子工作区会经 evaluation_operations 包的急切导出回到未初始化主干。改为保持原 388 个公共导出的按需加载，增加五种全新进程首导入、完整导出同一性和公开生产转换启动检查。最后一项明确使用合成组件夹具，不冒充真实闭环。
4. 新增带源码前后摘要的检查记录器，并修复其初版强制 PYTHONPATH/PYTHONDONTWRITEBYTECODE 导致 W1 环境身份拒绝的问题。正式原生检查使用候选自己的锁定 .venv，移除这两项覆盖。该记录器不是 W1 正式统一签收器。

## 测试与失败保全

最终机器可读汇总见 CHECK_RESULTS.json；原始命令/结果/JUnit/stdout/stderr 见 evidence/。

- B 固定源码独立复核：139 项定向检查通过；原 812 项主干矩阵全部通过。此为 macOS 接收复核，不能覆盖 B 既有 Windows 800/812 结果与未关闭的 CRLF/锁超时边界。
- 新入口与启动：修复前 24 项通过；第一轮审核修复后的 `fd516c3` 版本 30 项通过（增加 6 项真实 CLI 合法/伪造/迟到/重复/非提交 SHA/失败不发回执检查），源码前后不变；两个新增/修改生产模块的 Ruff、mypy 通过。
- `01d44d5` 的主干/新增组件回归：1170 passed / 11 warnings，673.70 秒，源码前后不变；警告来自故意构造无效模型的对抗测试。该矩阵不含 W1 三文件和依赖新产物的 W2 公平性消费者。
- `01d44d5` 的 W1 原生环境检查：85 项通过，包括执行身份、依赖与缓存攻击。没有执行全量正式统一验收，也没有由此签收 W2/W3 或科学门。
- 第一轮整合矩阵保存 1235 passed / 29 failed：28 项由错误的强制环境注入导致，1 项比较攻击读取旧 bundle_v2 缺少 fairness_step。通过单例有/无环境覆盖重现区别后，切换干净原生环境，并对当前源码生成新比较包；旧失败日志保留。另保留新入口初版测试的 16 passed / 1 failed，该失败是直接赋值给既有 frozen Pydantic 模型的测试错误，修正测试后 17 passed，未放宽生产验证。
- W2 旧测试含“CORRECT 后新旧都不提交”的预期缺陷断言；实际重跑 1 failed，在读取 operations 前失败。独立保留的 boundary_probes 明确是 `CORRECT not admitted by CCRR; transaction rolled back`，且 semantic_unchanged=true：旧输入未获修订授权，不是已证明合法纠正完成。RETRACT 返回并改变状态；重复/倒序拒绝且状态不变。按任务板由 B 更新探针，使其具有当前合法正路径并复核；A 不偷偷改变 B 的比较模块或科学协议。新生成包与旧证书分开，不将文件自洽等同于真实重算。

## 距离用户目标的实际缺口

| 链条 | 已有事实 | 尚缺的生产能力/证据 |
|---|---|---|
| 来源 → 原始输入 | 固定源码/回执/传感字节；原始观测适配和归档回放 | 在线采集所有者独立保留来源；同一生产运行默认入口 |
| 像素 → 对象/人物/角色证据 | RGB-D 可读；当前 perception_mapping 只有原始/合成适配和校准合同 | 实际语义感知、实例关联、人物可见动作与校准；robot agent 不算人物 |
| 证据 → 全轴提议 | 完整合同、G1 边界和修订组件 | 真实六操作监督、全轴模型及完整条件 log-q；现有原因/阶段 MLP 不能替代 |
| 提议 → 三个 RB blocks | 粒子工作区、条件更新及可逆账本组件 | 冻结物理状态与测量模型、实测噪声、默认自动生产/更新 |
| 后验 → 行动 → 反馈 | 联合消费者、实际七算子、修订及执行接口 | 从上述真实输入贯通默认路径，在同一连续历史改变实际行动并观察反馈 |
| 连续修订与验收 | 已有组件级取消/恢复/迟到纠正及源绑定测试 | 全链正路径、完整伪造、依赖/状态机、中断、账本与行动后果矩阵；B 对最终源码独立复核 |

不会以手填后验、真值充当感知、常数信息矩阵、六个物理动作名充当六操作标签、缩减轴或改科学门补齐这些缺口。工程候选通过与统一能力完成明确分开。

## 可继续的具体依赖

EXECUTION_PLAN.md 已给出全链成功条件和未冻结开发配置，待用户对连续物理状态、网络/预算方案作决定；没有因等待而视为批准，也没有启动模型训练或付费算力。即使批准这两项，人物/感知及原生监督仍需先实际实现并通过数据来源门。应先产生合法证据和训练材料，再接模型、三个条件块、唯一账本及联合行动，最后运行同一源码的一次连续验收。

## 用户要求的两轮独立审核

第一轮由独立审核者在 `01d44d58a691d4144ffaaa3c23b9aa414115d977` 自行检查并构造完整 Git 归档反例，结论 CHANGES_REQUIRED：不同释放文件引用能重用同一内部捕获身份，静默覆盖输出并多报观察数（报告 4，实际 2）。原始报告、probe、JUnit 和完整 fixture Git bundle 保存在 evidence/independent_round1/。这不是仅转述作者检查。

修复已在 `fd516c33532a3fb61a978d0a0c0bd403a3f3794a` 绑定文件引用与捕获编号、显式拒绝重复观察身份、独占创建文件，并在完成回执中提供每个输出的身份/摘要/字节数清单、核对实际文件集合；同时兑现入口已声明的完整提交 SHA 要求，拒绝树对象。作者再次用第一轮原始完整 Git fixture 执行真实 CLI：预期退出 1、拒绝引用/身份错配、无完成回执，保留原始失败输出。第二轮绑定该修复之后的新源码，不能用第一轮的旧 SHA 充数。两轮局部独立审核均不替代 B 的跨机复现与全链验收。

## 新比较包的准确状态

`comparison_generate_01` 在 `01d44d5` 上退出 0，1440.14 秒，源码前后不变；60 个 episode / 1920 个 step，保留评分不匹配列表为空。原始包及执行绑定已归档（audit.json/timing.json 额外 gzip 压缩，steps.jsonl.gz 保留原格式）。原比较包的 base_commit 字段仍是历史实验基线 `09eb4d48e1c11082e90ca18332d04333e6b5b47a`；本次实际执行源码由 source_bindings、真实 CLI execution_source 和外层固定 `01d44d5` 记录绑定，不能把历史字段说成当前执行版本。

该包只完成当前源码生成，状态为 GENERATED_NOT_INDEPENDENTLY_RECOMPUTED。没有执行完整的比较重算 → attribution 生成/重算 → 全部消费者链，不签 W2 统一通过。原生三臂行动适配仍 MISSING_PRODUCTION_CAPABILITY，scientific_acceptance=false，SEARCH/PUT_BACK 科学信号均不支持。此次没有重新命名已打开的开发数据为独立确认集。

修复后的真实归档回放 `real_ingress_02` 在 `fd516c3` 完成：36 次捕获、72 个观测，清单 144 个文件逐项复核 SHA/字节数。完整文件可按命令从已推送的输入 SHA 重新生成；仓库保留回执/输出清单，避免重复上传相同像素。下游必须要求成功回执并验证清单，失败残留的 observations 目录不具备消费资格。

## 第二轮审核发现与修复

第二名审核者在 `fd516c33532a3fb61a978d0a0c0bd403a3f3794a` 独立验证第一轮身份修复后，发现 P1：普通 Git `refs/replace` 可使 CLI 用旧 SHA 读取另一提交/对象的内容，回执却仍标旧 SHA。旧审核结论 CHANGES_REQUIRED 和完整替换仓库/脚本保留在 evidence/independent_round2/。

在 `06366579a1a72a46b465194b6e1bc2b6c27a3e87`，所有归档对象类型检查与内容读取均显式使用 `git --no-replace-objects`。新增 commit 和 journal blob 两种自洽替换攻击回归，先证明普通Git确实读到替代内容，再证明CLI仍按原SHA读取原档案。当前32项入口/启动检查通过、源码前后不变；最后版真实归档回放36捕获/72观测/144文件清单逐项验证。第二轮审核者另以自己的原失败fixture和新的blob替换fixture复验，不依赖作者回归测试结论；复验记录与适用范围见其独立报告。

这些防护依赖受信Git对象存储、Python/标准库与进程环境，不宣称对已控制对象库、解释器或进程的攻击者提供外部摄像头认证。完整闭环尚未实现，用户要求的完整版本完成后全链独立验收仍须在届时最终SHA进行；本批两轮只检查当前新增实现及相邻边界。

第二轮最终复验结论：在 `0636657` 上，审核者自己的原commit替换攻击、独立blob替换攻击均仍得到原SHA的2捕获/4观测；身份碰撞和后到坏摘要拒绝且无完成回执；独立32项通过；独立真实归档36→72及144文件摘要/长度全部核对。P1关闭，本批有限范围未确认其他实质缺陷。详见 evidence/independent_round2/RETEST_0636657.md。未覆盖项仍按两轮报告保留，不能据此宣称“全项目无更多漏洞”。
