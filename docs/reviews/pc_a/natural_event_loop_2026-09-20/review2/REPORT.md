# Independent component review, round 2

Reviewed fixed source: `288b429a7d1e1d7f22f282ded333eedcf41957bc` in `/private/tmp/cpswm-pc-a-natural-event-loop-20260920`. Read-only review; reviewer did not modify the worktree. Scope: repair of the two round-1 checkpoint findings, fixed-prediction evaluator and six-frame pixel recovery tool. This is not acceptance of the five-task natural semantic/P5 loop or independent computer-B acceptance.

## Verdict

No blocking findings within this bounded component scope. Both first-round findings are resolved in this version: missing/mixed hand records reject before assignment, and failed restore preserves the entire previous producer checkpoint. Restored evidence is tied to raw prefix identity/time/payload/receipt, configured model, dimensions and derived association history. Candidate dataclass validation runs again after deserialization.

## Independent executions

- `.venv/bin/python -m pytest tests/test_natural_hands.py tests/test_natural_vision.py tests/test_continuous_state_recovery.py tests/test_structure_two_continuous_input.py -q`: all 78 collected cases passed, including configured fresh-process recovery. These are fixture/boundary tests, not 78 real semantic trials.
- `.venv/bin/python /private/tmp/cpswm-natural-review2/repro.py`: legal restore and identical-prefix no-op pass. Six independently constructed malformed cases reject without any change to the full prior checkpoint: missing hands, missing visual frames with replacement interactions, conflicting hand model/observation, a deserialization-style NaN handedness score, duplicate raw prefix, inconsistent derived interactions. This includes the original two failure patterns, plus corruption not dependent on dataclass construction.
- Independently executed `tools/evaluate_natural_hands.py` with raw pin `0b8d5fe14d6416f1afe6fcf93e95166291aab9ec234a83f2dc4aa69019632250`, annotation pin `461ea1c4c0a9cdc09d5a63044af5367b7d068fceb418e646b75e0ba66b4e36c3`, and fixed prediction pin `3b3183f6b21d3de85b6b352fa519f1b864cf668a526abdf42be8a1e576c6cd59`; output `/private/tmp/cpswm-natural-review2/evaluation`. Reproduced 360 frames, 600 visible author hand instances, 463 assigned predictions and 137 unmatched annotations. Per-sequence median assigned mean joint errors: 2.943865, 3.531830 and 3.613153 pixels. This is unthresholded geometric matching, not detection accuracy or calibrated role/contact probability.
- `.venv/bin/python /private/tmp/cpswm-natural-review2/evaluation_checks.py`: geometrically swapped hands are matched independently of predicted handedness; zero predictions retain the two-label denominator. Swapping two real prediction records and recomputing the prediction file pin still rejects on raw receipt pairing before creating output. The original datasets and prediction file were not modified.
- Inspected `tools/check_natural_hand_recovery.py`: raw RGB/depth bytes are validated by the adapter; first six same-camera contiguous frames are required; reference consumes all six; interrupted run consumes three, closes SQLite and model, reconstructs models/store/producer, resumes and consumes the remaining three. It checks exact visual/hand outputs and rejects semantic advancement. Ordinal times are synthetic replay clocks, not exposure measurements. Its explicit claim remains pixel-candidate persistence.
- Inspected the actual `output/hand-recovery-final/summary.json` and independently verified all its recorded three source-file hashes match this reviewed source. Artifact reports six hands, exact candidate equality, source unchanged, zero transitions and actions. Reviewer did not rerun the actual MediaPipe six-frame inference, and does not represent artifact inspection as an independent inference reproduction.

## Limits and residual assurance boundaries

The evaluator's supplied digest pins establish consistency with retained artifacts, not a trusted author signature or cryptographic proof that arbitrary supplied predictions came from the model. The evaluator does not establish model training provenance; `predictions_fitted_on_labels=false` is justified only for this retained fixed pretrained inference workflow, not a fact discoverable from an arbitrary JSON prediction file. Runtime model/source attribution still relies on the retained inference run and its source/weight evidence.

The 360-frame model outputs remain bound to their earlier inference source `c8fe8d5152c6713f889a19aee4f4756dfa1de49c`; evaluating them with this source does not upgrade the inference to `288b429`. The six-frame repaired recovery is a separate evidence item. Only three selected source files are hashed by the recovery script; this is not an attestation of every imported dependency or the entire interpreter.

Neither inspection nor tests establish pickup, release, interperson transfer, delayed semantic correction, calibrated pose/role likelihoods, reversible memory correctness, changed executed action or scientific advantage. Restoring consistent uncalibrated pixel candidates is the completed component boundary. The second component review does not turn this partial delivery into two overall natural-loop approvals or a computer-B review.
