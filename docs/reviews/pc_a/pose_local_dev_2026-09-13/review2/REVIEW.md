# 第二轮独立审核：C 位姿与本地开发配置

审核对象：`188f9fc9aa17bf38f973ee00f04e0b2f148f4ca3`，相对 base `e6bdd018d3fd49cb304293d25651506e5342344f`。源码目录 `/private/tmp/cpswm-pc-a-pose-local-dev-20260913`。审核者未修改候选 src/tests/configs/HEAD，也未使用作者测试成功作为自己的结论。

结论：本轮检查范围内未发现新的可复现阻断缺陷。第一轮提到的 DST fold 错误同刻融合及巨大四元数平方溢出，均由独立输入重新构造验证拒绝。此结论仅适用于局部几何/维数解耦/开发配置，不是完整结构二验收，也不是“无更多漏洞”。

## 实际独立检查

命令：`.venv/bin/python -m pytest /private/tmp/cpswm-pose-review2/test_independent.py -q -o addopts= --junitxml=/private/tmp/cpswm-pose-review2/junit.xml`

结果：116 passed in 0.51s。原始探针、stdout.txt 与 junit.xml 位于本目录。

- 100 个不同随机参考四元数和局部增量；用独立手写四元数旋转矩阵与 Rodrigues 公式（不调用被测 SciPy Rotation 作为期望生成器）检查组合次序、平移与旋转往返；同时参考 q/-q 一致。
- 同一 New York DST fold 两个不同瞬间拒绝；同一瞬间转换至 UTC+09 的合法正路径通过。
- NaN、Inf、正负 1e308、零范数四元数，在公共 PoseState 字典入口与 model_copy 伪造后调用入口均拒绝。
- 三条旋转轴的正负π观测与增量均拒绝，包含非坐标轴；对象、有效时间、坐标系绑定不匹配拒绝。
- 四组 (RLS维数, 高斯维数, 观测维数)：(1,6,3)、(4,6,2)、(7,2,5)、(2,1,1)，使用非对角正定测量协方差及矩形 H；独立显式逆公式比对信息矩阵、自然向量与 RLS 更新。
- 同一先验上的单/双测量重建、相反测量的自然向量抵消、完全撤回恢复先验，以及重复证据拒绝。
- 检查结束 HEAD 仍为目标 SHA；`git diff --exit-code HEAD -- src tests configs` 为 0。其他作者文档进行中修改不作为固定源码的一部分。

## 配置与范围核对

本次 diff 未修改既有正式架构或训练顺序协议。新配置保留三架构、H/R/I/C/Z/r/V、六操作、完整历史与字段；训练预算明确仅供开发，不允许正式排名。3×3 seed 是开发运行数，不是独立确认数据单元。CPU/时间/步数上限只是声明，model_trainer_available=false；不能把预算配置存在当作已执行控制或已训练证据。

位置与朝向采用 R3×SO3 的混合局部坐标，旋转增量右乘、平移位于命名世界坐标；源码明确不称作 SE3 对数。当前 C 的位置/朝向范围未被替换为位置-only，速度/加速度未实现也未谎称实现。原有全轴框架并未删除。

## 未覆盖和使用前提

- 此处 frame_id / object_instance_id 是一致性比较，不是世界坐标注册、真实检测身份认证或来源可信性证明。
- generic ConditionalMeasurement 不携带位姿参考图表的完整绑定；本轮未形成位姿到条件信息块的生产接线。未来接线必须绑定 reference_pose、epoch、frame、object、校准版本，拒绝跨参考坐标直接累计；当前只验证通用算术，不能推导真实位姿后验已安全融合。
- π处拒绝只证明此单参考函数不会任意选分支；完整系统必须保持未知/多峰或重选参考，并重新线性化历史证据，不能丢场景。未验收对称物体朝向、近π数值条件、协方差运输、动态预测或轨迹状态。
- 未验收真实 RGB-D 朝向估计、测量误差校准、六操作监督、三架构实际训练、跨窗口复核、长期账本/行动/反馈后果。本轮无这些运行入口，也不伪造对应测试通过。
- 本轮新增独立116项，不重新命名作者的62项或历史871项为独立全回归。
