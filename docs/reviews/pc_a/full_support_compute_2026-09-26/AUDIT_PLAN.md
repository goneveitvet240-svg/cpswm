# 第四轮预先冻结的双审范围

本轮只改变完整提议图的 CPU 推理计算与可审计执行配置。原始权重逐字保留；不选择三网络、不新增正式训练、不取得自然统计或发布资格。

数值口径：密集实现与查询行分块实现使用同一全部上下文/候选和参数；float32 输出 atol=rtol=1e-5。跨后端不承诺逐位相同；同后端恢复与下一 RNG 必须精确一致。每一查询分块读取全部键值；不能按候选组单独归一化。小规模与 303 候选/10,246 节点均对照；默认密集 4,096 限额不改，显式派生配置上限 32,768，超限整体拒绝。

第一审：小规模三网络数值、跨 128 行边界和远距离掩码、全局注意力软最大、完整概率质量、所有参数相同；完整派生元数据重封、权重/训练/执行来源更换、超限拒绝和合法恢复。补充必要故障后冻结固定代码执行回归。

第二审：独立复核跨大图所有概率、生成支持完全性；真实网络重新评分和采样的完整子集伪造、外部摘要托管边界、改变执行配置后的恢复、故障后的下一合法动作；固定四段视频各前 4 个 10 Hz 样本，保留失败和全部候选。输入只含裁剪真实像素，不读作者答案；不得把 bootstrap 候选当已验证接触/人物/位姿。

工程测试、两轮 A 自审、受控 303 路径、真实像素提议和自然闭环分别报告。独立审核者 0，B 验收未完成。新发布/账本写入/物理动作不因该工具获得授权。完整框架与 B 所有权不变。
