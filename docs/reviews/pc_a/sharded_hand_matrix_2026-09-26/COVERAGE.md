# 固定矩阵分片覆盖与边界

受测7549f1eaaf16a50bc5ab8be30877f504883b3a65；第一审296、第二审80，mypy368与Ruff通过。每轮均先冻结后顺序两审，运行中的Python不改动。本轮生产src与第四轮已审核版本相同，仅改变诊断执行分片和完成覆盖核对。

计划始终是原8试次、32个四帧短窗、128唯一原帧、三臂共96组合。每个试次首窗做恢复/下一次采样，共24组合；其余72组合只做完整评分、固定支持消融和请求/RNG不变。每臂分8片，每片4窗、1个首窗；最多8个独立进程，各Torch两线程。分片只由来源与窗口序号轮转确定；执行顺序按完整候选数平方及既定检查次数估算，较重者先启动，未使用作者结果或检测成功率筛选。

|检查|实际覆盖|限制|
|---|---|---|
|分片数学正路径|1/2/3/4/8片均无漏项、重复或输入顺序依赖；8片各4窗、1首窗|不替代真实网络执行|
|真实网络正路径|2试次×4窗口夹具，未分片与4片并集的全部概率、目标、状态字段完全相同，仅耗时不同；恢复分母保持2|这是两帧夹具，真实四帧矩阵另计|
|错误计划|重复窗口、越界片号、非法片数、bool伪装整数拒绝；非法分片不写成功目录|不证明任意外部来源是真实数据|
|完整伪造覆盖元数据|内部自洽伪造表可通过结构覆盖检查；漏项、重复、错臂、虚报恢复被拒绝|结构检查本身不是模型执行认证；仍要求真实进程回执、完整工件与源码绑定|
|完整伪造q与状态后果|继承并重跑前轮完整q伪造、缓存篡改、重封、方法替换、来源与恢复攻击；合法正路径和失败后重试保留|A自审不等于B或独立真值验收|
|完成状态|命令完成不直接写complete=true；必须逐片实际窗口一致、跨臂全96唯一组合一致后才标完整|时间截止时保留部分分母和所有失败，不拼接旧版本|

实际分片内部每个窗口使用原有独立推理会话和固定seed，分片不改变采样语义。四帧短窗不等于全试次持续记忆，不能据此声明长时域或完整自然行动闭环。可用前端统计在每片都会重复出现，汇总只按唯一window/source key计数，不相加重复的128帧或378手候选。

独立人工接触/释放/身份、前端6D位姿误差校准、自然联合训练、活跃粒子六操作内核及具身行动收益仍未闭合。完整H/R/I/C/Z/r/V、三个RB blocks和七算子范围、原模型选择及科学阈值保持。真实矩阵使用的交付权重新增优化步骤0；自审中的临时夹具优化仅作软件验证。自然发布、记忆和动作均不属于本轮产出。

## 最终实际覆盖

第五轮固定矩阵实际完成 **46/96** 个评分/消融组合和 **10/24** 个首窗恢复组合，9/24 分片完整完成。19/32 个窗口至少一种网络完成，13/32 窗口三网络均完成；已完成组合涉及 76/128 唯一原帧。

|网络|评分/消融完成|首窗恢复完成|完整候选累计呈现数|
|---|---:|---:|---:|
|类型化因子图 Transformer|13/32|2/8|3313|
|槽条件 Perceiver|17/32|4/8|4740|
|自回归类型图策略|16/32|4/8|4121|


9 分片退出 0，8 截止超时，7 未启动。909 Python 前后相同，所有完成工件已回读；未完成项保留在 [逐项清单](evidence/artifact-readback-final.json)。仅 A 本机两次不同设计的自审，B 未签收；不等于独立审核、完整矩阵验收或科学收益。原微型夹具交付权重在真实实验中新优化步骤为 0；自审中的临时夹具优化只作软件测试。自然语义发布、记忆写入及行动均为 0。
