# PC-A 联合消费者与条件解析更新：组件交付，非完整闭环验收

## 范围与源码绑定

- 分支：`codex/pc-a-native-joint-consumers-20260912`，生产基线 `62870a3a38fce882b25d8d77f1d0526cca6fbc14`。测试时 HEAD 是仅含开工登记的提交；本次未提交源差异逐文件绑定在 `run_01/manifest.json`，不是将基线 SHA 冒作被测代码。
- 新增 `structure_two_joint_consumption.py`、`structure_two_conditional_updates.py`，扩展 `active_verification.py`。不改 B 独占的 `prototype_spine.py`、`structure_two_particle_workspace.py`，不改科学配置、W1/W2 实现。
- 用户确认没有全轴已训练工件与连续测量/噪声模型。后续获取路线见 `INPUT_ACQUISITION.md`，没有生成占位权重或假传感数据。

## 已实现和已验证

1. 三解析块同步纯函数更新：Dirichlet alpha、RLS A/b、高斯信息 Lambda/xi；输入明确给定测量、设计矩阵、噪声和权重。独立标量公式、相关噪声二维公式、累计、撤回后重建、改正后重建验证；重复簇、非法维度、非有限数、非正定噪声与溢出拒绝。零信息权重保留零贡献，不为了非零而制造测量。此函数没有长期提交权限。
2. 完整粒子决策视图保留类型化状态、事件链、三个统计块和未解析质量；快照、粒子、统计引用、账本引用检查，脱离调用者嵌套可变容器。效用表必须覆盖全部粒子和未解析桶并绑定视图摘要。
3. CIAV 新增联合粒子输入。效用在完整联合原子上计算，原因信息增益仍由原因边缘计算，未擅自改成联合熵目标。保留旧原因后验路径。
4. 相关性反例：相同原因、角色、实例边缘，但不同角色-实例相关性；联合输入得到任务 EVSI 0.4 / 0.0，净值 0.3 / -0.1，分别执行/不执行。原因信息增益均为 0。证明消费者对联合相关性敏感，不证明生产者已学得正确相关性。
5. 现有真实后验加显式 prepared 接缝的视图/CIAV 集成正例，验证只读消费不写长期账本；这明确包含测试注入，不是用户所要求的默认生产闭环证据。

## 实际检查

运行本目录 `run_checks.py run_01`，解释器和完整参数见 manifest：

- pytest：**85 passed，74.18 秒**，包含新增 33 项和已有 CIAV、负观测、后验投影、交互与 break-even 回归。
- Ruff：通过。
- mypy：三个源文件通过，`--follow-imports=silent`，不是全仓库类型验收。
- 执行前后 src/tests/configs/artifacts 摘要相同；原始日志不覆盖，运行器拒绝复用输出目录。
- 早期无归档预检：27 项通过；两个新模块曾受既有导入循环影响，已在新模块使用延迟导入修正，新增两个独立进程导入回归通过。开发时 Ruff 超长行已修复。没有全仓库 812 项重跑，也没有 Windows 复现。

## 未完成及信任边界

- **默认完整联合主干与同一历史端到端仍未完成。** 未训练或接入全轴模型，未生成完整生产 log-q，未采集/标定连续物理观测，未把新消费者与解析更新接入 core 默认调度，未完成任务执行、反馈、合法长期贡献与迟到修订的同一连续联合历史验收。
- 新视图是值对象，不是源身份授权回执；摘要不是可信输入证明。`from_batch` 的局部引用检查不替代 B 负责的地点支持、跨代与证据链验证，直接构造数据对象也不授予生产权限。模型 ID 是声明，不是独立校准认证。后续必须由可信生产所有者绑定输出似然/效用表及当前运行状态，不能仅因 caller 填了匹配摘要就接受其科学真实性。
- 重建算术通过不证明长期账本撤回、并发事务、跨进程恢复或七算子全部连接通过。本批没有科学收益、环境公平性或完整攻击矩阵签收。
- 交接后需 B 绑定实际代码提交独立复核；不自动合并共享集成分支。
