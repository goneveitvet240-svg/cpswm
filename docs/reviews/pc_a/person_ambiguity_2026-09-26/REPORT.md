# 人物身份歧义进入角色候选：局部双审交付

三小时授权第一轮。分支 `codex/pc-a-person-ambiguity-20260926`；base PR46 `47a51cc88c1cd9bc1d661eaedf12e703fee48983`；最终受测源码 `4b576567d11258f18697ad74c680e91cadf77cde`。A 自审，B 未复核，不合并共享集成。

以往不同人物检测轨迹可直接产生多人角色。现在每个人物轨迹对同时保留 `same_person_multiple_detections` 与 `distinct_people`，不以框重叠阈值选择身份；角色明确引用该人物对并要求 distinct_people 才成立。真实多人重叠、局部/全身重复框、分离身体局部框均不被提前抹掉。这里是身份条件表示，尚未解决真实身份或计算联合人物划分。

最终首审185、第二审34（8项新攻击 + 26项来源/窗口回归）、mypy367源文件、Ruff均通过。8条执行命令和总执行器exit0；893个Python文件每命令前后与Git相同。第一次二审有1个审查脚本产物读取错误，修复后重新冻结两审，旧失败未隐藏。详见 FINDINGS、COVERAGE 和 COMMANDS。

原9.2GB HFD training归档重新构建全部337训练试次并经新进程复验；固定同8试次128唯一原帧/32短窗全部运行成功，与上一轮128帧的源键和像素SHA逐一相同。实际1,002检测、8,012完整候选、34个未决人物对；原11帧的64角色仍作为条件候选保留，0无条件角色。相关人物对 IoU 范围0至0.48693，说明仅按重叠阈值删除重复框也不能覆盖所有旧反例。该范围不作为新门限，也不是身份判定精度。

恢复后的视觉、交互、手物测量与在线相同；实际core、snapshot、ledger不变，execution traces为0。新增训练、自然语义发布、记忆写入和动作均为0。手物测量本轮仍未进入提议网络，下一轮专门接入并检验消费。模型仍有此前NORM_RECT投影警告，未将图像坐标升级为校准位姿。

完整内部一致的检测缓存伪造能够通过现有一致性恢复，但只能产生未决候选、infer输出None。该局限被完整正路径实测并保留，不把一致性校验当模型执行认证。34人物对是帧内成对假设，不能独立选择而破坏同一身份传递性。完整自然闭环、身份金标准和B统一验收仍未完成。

470文件、139,975,519字节已逐文件校验备份主仓 `output/person-ambiguity-20260926/closed/`。Git内保留命令、日志、源码前后哈希、身份汇总和备份清单；完整原帧与上下文在本地备份。原始输出 `output/person-ambiguity/final-02`，失败 `final-01` 均保留。

下一主项：来源绑定的手原始检测、由可见前缀重算的手物关系与身份未决条件进入三种未选型网络，固定候选集合做消融和恢复；不改变科学先验、训练方案、完整研究范围或权限。
