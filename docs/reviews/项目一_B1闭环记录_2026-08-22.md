# 项目一 B1 闭环记录（joint CF-BOCPD 第 11 臂 · 纯拓扑接入）

状态：`b1_topology_implemented`
日期：2026-08-22
范围：仅 B1（拓扑层）。**B2/B3 继续封锁。**

> 本记录只声明 v0.2 消融协议在**拓扑层**加入了 joint CF-BOCPD 第 11 臂，并保留普通 BOCPD 和旧独立原因 BOCPD 作为三臂控制。**不**声明公平实验、调参、预算、TEST 解封、指标或效力结论。

---

## 1. 交付内容（全部按审核规范实施）

**改动（版本冻结，行为等价）**
- `fair_ablation.py`：新增 enum `JOINT_CAUSE_FACTORIZED_BOCPD`；新增冻结常量 `PROJECT_ONE_ARMS_V01(_ORDERED)` / `PROJECT_ONE_ARMS_V02(_ORDERED)`；`ProjectOneFairAblationManifest` 精确要求 `== V01`；新增 `ProjectOneFairAblationManifestV2` 精确要求 `== V02`。
- `project_one_ablation_pilot.py`：两处 enum 推导（topology `:170`、report）改用 `PROJECT_ONE_ARMS_V01`；v0.1 Config/Manifest 的 `protocol_version` 冻结为 `Literal["…@0.1"]`（严格拒 @0.2）；`PilotAdapterStatus` 新增 `TOPOLOGY_ONLY`。

**新增**
- `project_one_ablation_v0_2.py`：`ProjectOneProtocolPilotConfigV2`（Literal @0.2 + `SealedSplitMetadata`）、`ProjectOneProtocolComparisonV2`（`_COMPARISON_ARMS_V02`，SHIFT 三臂有序）、`ProjectOneProtocolPilotManifestV2`（11 臂各一次）、`ProjectOnePilotArmTopologyResultV2`（`declared_model_version` / `executed_model_version=None`）、`ProjectOneProtocolPilotReportV2`（11 arm_results / `formal_experiment_ready=False` / `claim_scope=protocol_only` / `all_adapters_bound=False`）、`ProjectOneProtocolPilotRunnerV2`（纯拓扑）、`select_pilot_runner`（严格版本分派）。
- `sealed_test_split.py`：`SealedSplitMetadata`（仅 hash + count，**无** cases/loader/unseal）。
- `benchmarks/project_one_ablation/project_one_protocol_pilot_v0.2.json`（新 config，@0.2）。
- `apps/evaluation_runner/run_project_one_ablation_pilot_v0_2.py`（新 CLI，输出 `output/project_one_protocol_pilot_report_v0.2.json`）。
- 导出：`evaluation_operations/__init__.py` 追加全部 V2 符号。
- 测试：`tests/test_project_one_ablation_v0_2_topology.py`（23 项，正/负/零-TEST/版本/资产 hash）。

**不动**
- v0.1 CLI `run_project_one_ablation_pilot.py`、v0.1 output `…report.json`、v0.1 类的已存字段/默认值。

---

## 2. v0.1 权威资产（方案一，已跟踪）

已 `git add`（跟踪）并 pin raw SHA-256（测试守护）：
- `project_one_protocol_pilot_v0.1.json` → `df0f4d19f172da6df790164ac23e7ccddd88c2643dda102e928459570a7fd307`
- `project_one_protocol_pilot_report_v0.1.fixture.json` → `93c3f5975340a581d74b5541202dfcf706aeb93bc2c0e50e6df8dc322924c648`
- `project_one_protocol_pilot_v0.2.json`（新增，已跟踪）

（清除了一枚 Aug 19 的**陈旧** `.git/index.lock`——当时无 git 进程运行。）

---

## 3. 三轮对抗核验结果（全部 PASS）

**Round 1 · v0.1 兼容与冻结**
- v0.1 config raw hash 不变；v0.1 report 逐字节重生一致；v0.1 output 未动；v0.1 类拒 @0.2；v0.1 pilot 回归测试通过。

**Round 2 · v0.2 拓扑与零-TEST**
- **静态**：`project_one_ablation_v0_2.py` 全文无 `OnlineShiftSuiteGenerator` / `.unseal(` / `.predict(` / `OnlineShiftEvaluator` / `SealedTestSplit` / `.cases` / `OnlineShiftSplit.TEST` 引用。
- **行为陷阱**：把 suite 生成器 + 全部模型 `predict` 打成爆炸，RunnerV2 仍产出 11 个 topology-only 臂（证明零模型运行 / 零 TEST 读取）。
- 负向电池：缺 joint / 重复 / 第 12 臂 / 替换 legacy / SHIFT 二臂 / 顺序错 / joint 入他组，全部被拒。

**Round 3 · 分派 / 输出隔离 / 幂等 / B2-B3 锁**
- v0.1↔v0.2 config 类互拒；`select_pilot_runner` 严格分派；RunnerV2 跨重启**逐字节确定**；v0.2 输出路径独立于 v0.1；v0.2 CLL 拒 v0.1 config；`SealedTestSplit` 无 receipt 仍封存（B3 未解封）。

全套 `521 passed, 1 xfailed`；本复审者触及文件 ruff 全绿。

---

## 4. B1 验收门槛对照（§十二）

原 10 臂 ID/值不变 ✅；第 11 臂唯一新 ID ✅；v0.1 精确 10 臂 ✅；v0.2 精确 11 臂 ✅；legacy 未被 joint 覆盖 ✅；SHIFT v0.2 精确三臂 ✅；v0.1 config/report/fixture hash 不变 ✅；V1/V2 不可交叉运行 ✅；RunnerV2 不生成/加载/遍历 TEST ✅；RunnerV2 不运行任何模型 ✅；11 臂全 topology-only ✅；无调参 ✅；无实测预算 ✅；无正式指标 ✅；无效力结论 ✅；`formal_experiment_ready=False` ✅；`claim_scope=protocol_only` ✅；v0.1 output 未覆盖 ✅；v0.2 独立 config/CLI/output ✅；B2/B3 仍封锁 ✅。

---

## 5. B1 后可/不可宣称（§十三）

**只能宣称**：项目一 v0.2 消融协议已在拓扑层加入 joint CF-BOCPD 第 11 臂，并保留普通 BOCPD 与旧独立原因 BOCPD 作为三臂控制。

**不能宣称**：joint 已完成公平实验 / 优于 independent / 优于 ordinary / 11 臂已调参 / 预算已公平 / TEST 已解封 / 指标已产生 / CF-BOCPD 已成为项目主要贡献。

---

## 6. B2 / B3 仍封锁

- B2（每臂独立调参 + 实测预算账本 + fail-closed）、B3（凭合规 receipt 解封 test split + 全指标 + household/trajectory 级 paired CI）**均未开始**。
- 已知前提：现有 `TuningCompletionReceipt` 为占位，需升级为 tuning orchestrator 依真实运行签发、绑定十余项 hash 的 committed/签名 receipt；解封需满足 §（B3 全部条件）；跨重启幂等与原子审计需先解决。
