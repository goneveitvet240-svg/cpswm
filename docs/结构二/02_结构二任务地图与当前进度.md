# 结构二任务地图与当前进度

更新时间：2026-09-10
证据边界：`81e5015` 及其后的 Route C v0.1/v0.2 历史本地开发工件；2026-09-10 终审已经重新收紧
Architecture-A adaptive readiness。当前机器工件已在最终源码/配置/测试快照上重建并鲜验证；当前
source manifest SHA 为 `ebf05dad4785182c139a552658344acd9a3b812e3437f32aabfa5dcbe84ac9ff`。
这不是新提交号，也不把较早 source binding 写成当前 checkpoint。

## 1. 为什么编号从 Task 7 开始

当前仓库没有一套仍有效的正式 `Task 1--6` 协议编号。早期工作使用 M01--M32、WP2-A--E、路线和
实验名称组织；Task 7--13 是进入骨干科学门后才形成的正式任务编号。为了避免伪造不存在的历史，
本文件把早期工作称为“基础工作包”，不倒填 Task 1--6。

## 2. 基础工作包

| 工作包 | 内容 | 当前状态 |
|---|---|---|
| F-A 问题与数据合同 | 选择性观察、隐藏事件、多人、未知和习惯变化的统一定义 | 已定义并有契约测试 |
| F-B 七算子纵切 | OPCEU、ORRER/CHEH、PCHMP、CF-BOCPD、CCRR、RGRC、CIAV | 均有实现接口；联合收益未成立 |
| F-C 统一推断主干 | 神经提议、类型粒子、解析块、显式未决质量、可逆账本 | 方法已冻结；Architecture A 已有 transitively read-only plan/consumption registry、部分 path kernels、P0 debt、异位置检测完整 CIAV closure 与同位置快速验证，但不是完整 P0–P5 executor 或 consequential identity；negative-observation full closure 尚缺，旧 Route-C 轨迹仍不追溯绑定 |
| F-D D0 合成评价 | 多因素世界、强基线、行动读出、污染和恢复 | 已运行多轮；旧动作构念混用，新 typed gate 仅为通过的 3-step 开发诊断 |
| F-E 证据治理 | 来源、回执、重放、篡改检测和 fail-closed 授权 | 两个 adaptive 公开入口的 caller context/selection snapshot 与 pending-debt core-transition barrier 已修；已有局部债务、异位置检测完整 CIAV 闭包、同位置快速验证及已执行算子耗时回执，但屏障不覆盖全部 wrapper rebinding、replay 不绑定 origin policy/trace，完整路径身份、fallback、资源核算、独立信任锚与跨系统持久原子性仍缺失 |

## 3. 正式任务状态

| 任务 | 回答的问题 | 已推进 | 当前结论 | 下一步 |
|---|---|---|---|---|
| Task 7 | 局部迟到纠正能否接近完整重跑且更便宜 | 五臂历史支持恢复已运行；pre-correction reservoir + RNG checkpoint 已修复 | `D0 PARTIAL PASS`：reservoir-only 与 full rerun 精确等价且更便宜；backward-only/combined 失败；这不改变用户选定路线 | 按用户冻结选择继续 combined reservoir + backward-message，并保留 reservoir-only、backward-only、combined、full-rerun 和旧失败实现五臂；后向消息需多后代或解析消息后再测 |
| Task 8 | 联合模型在同信息同计算下是否值得 | Route C 完整七轴、三个 RB blocks 和七算子范围保留；typed SEARCH/PUT_BACK 3-step 构念门通过；项目所有者冻结 `A1 + B1` 后，matched direct `P5_FULL_EAGER` 已在 60 test episodes / 1,920 steps 上执行，其中 1,243 次真实七算子正路径、677 次真实负观测闭环；PCHMP→CIAV sequential actor-prior handoff 已修复并通过定向回归 | `P5 ACTION SIGNAL NOT DETECTED; TWO POST-HOC WIRING CONFOUNDS FOUND`：SEARCH 三臂 error 均为 0.0891；P5 PUT_BACK error 0.6849，差于 learned two-stage 0.2740 与 AMG 0.0536。v0.1 未接入 v0.6 readout；readout-only preflight 仍均匀，进一步发现 CIAV 把 primary PCHMP owner mass 示例从 0.7614 重置到 0.3333。修复后单 episode posterior 已非均匀，但完整 post-hoc 尚未运行；所以 v0.1 必须保留为失败，仍不能判定七算子内部统计无行动信息。历史 Route-C runtime identity、combined utility、长期污染和 Task 8 正式通过仍为 false | 冻结两个修复及其事后披露，不覆盖已打开 test；运行完整 post-hoc 三臂诊断，再用 production debt replay 确认相同 sequential prior/readout 消费。若需新的死亡测试，必须使用未见过的新 holdout；router calibration 仍不启动，固定解析阈值继续只作基线 |
| Task 9 | 七算子关键耦合是否产生联合贡献 | 四组 `00/10/01/11`、DiD、cluster bootstrap、Holm 协议已定义 | `DEFINED_NOT_RUN` | 独立托管正式运行 |
| Task 10 | 粒子预算如何选 | D0 多预算曲线可复算、正向字段有绑定 | 工程复算完成，`formal_task_10_passed=false` | 外部托管解析预算并补机制归因 |
| Task 11 | 选择哪种重采样策略 | 本地全预算运行 130 条件、11,856 traces，并完成 fresh replay | `LOCAL DIAGNOSTIC ONLY`：未选择 policy，正式绑定未解析 | 等 Task 10 独立预算回执后才能正式选择 |
| Task 12 | 窗口内采用哪种回春核 | 本地运行 104 个 condition-kernel 臂、7,488 traces | `LOCAL DIAGNOSTIC ONLY`：0 个正式可选 kernel，`selected_kernel=null` | 先补齐 Task 10/11 正式父回执；不能拿 Task 7 代替 |
| Task 13 | 神经提议器如何求梯度 | 三种策略已完成等计算本地诊断 | `COMPLETED_LOCAL_DIAGNOSTIC_FORMAL_BLOCKED`：`selected_strategy=null` | 上游 Task 10--12 与 sealed holdout 完成后再正式选择 |
| P5 | 真值兼容候选在哪个阶段丢失 | 四臂、五阶段 recall 本地诊断已运行 | `MIXED_PROPOSAL_AND_DOWNSTREAM_BOTTLENECK`，只诊断不产生 pass | 保留分阶段瓶颈结论，等待正式上游回执和密封负载 |

## 4. Gate 与授权状态

| 门 | 当前状态 | 大白话 |
|---|---|---|
| Gate B v0.8 | `FROZEN_NOT_EXECUTED` | 比较器语义已经写清，但正式比较链还没运行 |
| Task 7/8 | Task 7 为 D0 partial pass；Task 8 v0.4 FAIL；旧 Route C v0.2 verifier 只在其冻结合同内通过 | Task 7 的局部信号不能替 Task 8 开门；旧 v0.2 没有有效任务分离动作或实际状态改变 |
| Action/utility construct gate v0.1 | `status=D0_DIAGNOSTIC_ONLY`；`action_utility_construct_gate_passed=true` | 3-step 中 typed SEARCH/PUT_BACK、独立指标、1 次成功 nonnoop 转移及两个单头敏感性对照通过；`evidence_status=D0_CONSTRUCT_VALIDITY_DIAGNOSTIC_NOT_PAPER_EVIDENCE`，不授权组合效用、长期动作语义、长期污染或 Task 8 |
| Runtime identity gate v0.1 | `false`；`MISSING_LIVE_RUNTIME_OPERATOR_CALL_CONSUMPTION_BINDING` | 只针对旧 Route-C：生产系统是运行后 sidecar；A 的普通转移已绑定，但不追溯提升旧轨迹 |
| Adaptive-compute pre-death v0.1 | `PROTOCOL_IMPLEMENTED_EXECUTION_BLOCKED`；四项完整工程门均为 `false` | 已有深层只读 plan/consumption registry、partial kernels、P0 debt、异位置检测完整 CIAV closure、同位置快速验证、executed-operator elapsed receipts；negative-observation full closure、全 wrapper debt barrier、origin-policy/trace-bound replay、P1/P2/P4 独立实质内核、state-derived feature extractor 和真实 fallback 尚缺，故 full executor、state-derived eligibility、P0–P5 consequential identity、full resource accounting 均未成立；当前共 12 blockers |
| Task 9--13/P5 | 定义或局部工程完成，正式回执缺失 | 本地结果不能自行给自己授权 |
| 七算子消融授权 | `false` | 依赖链没有全部通过，不能宣称每个算子贡献成立 |
| 外部强基线 | 6 个 component cores、0 个 native reproduction | 接口骨架不是忠实复现 |
| independent custody | 未完成 | 换机器已经决定，但账户、密钥、种子和结果回传制度尚待落实 |

不同历史协议也使用过 Gate A/B 名称。任何对外陈述必须写完整协议号，不能只说“Gate A 通过”。

Adaptive readiness 的 12 个 blocker 是：state-derived path eligibility、full legal-path executor、
P0–P5 consequential runtime identity、full resource accounting、cross-system trace/state atomicity、
path-specific operator budgets、frozen branch-point manifest、debt replay/eager-reference equivalence、
empirical power/ICC、confirmatory custody、exogenous production RNG 和 long-horizon action consequences。
historical Route-C identity failure 是保留的历史诊断，不额外阻断 Architecture A，也不因 A 的局部修复
而被追溯提升。

## 5. 历史 v0.6 action-readout 开发结果（2026-09-06）

在 60 个新的 development holdout episodes 上：

| 方法 | 累计行动遗憾 | 放回 | 搜索 | 主人污染 | 恢复成本 |
|---|---:|---:|---:|---:|---:|
| Project Two v0.6 | 3.455556 | 1.716667 | 1.738889 | 0.007292 | 0 |
| matched AMG | 3.933333 | 1.716667 | 2.216667 | 0 | 0 |

配对优势 `AMG - Project Two = +0.477778`，95% bootstrap interval 为
`[+0.338889,+0.633333]`。这说明新的行动读出在 D0 开发环境出现了搜索收益，但污染硬护栏仍未
通过，而且数据不由独立方保管，因此 `paper_claim_allowed=false`。

## 6. 外部验证状态

| 轨道 | 决定 | 状态 |
|---|---|---|
| D1 | ProcTHOR + AI2-THOR，多人物事件调度和长期日程 | runtime preflight 已通过，尚未采集 replay |
| D2 | 1 个方法 RGB-D 视角 + 2 个 evaluator-only 真值视角 | 已冻结，未采集 |
| LLM/VLM | 本地 Qwen3-VL-8B 第一候选；无 VLM 为主基线 | 已冻结，未接入正式实验 |
| 外部基线 | 先忠实复现最强 2--3 个 | 候选顺序尚需根据原生可复现性落实 |
| 独立托管 | 第二台、不同流程控制的机器 | 方向已定，信任锚和运行制度未建立 |

## 7. 依赖顺序

```text
最终效用/护栏协议 + checkpoint 81e5015
        ↓
Task 8 learned equal-compute v0.1（已运行：无信号）
        ↓
Route C full-state stateful joint v0.1（冻结回放：后验仅微量影响、RGRC 未写入、动作与效用无增益）
        ↓
Route C v0.2 历史 evaluator-local 工件
        ↓
typed SEARCH/PUT_BACK construct gate（D0 诊断通过）
        +
historical Route-C runtime identity gate（false；架构 A 已选择，旧轨迹未重绑定）
        ↓
Architecture A 已有深层只读 plan/consumption registry、partial kernels、P0 debt、异位置检测完整 CIAV
closure、同位置快速验证与已执行算子耗时回执
        ↓
项目所有者选择 P5_FIRST：先在 typed SEARCH/PUT_BACK 与真实 nonnoop transition 下，
以完整七算子 P5 full-eager 重跑 decision-sensitive readout、learned matched two-stage 与独立调参 AMG
        ↓
evaluation-only direct P5 engineering bring-up 已通过：正常 router 对同一低风险特征仍选 P0，
direct receipt 单独授权真实 P5；fresh trace 为 7 个主路径调用 + 6 个 CIAV 反馈闭环调用；
共享 SEARCH/PUT_BACK decoder 已抽出。该结果尚未建立跨臂 CIAV 匹配或行动收益
        ↓ readiness 与方法定义已完成
项目所有者冻结 A1 外生预承诺 schedule + B1 共享条件 location head；negative-observation closure 已补齐
        ↓ direct P5 v0.1 已运行：无信号，且发现两个 wiring confounds
SEARCH 三臂打平；P5 PUT_BACK 显著更差；readout 漏接 + PCHMP posterior 被 CIAV 原始 prior 重置
        ↓ 当前工作点
v0.1 已冻结；sequential actor-prior handoff 已修复；冻结修复后运行完整 post-hoc readout 诊断
        ↓
用 production debt replay 对同一读出和状态消费做生产路径确认
        ↓ 只有新的未见 holdout 再现可重复行动信号后
validation-only calibration + 固定解析阈值基线；
再冻结 feature formulas、P1/P2/P4 实质内核、negative-observation closure、debt rebinding 与 fallback
        ├────→ Task 7 用户选定 combined 五臂 + 全系统 guardrails
        ├────→ Task 10 → Task 11 → Task 12 → Task 13 → P5
        └────→ Task 9 四耦合 + ProcTHOR replay
                              ↓
                    七算子正式消融
                              ↓
                    D2 确认 + 独立托管 sealed run
```

并非所有工作必须串行：D2 timing pilot 的设备/同步/记录格式准备可以并行；ProcTHOR 与外部基线可做
低成本适配准备，但在 Task 8 新机制出现信号前不扩大采集和算力投入。正式确认集只能在指标、模型、
prompt、阈值和数据划分全部冻结后开启。

当前 P5_FIRST 顺序与校准族选择记录在
`configs/project_two_experiments/structure_two_p5_first_decision_v0_1.json`；首轮 SEARCH/PUT_BACK 是分阶段
死亡测试，不删除 HANDOFF、DELIVERY、REMIND 或 ASSIST。

## 8. 工程回归与历史边界

2026-09-08 的旧工程回归、manifest 与 checkpoint 只属于当时冻结合同的历史快照；其具体哈希和测试
计数保留在对应 dated artifacts（日期工件）中，不在当前进度页复写。那些历史通过不建立 trajectory
runtime identity，也不改变 Task 7/8 的科学门结论或把七算子装配升级为七算子效用贡献证明。

2026-09-10 终审后的当前工程快照：

- 7-scope P0 manifest（含独立 `test_fixture_contract`）：manifest SHA
  `ebf05dad4785182c139a552658344acd9a3b812e3437f32aabfa5dcbe84ac9ff`；
- current world source bundle：301 files，SHA
  `8a48b902c070ea7d1cb1f49ca880f6cb40e40ce282451e7cf0fbbb3686f2c7d7`；与冻结 v0.5 明确不兼容；
- adaptive readiness content SHA：
  `f1356ad5aa2ac427c4d9168acd79dc43084e8246be264eb423f9521ff077a54b`；状态仍为
  `PROTOCOL_IMPLEMENTED_EXECUTION_BLOCKED`，12 blockers，四项完整工程门均为 false；
- engineering audit receipt content SHA：
  `1dc800c0a2271beda1cd0e6d69bf12a5dd48937ce8aca1b5c47ac72137b3d0f7`；8/8 命令通过；
- engineering checkpoint content SHA：
  `c0da629d5f6da45890409afee3808cd8b7ca11370e784ef8fedc42192f7408d6`；generation 与 no-fresh verify
  均通过，生成过程本身完成 fresh task recomputation；
- 全仓 core audit：3,789 passed、1 skipped、1 xfailed；P0 adversarial matrix：118 passed；结构二定向
  合并回归：229 passed；checkpoint 独立回归：13 passed；mypy、Ruff lint/format、compileall、
  frozen/offline dependency check 与 `git diff --check` 全部通过。

这里的 `engineering_trust_gate_passed=true` 只表示 current-local recorded audit（当前本地记录审计）
内部一致；`recorded_execution_authenticity_established=false`、`hermetic_toolchain_established=false`、
`independent_custody_established=false`，也不建立外部有效性或科学优越性。
