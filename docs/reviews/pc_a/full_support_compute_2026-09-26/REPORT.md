# 完整候选图计算与局部双审

本轮解决此前 303 候选/10,246 节点超过默认 4,096 上限而整体拒绝的问题。新增显式 CPU float32 eval 后端：独立叶序列分批编码，注意力只沿查询行分块，每行读取全部原始键值；完整候选树的九因子归一化保持统一。资源上限仍存在，显式派生可至 32,768 节点，超限整体拒绝。原默认密集配置不改。

## 身份与范围

- 分支 `codex/pc-a-full-support-compute-20260926`；base PR40 `aeb0275ae2e83a9b20e80e7b2e1437d8ef43a4e0`。当前受测源码 `c25fa44d51a42d87b906c844b03b67c39900ff2a`。
- 三种既有神经网络都保留，不选择架构。派生检查点逐字复制原始 weights.pt，内嵌原始 manifest 字节、其摘要、原训练报告，并记录执行源码与 PyTorch 版本；新增优化器步骤 0。
- 原始训练为此前的 COMPONENT_FIXTURE_ONLY 开发夹具，派生不会升级其证据等级。未新增自然训练、测量校准、原生发布或账本资格。
- H/R/I/C/Z/r/V、三 RB blocks、七算子、多人、隐藏事件、开放世界、可逆归因、具身反馈及 C 的位置＋朝向范围不变。

## 验证口径

数值容差事先固定为 float32 atol=rtol=1e-5，未因失败调宽。对照使用相同参数、全部上下文和全部支持；密集参考仅在进程中显式提升资源限额，不导出成新的训练检查点。跨后端不保证逐位相同；同后端恢复、幂等请求、失败后的后续随机数必须精确一致。

两轮均为 A 自审，独立审核者 0，B 未验收、未合并集成。第一审与第二审采用不同攻击设计，覆盖正常运行后的完整结构/缓存替换、完整子集重新评分与重封、合法恢复、执行配置/来源绑定及错误导出。修前失败与后续修复完整保留，见 [发现记录](FINDINGS.md)。

## 最终结果

已完成受控部分：第一审及关联回归 99 passed / 1 个预期畸形夹具警告；第二审 26 passed（其中 18 个谱系/完整子集测试与前轮重叠）；mypy 365 文件与 Ruff 通过。三个原始实际检查点各处理 303 候选/10,246 节点，六操作计数为 branch 84、revise 84、retract 2、reactivate 1、rejuvenate 126、preserve_unresolved 6。

| 网络 | memory 最大绝对差 | 九因子 log q 最大绝对差 | 受控全流程秒数 |
|---|---:|---:|---:|
| typed factor graph Transformer | 7.16e-7 | 1.70e-7 | 65.74 |
| slot-conditioned Perceiver | 4.77e-7 | 1.10e-7 | 56.20 |
| autoregressive typed graph policy | 1.20e-6 | 1.68e-7 | 65.37 |

三者均完整质量归一化、同后端恢复精确相同、失败后下一随机数与合法续跑一致。原始默认密集预算仍整体拒绝 303 案例且历史不变。运行进程的 peak RSS 为包含先跑密集参考的累计值，不能用于宣称分块后端实测峰值更低。完整数据见 [受控结果](CONTROLLED_RESULTS.json)。

四段预定视频均已执行，完整视频矩阵未通过。0001、0002、0004 的三臂全部完成，候选分别 108→324、117→378、189→594，在线/恢复完全一致且核心/动作不变。0003 为 216→783 候选，第一臂迟到阶段超过 32,768 节点预算并整体拒绝（exit 1），后两臂未完成；该失败案例没有产出完整恢复或失败原子性结论。总计 3/4 片段、9/12 片段×网络组合完整完成，不能剔除第三段后声称全通过。见 [固定矩阵](evidence/FINAL_MATRIX.json) 和 [原始失败](evidence/final-03-video-0003.log)。

最终串行任务于 UTC 01:40:37 前后结束；868 个受版本控制 Python 文件的运行前后摘要一致。整套执行器 exit 0 只表示执行完预定列表，视频子命令的 exit 1 仍是失败。第五轮曾在受控双审与首段视频完成后、剩余扩展矩阵未完成时开工；因此不得称每一整轮全部闭合后才推进，后续应先解决本资源缺口再恢复扩展推进。

## 边界

该后端是完整有限支持的计算实现，不是可无限扩展的连续空间提议器。候选枚举仍可能组合爆炸，4,096 候选及节点/叶字节限额不会通过截断绕过。恢复和派生摘要约束依赖调用者正确托管预期根摘要；若攻击者连托管根也替换，内部自洽不等于外部来源认证。

缓存张量字节、候选—因子对应关系和模块图绑定拒绝已覆盖的运行时漂移，未声称对恶意 Python 解释器或全部外部库全局状态提供安全隔离。实际训练权重、模型校准、自然六操作活跃后验核、重采样/再生和自然动作收益仍需分别闭合。

真实视频试验固定使用四段官方视频各前 4 个 10 Hz 样本，裁剪底部作者标签；未按事件成功挑片段，未读取作者答案。候选仍是未知身份/未校准检测和有限事件语法展开，不能当成人工接触、释放或位姿误差证据。

数值容差验证仅覆盖本报告列出的输入和三个既有检查点，未证明任意权重或任意 32,768 节点图均满足同样误差界。旧 `6894059` 的受控数值通过未被作废，但其缓存关联完整性结论已被补充反证推翻；最终全部结论须绑定 `c25fa44`。
