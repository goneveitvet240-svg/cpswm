# 后续手物提议接入设计（轮一运行期间仅设计，尚未实现）

目标是让已经观测到的区域手候选、手物几何及人物对身份未决条件真实进入三种现有提议网络。保留原模型、原权重、九因子解码、完整离散语法与所有科研路线；不以作者阶段填补接触/身份标签，不引入新的概率先验或科学阈值。

- 提议像素观测可选携带手检测原始结果。严格绑定 observation、capture、像素、receipt、尺寸和已存在 ROI 来源；无手模块与手模块已运行但未检出分开表示。区域候选可能来自同一只手，不能按候选数量宣称手数量或多人。
- 人物轨迹关联、人物身份对和手物关系由同一可见前缀重算，不接收调用者填写的接触/身份已确认字段；迟到观测重建关联，不泄漏未来观测。
- 模型只看到几何、未决状态、局部关联标识和可见因果次序；不看到作者答案、来源文件名/试次名称、外部哈希或下载元数据。来源验证和模型特征分离。
- 三网络消费检查需要冻结同一候选集合做手观测消融，避免候选 ID 随输入重生造成假性敏感度。随机/微型夹具训练权重仅验证代码消费，不证明正确接触或任务收益。
- 固定真实范围：完整128帧/32窗都建立含手特征的上下文；先用每个预定8试次的首窗前2帧验证三臂消费和恢复（24组合），保留空手分母。若算力时间允许，再做首窗4帧全链支持，不按检测成功选择窗口。不改变现有资源上限或截断支持。
- 每轮保持两次源码绑定自审、失败后重审；完整伪造有效路径、实际恢复/请求状态/RNG和系统账本/动作后果均要记录。模型响应变化与自然校准/闭环收益分开。

实施必须等轮一双审及固定真实复验完成，使用新A工作目录。截止UTC 2026-09-26 16:47:47 后只交付，不继续跑新实验。
