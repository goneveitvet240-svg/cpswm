# 实际反例与修复

65aa8b3上，两轮既定攻击112/33和三个实际权重的303候选数值对照通过后，追加合法评分→替换自有注意力函数的已加载__code__→完整重新评分攻击。原参数绑定完全不变，伪分布总质量仍1，但log因子最大改变0.0011523322527917657。原始development/loaded-helper-probe.json保存这一实际输出。

正式补充三个攻击（已加载函数、实际导入别名、资源常量）结果3 failed/7 deselected，原始development/owned-helper-before.log保留。不是只改文件名或缺字段的负例。

停止旧final-02父52086及子52655，保存SIGTERM回执后修改源。7c19637把自有计算函数实际code、导入别名、自有网络/评分类可调用方法和资源常量一起绑定到模型实例；每次评分前后仍检查身份。恢复正确函数后合法评分应精确恢复，完整回归顺序重做。

边界：防止受托管模型构建后的已覆盖实现漂移；不是对可替换整个Python解释器、检查器和外部托管根的攻击者提供认证。保持原权重、所有候选、原默认配置与1e-5数值门。整个真实视频矩阵结束前不推进下一轮实现。
