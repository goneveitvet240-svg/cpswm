# 本轮过程记录（尚未最终交付）

基线为 PR39 `1c1281b9d8ed5d535a335149ebfe622150d6bf0f`。开工 `cf4eabd` 已推远端；初版实际实现 `5564370`，后果追踪/测试入口修正 `1aad655e73aa2da54fe557cf31f8ad270e1a469c`。后者生产代码与初版相同，新增独立运行工件与旧负例诊断适配。

开发日志全部保留在 `output/joint-full-replay-20260926/development/`：

- first：合法源重放失败，暴露只靠已消费联合批次不足以覆盖所有七算子产出的有效观测；增加实际源发布时的核心接受绑定。
- second：旧工作区对象不能直接通用 JSON 哈希；改为完整字段字典参与代际归档哈希，类型与原始接受绑定另校验。
- third：首个合法完整重算正例通过。
- four：开发过程中源码被更新，运行中源码绑定明确拒绝；本轮输出不可当固定源码验收。
- five：65 passed / 7 failed。5 个旧负例在捕获故意破坏状态的公开诊断快照时提前被新增源绑定拒绝；1 个环境缺 torch；1 个新增测试读错状态所属对象。旧负例改为捕获原始破坏状态且仍明确断言公开诊断拒绝，没有弱化保护。后续定向 5 passed。
- first formal attempt (`round1`)：专项 5 passed，但回归收集缺 OpenCV、全量 mypy 缺 cv2/mediapipe，整组不通过。补到与前轮一致的锁定环境：`uv sync --frozen --extra dev --extra perception --extra hand-perception`，88 包。
- `round1-retest`：提交钩子因工具一行过长而拒绝后，误启动了尚未冻结的运行；已中断（130），不计审查通过。已补审查入口的源目录干净检查，避免再发生。
- `round1-complete`：固定 `1aad655...`，646 工程回归通过（11 个既有故意坏类型序列化警告），771.90 秒；其余命令仍以最终 manifest 为准。

两轮均 A 本机自审，独立审核者 0。自然监督/校准、正式神经架构与修订内核选择、B 独立复核没有由这些工程结果替代。重算保持七变量/三解析块/七算子的完整研究范围，当前可执行范围是显式 prepared 模型的有效语义源历史；新生纠正来源缺完整推理记录时仍拒绝，不能称全部六操作已经闭合。

第一轮补验：`round1-complete` 的 646 回归、5 专项、受控连续工具、Ruff 均通过；mypy 使用了安装新可选依赖前的缺包缓存，仍报 mediapipe 缺失。模块实际可发现；同源码 `mypy --no-incremental src` 重新检查 364 文件通过。原失败 manifest 保留，补验记录 `round1-recheck.json` 不改写原结果。连续工具实际重算 23 个仍有效源、形成 46 个新记录和 2 个活跃原子；旧 24 记录归档，账本无新增，六维位姿和 23 步历史保持，恢复后受控相机命令 1 次。原源中 11 个有效观测此前没有被联合批次消费，此次补进完整重算，不能把前后统计差全部归因于撤回一个来源。
