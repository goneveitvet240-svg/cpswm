# 后果覆盖与接受边界

本表是7c19637的覆盖索引。final-04全部14条命令及完整12组合通过，877个Python文件前后相同；实际回执见evidence/MATRIX_RESULTS.json。检查脚本中常量True不能替代先行断言。

| 有后果的输出 | 合法依据与实际重算 | 对抗路径 | 边界 |
|---|---|---|---|
| 派生检查点可加载 | 原manifest内嵌字节、原权重SHA、实际执行源码/PyTorch和限定配置一致 | 完整重封修改权重、训练来源、优化步数、资源上限或发布资格 | 外部预期根须由调用者托管；不是独立认证 |
| 模型可继续评分 | 参数/缓冲区实际字节、模块结构、自有已加载实现及常量一致 | 完整网络后替换自有函数code、导入别名、层实现、缓存张量和因子对应关系 | 不覆盖恶意替换整个解释器/检查器 |
| 完整分布质量1 | 全候选集统一九因子树；303密集/分块逐目标log概率对照 | 质量1但已篡改的概率、完全自洽子集重评分与重封、旧限额外远端键删除 | 质量1单独不能证明概率正确或校准 |
| 资源拒绝后可恢复 | 先有合法历史，再触发预算或实现绑定失败，对比完整快照及下一次采样 | 非空历史后的失败、同request重试、跨执行配置快照重用 | 实际旧783输入另做三臂回滚检查，不以小夹具替代 |
| 在线与恢复完全相同 | 同输入、同参数、同后端、同线程和随机数位置重新执行 | 修改已序列化权限或依赖身份并重新计算摘要 | 跨CPU/PyTorch版本不宣称逐位相同 |
| 自然语义/动作资格 | 提议回执不授予账本或联合发布资格，视频前后核心身份一致 | 伪造ledger_authorized及谱系资格 | 本轮没有自然动作正例；不把零副作用检查称完整动作验收 |
| 整个矩阵完成 | 所有预定子命令都执行且exit0，源码前后同Git字节 | 单视频失败而外层列表执行完成 | 新入口遇任一子命令失败最终exit1；不得只挑成功视频 |

第一审：独立float64全键数值参考、不同查询分块的原注意力对照、远端键实际影响、资源边界及原结构攻击。
第二审：不同的完整配置/历史/权限重封、状态机和实现替换攻击，再检查合法恢复；18个既有谱系测试与首审重叠须单列。

三网络仍为此前开发夹具训练权重，未选择正式方法或自然训练日程。新的显式65,536节点配置已在完整真实矩阵关闭特定37,241节点资源缺口；不能据此声称长期在线可扩展、科学收益成立、或完整自然六操作后验已实现。
