# 已复现与已修复问题

第一审初版固定源码 `ce59e246302d7bae525ca8254aca62b7678cf05d`：31 既有/开发检查通过，新增 3 个非权重架构攻击失败（4.12 秒）。先正常生成一个完整提议，再分别改变 LayerNorm eps、MultiheadAttention num_heads 或 encoder activation；参数字节不变，因此原身份检查未拒绝，第二个提议被接受。不是缺字段或只看命名的负例。

修复源码 `3561aad43c23b7078123013fcbcd010820b9760f`：记录实际模块图、影响运算的非张量配置、加载的 forward 与 activation 代码；拒绝运行时 hooks；每次参数身份检查同时验证初始化结构，导出也验证。源码绑定仍不是外部证据或可信执行环境证明。改变 Python 解释器、可信入口或调用者托管根摘要不在认证承诺内。

旧开发派生工件保留，因底层源码更改不再作为修后通过证据。修后必须重新派生到新目录，逐字对照原权重；原始训练摘要与源权重不修改。两轮结果及覆盖限制另见最终报告。

第二审固定源码 `4cfa2316f8face8729f39e73e396c0f97653e9a7`：加载方法替换、hook 和错误结构导出 3 项通过；4 个缓存数据攻击失败（2.09 秒）。通过 `.data` 分别改写完整图 memory、候选 vectors、缓存 key/value，版本计数不变，原 scorer 仍返回分数。修复 `b9068a656ca9e4b2958867927a05ff41feb4fabc` 在评分前后校验这些实际缓存张量的类型/形状/设备/字节，拒绝后恢复原字节可合法重试。

最终验证入口与实现一起冻结为 `925980d4d0d9014f8cbdf2b54e941dc81632db7c`。该入口按顺序执行第一审回归、第二审、静态检查、三臂派生/303 全支持对照及四段固定视频；每条命令记录开始 UTC、耗时、退出码、完整 argv，并逐个验证所有被纳入 Python 文件与 Git 对象相同。未完成前不声明最终通过。

固定源码 `925980d4d0d9014f8cbdf2b54e941dc81632db7c` 的第一审 99、第二审 25、mypy 365/Ruff 通过，但实际 303 候选大图对照失败：typed_factor_graph_transformer 的 655,744 个 memory 元素中 17 个超出预先容差，最大绝对差 3.3020973e-5；九因子 log q 最大差 4.5674416e-7、总质量均 1，不能因此忽略中间图失败。完整运行在此停下，未继续宣称三臂或视频通过。

对照 [PyTorch 2.13 CPU attention 源码](https://raw.githubusercontent.com/pytorch/pytorch/v2.13.0/aten/src/ATen/native/transformers/attention.cpp) 的实际执行路径后，修复 `6894059defcab6e117548423166c461f6499fa31`：自注意力采用不含 bias 的投影，接同版本 bias/rescale 与 masked-softmax 运算，再沿查询行做乘法；每行仍读取全部键。交叉注意力保留全键 SDPA。未调宽 atol/rtol；私有内核依赖明确受执行 PyTorch 版本及源码摘要约束，其他版本需重新验证。

补充后果追查在 `6894059defcab6e117548423166c461f6499fa31` 复现完整候选—因子对应关系置换：上下文和目标集合不变、质量仍 1，但最大 log 因子差为 0.00853683023703744。缓存张量检查漏掉了关联元数据。该版本已完成两审及三臂 303 数值/恢复，但补充攻击使其整体验收重新打开；正在计算的视频批次于 UTC 00:34:59 主动停止（143），仅原第一段前两臂完成，不计完整视频通过。随后才编辑源码，将上下文身份、候选集合、对应因子行与节点数一并纳入缓存摘要，新增攻击及合法重试。最终所有步骤须重跑。
