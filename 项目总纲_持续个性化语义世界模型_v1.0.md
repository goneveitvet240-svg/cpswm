# 持续个性化语义世界模型与家庭具身智能机器人项目总纲

英文工作名：**Continual Personalized Semantic World Model for Household Robots**  
版本：`v1.0`  
日期：2026-08-10  
性质：长期研究总纲、系统路线图与后续子项目准入依据

---

## 0. 总纲效力与方向声明

本文件确立此后相关研究与工程工作的共同最终方向：

> 构建一个能够长期生活在家庭环境中，持续观察并学习主人及家庭成员的生活习惯，维护包含物体实例、三维位置、时间历史、人物归属、使用事件、关系结构、隐藏状态推断和概率不确定性的个性化语义世界模型；机器人能够通过自然语言理解模糊、时间化和功能化指令，在部分可观测环境中推断物体的未观测转移，完成搜索、导航与操作，并在执行任务的同时持续更新、纠错、巩固和遗忘长期记忆。

本方向是一个独立的新总项目，不以任何既有项目的问题定义为前提。过去项目中的代码、数据或方法只有在确实服务于本总纲时，才可作为普通组件评估是否复用；不得让任何旧项目的动作空间、实验环境、目标估计量或叙事边界反向限制本项目。

特别规定：

1. 本项目的中心是**长期记忆驱动的持续个性化语义世界模型**。
2. 主动搜索、重新观察和信息获取是世界模型的下游能力，不是项目的唯一中心。
3. 搜索时间、移动距离、观测成本和验证成本是系统评价的一部分，不得取代语义地图质量、长期记忆质量、人物—物体关系和持续学习能力。
4. 系统范围可以完整，但每个阶段必须明确其独立研究问题、可测改进和与总系统的接口。
5. 后续子项目必须强化本世界模型的表示、感知、更新、学习、推理、查询或行动闭环之一。

---

## 1. 项目愿景

### 1.1 机器人不只是执行命令，而是长期理解家庭

目标机器人不是一次性接受地图、执行固定指令的工具，而是家庭中的长期具身智能体。它通过日常观察逐渐理解：

- 主人喜欢吃什么、喝什么；
- 主人和其他家庭成员经常使用哪些物体；
- 不同人物通常把物体放在哪里；
- 同一物体在不同时间、活动和人物条件下的常见位置；
- 哪些物体属于主人、家庭公共区域或客人；
- 哪些物体位置稳定，哪些经常移动；
- 哪些物体会离开后返回，哪些形成新的常用位置；
- 家庭布局、物体关系和生活习惯如何随时间变化。

机器人形成的不是通用常识数据库，而是针对当前家庭和人物的 personalized household model（个性化家庭模型）。

### 1.2 语义地图从空间快照升级为长期世界模型

传统语义地图主要回答：

```text
这里有什么？
某类物体最后在哪里？
```

本项目的语义世界模型还要回答：

```text
这是哪个具体物体实例？
谁拥有或使用过它？
它昨天在哪里、现在可能在哪里？
谁可能移动了它？
当前位置是直接观测还是事件推断？
哪些位置是长期习惯，哪些只是临时异常？
机器人为什么相信或怀疑这条记忆？
```

长期记忆与语义地图必须共同演化：记忆提供跨时间证据，地图组织空间与实体状态；新地图状态又成为后续记忆、推理和行动的基础。

---

## 2. 目标机器人的完整能力

### 2.1 长期、持续、非任务限定的家庭观察

机器人应在获得合适授权和隐私边界的前提下，长期积累家庭中的人物、物体、地点和活动证据，而不是只在收到搜索命令后才开始感知。

它应能够：

- 记录物体的常见位置和历史位置；
- 记录人物与物体的使用、携带和交互事件；
- 学习时间、人物、活动与位置之间的规律；
- 发现家庭布局或习惯变化；
- 区分一次性异常与持续形成的新规律；
- 将日常任务中的顺带观测写回长期地图。

### 2.2 物体实例级记忆

机器人不能只识别 `cup`、`book` 或 `tool` 类别，还要维护具体实例，例如 `Cup_17`、`Cup_23`。

实例记忆至少包含：

- 类别、属性、外观和三维几何特征；
- 多视角表征；
- 当前身份置信度；
- 历史身份关联；
- 与其他相似实例的区分证据；
- 最后确认时间与位置；
- 使用者、拥有者及相关事件；
- 身份冲突和候选匹配。

### 2.3 人物归属、使用关系与家庭角色

世界模型必须区分 `belongs_to`（属于）、`used_by`（被使用）、`carried_by`（被携带）和 `shared_by`（共同使用）等关系。

示例：

```text
Cup_17 belongs_to Person_A
Cup_17 used_by Person_A yesterday
Cup_23 belongs_to Household
Cup_23 used_by Guest_B today
```

机器人应避免把客人刚使用的相似杯子误认为主人要求的杯子，并在证据不足时维护多个候选，而不是强行给出确定身份。

### 2.4 时间化情景记忆

机器人应保存 episodic memory（情景记忆），记录：

```text
谁，在什么时间、什么地点，对什么物体做了什么；
哪些内容被直接观察；
哪些内容由模型推断；
推断依赖哪些证据。
```

情景记忆支持“刚才”“昨天”“上周”“经常”“最近一次”等时间指代，并允许查询具体活动中的物体实例。

### 2.5 个性化习惯学习

机器人需要学习条件化习惯分布：

\[
P(\text{location}\mid \text{person},\text{object instance},\text{time},\text{activity},\text{context})
\]

习惯模型应表达：

- 同一物体的多个常见位置；
- 人物之间的差异；
- 日间、夜间、工作日和周末差异；
- 活动触发的临时移动；
- 离开后返回原位的规律；
- 习惯改变的时间边界；
- 新习惯与偶然异常之间的不确定性。

### 2.6 关系与因果事件推断

机器人应从人物—物体—地点事件链推断未被直接观察的状态变化。

例如：

```text
t1: Cup_17 observed on Desk
t2: Person_A holding Cup_17
t3: Person_A entered Kitchen
t4: Cup_17 not directly observed
```

系统不应直接写入 `Cup_17@Kitchen=true`，而应更新：

```text
P(Cup_17 @ Desk) decreases
P(Cup_17 @ Kitchen) increases
P(Cup_17 still being carried) remains non-zero
evidence_source = inferred_from_carry_event
```

需要支持的关系与事件包括但不限于：

- 人拿起、携带、放下或交接物体；
- 人进入或离开房间；
- 人使用某物体完成某种活动；
- 物体共同出现或共同移动；
- 房间、家具、容器和物体之间的空间关系；
- 直接观测、语言信息与推断证据之间的依赖关系。

### 2.7 部分可观测下的概率世界状态

家庭环境中存在遮挡、视野外移动、相似实例、误检、漏检和未观测交接。地图不能只保存单一硬状态，而应维护联合信念：

\[
P(\text{location},\text{identity},\text{owner},\text{state},\text{relations}
\mid \text{observations},\text{events},\text{history})
\]

系统需要：

- 保留多个竞争假设；
- 区分“没有看见”和“确认不存在”；
- 保存证据来源和观测质量；
- 在新证据到来时进行一致更新；
- 发现并处理地图内部矛盾；
- 防止单次误识别覆盖长期稳定记忆。

### 2.8 严谨的记忆巩固、弱化、纠错与遗忘

记忆管理不得简化为“距离上次观察多久 + 物体类别通常多稳定”。当前记忆是否可靠还取决于：

- 最后观测质量；
- 实例关联置信度；
- 后续人物和物体事件；
- 遮挡与视野覆盖；
- 新旧证据是否冲突；
- 该物体的返回和转移规律；
- 记忆来自直接感知、语言还是推断；
- 家庭习惯是否发生变化。

系统必须区分：

- forgetting（遗忘）；
- uncertainty growth（不确定性增长）；
- contradiction（记忆被反证）；
- consolidation（记忆巩固）；
- hypothesis replacement（候选假设替换）；
- stale but useful memory（陈旧但仍有用的记忆）。

### 2.9 异常位置发现与地图持续更新

机器人在执行任何任务时，如果观察到“平时在书房的书出现在沙发上”等异常，应：

1. 判断是否为同一物体实例；
2. 记录新的时空观测；
3. 更新当前状态概率；
4. 保留旧的常见位置分布；
5. 判断临时移动、新习惯或识别错误等候选解释；
6. 更新相关人物、活动和地点关系；
7. 将该证据用于未来查询、预测和行动。

### 2.10 自然语言记忆查询

机器人应理解模糊、时间化、人物条件化和功能化指令，例如：

- “找我昨天喝水的杯子。”
- “不要客人刚才用过的那个杯子。”
- “找昨天从书房拿出来的那本书。”
- “找一个能帮我修车的工具。”
- “找我经常放在床边的那个东西。”
- “找昨天吃过的红色东西。”

LLM（大语言模型）负责：

- temporal grounding（时间语义落地）；
- reference resolution（指代消解）；
- attribute grounding（属性落地）；
- affordance reasoning（可供性推理）；
- 把自然语言转换为结构化地图查询；
- 根据事件链提出候选解释；
- 在描述不完整时生成可检查的候选集合；
- 生成面向用户的推理说明。

LLM 不得代替真实地图或直接把语言猜测写成事实。它提出语义约束和推理候选，世界模型结合感知证据维护概率状态。

### 2.11 主动搜索、导航与操作

机器人搜索时应综合：

- 当前地图信念；
- 具体物体实例与主人归属；
- 时间和活动上下文；
- 人物轨迹及未观测转移假设；
- 常用位置与异常位置；
- 视野、遮挡和可观察性；
- 路径长度和任务时间；
- 某次观察能够排除多少候选；
- 是否可以在完成其他任务时顺路获取信息。

若记忆位置没有找到目标，机器人应把搜索结果写回地图，更新假设并重新规划，而不是机械执行固定位置列表。

### 2.12 任务执行与地图维护同时进行

本项目的基本闭环为：

```text
持续观察
→ 人、物体实例与事件识别
→ 更新长期语义世界模型
→ 学习人物习惯和关系
→ 推断未观测状态变化
→ 理解自然语言查询
→ 规划搜索、导航和操作
→ 从任务过程获得新证据
→ 再次更新地图与长期记忆
```

地图不是任务执行前的静态输入，任务执行也不是地图完成后的独立阶段。二者持续共同演化。

### 2.13 可解释的记忆与行动

机器人应能够说明：

- 为什么认为某个候选是主人要求的物体；
- 为什么优先搜索某个位置；
- 哪些事实来自直接观察，哪些来自推断；
- 当前还有哪些候选位置或实例；
- 哪条证据改变了地图；
- 为什么旧习惯被保留、降低权重或替换。

解释必须能够回溯到结构化证据，不能只生成听起来合理但无法核验的语言。

---

## 3. 语义世界模型的数据结构

### 3.1 总体状态

世界模型在时刻 `t` 可表示为：

\[
\mathcal{M}_t =
(\mathcal{G}^{3D}_t,\mathcal{E}_t,\mathcal{R}_t,
\mathcal{V}_t,\mathcal{H}_t,\mathcal{B}_t,\mathcal{P}_t)
\]

其中：

- `G³ᴰ`：几何地图、房间、家具、容器和可通行空间；
- `E`：人物、物体实例、地点、活动等实体；
- `R`：空间、归属、使用、携带、功能和社会关系；
- `V`：按时间排序的事件与情景记忆；
- `H`：人物和物体的个性化习惯模型；
- `B`：位置、身份、状态和关系的概率信念；
- `P`：证据来源、时间戳、模型版本和更新谱系。

### 3.2 物体实例节点

每个物体实例至少包含：

```text
instance_id
category
open_vocabulary_labels
visual_embeddings
geometry_descriptor
current_location_distribution
location_history
owner_distribution
user_history
affordances
mobility_pattern
habit_conditionals
last_direct_observation
last_inferred_event
identity_confidence
state_confidence
evidence_provenance
conflicting_hypotheses
```

### 3.3 事件节点

每个事件至少包含：

```text
event_id
event_type
participants
object_instances
source_location
destination_location
time_interval
direct_or_inferred
observation_sources
confidence
causal_parents
alternative_explanations
```

### 3.4 关系类型

首批关系词表包括：

```text
located_at
inside
on_top_of
near
belongs_to
used_by
held_by
carried_by
moved_by
shared_by
associated_with_activity
usually_located_at
observed_with
visually_similar_to
affords
caused_or_explained_by
```

关系必须允许时间区间、置信度和证据来源，不得被实现成永久不变的布尔标签。

---

## 4. 技术架构

### 4.1 感知与空间定位层

- VIO（视觉惯性里程计）：机器人位姿、局部轨迹和跨时刻观测对齐；
- SLAM / 3D mapping（三维建图）：房间、家具、容器和可通行空间；
- RGB-D / point cloud（点云）：三维物体位置、几何关系和遮挡；
- object detection / segmentation（物体检测与分割）；
- VLM（视觉语言模型）：开放词汇类别、属性、场景和功能理解；
- instance re-identification（实例重识别）：跨视角、跨位置、跨时间身份关联；
- data association（数据关联）：把新观测连接到已有实体或创建新实体。

VIO 解决“机器人在哪里、观测来自哪个位姿”；VLM、几何特征和实例重识别解决“看见的是什么、是不是过去那个具体物体”。

### 4.2 长期记忆与地图层

- 3D semantic graph（三维语义图）；
- episodic event store（情景事件存储）；
- person-object-place-time relational graph（人—物—地点—时间关系图）；
- probabilistic belief state（概率信念状态）；
- habit and transition models（习惯与转移模型）；
- contradiction and consolidation manager（矛盾与记忆巩固管理器）；
- provenance and confidence store（证据谱系与置信度存储）。

### 4.3 推理与持续学习层

- 多假设状态估计；
- 未观测携带、放置和交接推断；
- 物体下一位置和当前位置预测；
- 人物条件化习惯学习；
- 新旧习惯的变化点检测；
- 稳定性—可塑性平衡；
- 关系图更新与冲突消解；
- 长期记忆选择、摘要、巩固和遗忘。

### 4.4 语言接口层

- 自然语言到结构化查询的转换；
- 时间、人物、属性、活动和功能条件解析；
- 候选物体和关系检索；
- 基于证据的推理说明；
- 语言不确定性与地图不确定性的分离；
- 禁止 LLM 无证据覆盖世界状态。

### 4.5 任务规划与控制层

- task planning（任务规划）：把语言目标分解为地图查询、搜索、导航和操作子目标；
- active perception（主动感知）：选择观察位置和视角；
- semantic navigation（语义导航）：利用世界模型生成搜索顺序和路径；
- manipulation planning（操作规划）：抓取、递送、放置和交互；
- MPC（模型预测控制）：局部轨迹、动态约束、避障和精确执行；
- online replanning（在线重规划）：根据新观测同步修改计划和地图。

---

## 5. 核心研究问题

### RQ1：长期记忆如何改变语义地图的表示能力？

能否把以 `类别—位置` 为主的语义地图，升级为包含物体实例、时间事件、人物关系、习惯分布、隐藏状态和证据谱系的长期概率世界模型？

### RQ2：如何在跨天、跨房间和遮挡条件下保持物体实例身份？

如何融合视觉、三维几何、运动连续性、人物交互和历史事件，降低相似物体之间的 ID switch（身份切换）？

### RQ3：如何学习主人和家庭成员的个性化习惯？

如何从有限、偏置且不断变化的长期观测中学习人物—物体—地点—时间规律，同时避免把一次异常或客人行为错误固化为主人习惯？

### RQ4：如何根据关系事件推断未观测物体转移？

在机器人看到拿起和进入房间、但没有看到放下的情况下，如何维护可解释的多假设位置分布，并随新证据更新？

### RQ5：如何让 LLM 查询真实、可追溯的长期世界模型？

如何把模糊语言转换成时间、人物、活动、属性、功能和实例约束，并保证回答和行动能够回溯到地图证据，而不是依赖语言幻觉？

### RQ6：如何让任务执行反过来持续改进语义地图？

如何统一任务相关观察与顺带观察，使搜索、导航和操作产生的新证据实时进入地图，并在不中断任务的情况下修正身份、位置、关系和习惯？

### RQ7：长期持续学习如何兼顾适应与稳定？

如何快速适应新物体、新家庭成员和新生活习惯，同时保留仍然有效的长期知识，并明确哪些记忆应巩固、弱化、保留为历史或真正遗忘？

---

## 6. 后续子项目体系

本总项目允许形成多个相互连接的研究子项目，但所有子项目共享世界模型接口和最终方向。

| 子项目 | 中心问题 | 对总系统的贡献 |
|---|---|---|
| SP1 长期实例语义地图 | 跨时间物体实例重识别与三维关联 | 建立可靠实体层 |
| SP2 人物—物体关系记忆 | 归属、使用、携带与交接关系 | 建立个性化关系层 |
| SP3 个性化习惯学习 | 人物条件化位置和活动规律 | 建立家庭习惯模型 |
| SP4 情景与隐藏事件推断 | 漏观测下的物体转移和多假设推断 | 建立事件与推理层 |
| SP5 持续记忆管理 | 巩固、矛盾消解、变化点和遗忘 | 保证长期稳定运行 |
| SP6 LLM 世界模型查询 | 时间、指代、属性和功能化查询 | 建立语言接口 |
| SP7 任务耦合地图维护 | 执行任务时同步更新地图 | 闭合感知—行动循环 |
| SP8 主动搜索与操作 | 使用世界模型完成检索、导航和抓取 | 验证地图的任务价值 |
| SP9 集成家庭机器人 | 多模块长期部署与系统评价 | 形成完整具身系统 |

已确认的项目方向结构：

- [方向结构一：长期家庭观察与持续个性化习惯世界模型](docs/research/项目结构一_长期家庭观察与持续个性化习惯世界模型_v0.2.md)：规定机器人通过跨天、跨周和跨月观察形成家庭生活模型的完整研究范围，包括物体属性、常识与场景理解、移动性、多位置记忆、人物习惯、偏好规范、语言查询和具身协助。
- [方向结构二：选择性观察、隐藏事件、同屋多人和非平稳个体习惯](docs/research/项目方向结构二_选择性观察隐藏事件同屋多人非平稳个体习惯_v1.0.md)：研究如何从选择性、稀疏且不完整的长期观察中，联合学习具体人物的物体摆放习惯、推断未观测人—物事件、抵抗多人污染并适应习惯变化；该方向可形成一篇或多篇论文，但不取代方向结构一及总项目其他主线。其[紧邻研究方向与实现方法检索总结](docs/research/方向结构二_紧邻研究方向与实现方法检索总结_2026-08-13.md)单独维护强基线、方法证据和新颖性边界。
- [方向结构三：模糊语言联合后验、主动确认与不确定性具身闭环](docs/research/项目方向结构三_模糊语言联合后验主动确认与具身闭环_v1.0.md)：研究如何把模糊语言编译为可检查查询，以语言、视觉、几何、事件、人物和习惯证据形成包含 `unknown` 的实例级联合后验；在 `ambiguous` 时通过多视角、实例重识别、必要且安全的触觉以及信息价值主动确认，并把未找到、抓取失败、滑落、部分和未知结果作为概率证据写回长期世界模型。

已确认的跨子项目技术子结构：

- [OAM-PHM：机会式观察、多人物归因与抗污染个性化习惯记忆](docs/research/opportunistic-observation-personalized-memory-substructure-v0.2.md)：横跨 SP2–SP8，负责把任务途中获得的非目标物体观察连接到人物归因、实例身份、隐藏事件、习惯阶段、持续记忆管理、查询解释和成本感知行动闭环；其完整能力均属于总项目，不构成独立世界模型。

子项目可以分别形成论文或工程里程碑，但不得彼此建立互不兼容的地图格式、实体 ID 或事件语义。

---

## 7. 分阶段实施路线

阶段划分用于控制工程复杂度，不代表最终研究范围被永久限制。

### Phase 0：任务、地图和证据标准

目标：先定义机器人究竟需要记住和回答什么。

交付：

- 统一实体、关系、事件和证据 schema；
- 家庭长期任务集；
- 时间化、人物条件化和功能化查询集；
- 数据隐私、人物授权和保留策略；
- 强基线与评价协议；
- 最小可运行世界模型接口。

### Phase 1：实例级三维长期语义地图

目标：建立跨时间可持续更新的实例语义地图。

交付：

- VIO/SLAM 与三维对象地图；
- 开放词汇检测与实例重识别；
- 多视角融合和数据关联；
- 物体位置历史、身份置信度和冲突记录；
- 当前状态与历史状态的分离存储。

### Phase 2：人物关系、情景事件与习惯模型

目标：让地图知道“谁、何时、如何使用或移动了什么”。

交付：

- 人物—物体事件识别；
- `belongs_to / used_by / carried_by` 等关系；
- 事件时间线；
- 条件化习惯分布；
- 异常位置与习惯变化检测。

### Phase 3：隐藏状态与关系推断

目标：处理没有被完整观察的物体移动和身份变化。

交付：

- 多假设位置和身份后验；
- 携带、进入房间、放置和交接推断；
- 直接观测与推断证据分层；
- 冲突消解和因果解释；
- 当前状态预测和下一位置预测。

### Phase 4：LLM 记忆查询和解释

目标：通过自然语言访问长期世界模型。

交付：

- 结构化查询语言；
- 时间和人物指代消解；
- 属性和 affordance（可供性）查询；
- 查询结果的证据回溯；
- 无答案、歧义和多候选处理；
- 面向用户的可核验解释。

### Phase 5：任务耦合地图维护与主动行动

目标：让机器人在完成搜索、导航和操作时同步维护地图。

交付：

- 基于地图信念的搜索与导航；
- 新观测驱动的实时重规划；
- 顺带观测和异常记录；
- 操作后的地图状态更新；
- 地图改进对任务表现的因果消融。

### Phase 6：长期持续学习与家庭部署

目标：验证系统能否在长时间、多人物、习惯变化和开放世界条件下稳定运行。

交付：

- 跨天或跨周长期数据；
- 新家庭成员、新物体和新习惯适应；
- 遗忘、巩固和变化点分析；
- 仿真、半真实和真实家庭层级评价；
- 集成机器人演示与可复现实验包。

---

## 8. 评价体系

### 8.1 语义地图质量

- 三维位置误差；
- 当前物体位置 Top-k accuracy / NLL；
- 地图 object-level precision / recall；
- 动态地图 freshness（新鲜度）；
- 异常位置检测准确率；
- 地图矛盾发现和解决率；
- 直接观测与推断状态的一致性。

### 8.2 物体实例与关系质量

- instance re-identification accuracy；
- ID switch rate；
- owner attribution accuracy；
- use/carry/transfer relation F1；
- 相似实例误拿率；
- 跨房间和跨天身份保持率。

### 8.3 时间与事件记忆质量

- temporal query accuracy；
- event retrieval precision / recall；
- 时间顺序一致性；
- 未观测转移推断准确率；
- 当前状态和下一位置预测；
- 事件解释与真实证据的一致性。

### 8.4 个性化与持续学习质量

- 人物条件化习惯预测；
- 新习惯适应速度；
- continual-learning forgetting（持续学习遗忘）；
- backward transfer / forward transfer；
- 异常行为被错误固化的比例；
- 客人行为污染主人模型的比例。

### 8.5 语言查询质量

- 指代消解准确率；
- 时间约束解析准确率；
- 属性与功能约束召回率；
- 正确实例查询成功率；
- 多候选与无答案校准；
- 回答证据可追溯率；
- LLM 幻觉写入地图的错误率。

### 8.6 下游具身任务质量

- correct-instance retrieval success；
- object-goal success；
- SPL、搜索时间与移动距离；
- 错误拿取和错误递送率；
- 无效观察或无效搜索比例；
- 地图更新前后的任务性能变化；
- 执行任务期间获得的有效地图增量。

下游任务指标用于证明世界模型有实际价值，但不能替代地图、实例、关系、事件和持续学习指标。

---

## 9. 基线与消融原则

为了证明“长期记忆确实提高了语义地图和机器人能力”，后续实验应在同感知、同导航、同语言模型和同观察预算下比较：

- static semantic map（静态语义地图）；
- latest-observation map（只保留最后观测）；
- category-level dynamic map（类别级动态地图）；
- instance map without person relations（无人物关系的实例地图）；
- relational memory without habit learning（无习惯学习的关系记忆）；
- habit model without event inference（无事件推断的习惯模型）；
- LLM retrieval without grounded world state（无真实世界状态约束的 LLM 检索）；
- full continual personalized world model（完整方法）；
- identity/location/event oracle upper bounds（身份、位置和事件上界）。

必须分别消融：

- 三维几何；
- 视觉实例特征；
- 人物关系；
- 时间事件；
- 习惯模型；
- 隐状态推断；
- LLM 查询；
- 任务过程中的地图写回；
- 记忆巩固与遗忘。

“换了更强的 VLM、LLM 或导航器”不能被误写成长期记忆或语义地图方法收益。

---

## 10. 首个系统原型

首个原型用于验证完整闭环，不代表最终系统范围收窄。

建议原型具备：

- 一个家庭或家庭仿真环境；
- 多房间三维地图；
- 至少两类人物角色：主人和另一家庭成员/客人；
- 多个外观相似的物体实例；
- 杯子、钥匙、书、遥控器、食品和工具等多类物体；
- 跨多个 session 的观察与事件历史；
- 拿取、携带、进入房间、放置和遮挡事件；
- `昨天/刚才/经常/属于谁/用于什么` 等查询；
- 搜索过程中的实时地图更新；
- 结构化证据、概率状态和可解释输出。

首个端到端演示目标：

> 主人要求机器人寻找“我昨天喝水的杯子”。机器人从人物—物体使用事件、时间约束、实例外观、历史位置和未观测携带推断生成候选；在搜索过程中同步更新地图，排除错误杯子和客人使用的杯子，最终返回正确实例，并解释其判断依据。

---

## 11. 项目风险与应对

### 11.1 系统过大

应对：保持最终系统完整，通过共享接口拆分阶段和子项目；阶段化不等于永久砍掉能力。

### 11.2 物体身份错误长期传播

应对：维护身份概率、多候选和证据谱系；禁止一次观测静默覆盖历史实体。

### 11.3 LLM 幻觉污染地图

应对：语言推断与感知事实分层；LLM 只产生候选和结构化约束；地图写入必须记录证据类型。

### 11.4 客人和异常行为污染个性化模型

应对：人物条件化、异常检测、变化点和长期/短期记忆分层。

### 11.5 持续学习导致灾难性遗忘

应对：事件回放、结构化巩固、参数与非参数记忆结合、历史版本和可逆更新。

### 11.6 地图提升无法转化为机器人能力

应对：同时评价地图层和任务层，通过同预算消融验证地图改进对查询、搜索、导航和操作的作用。

### 11.7 真实家庭隐私与数据伦理

应对：最小化采集、人物授权、本地处理、可删除事件、敏感信息分级、明确保留周期和审计记录。

---

## 12. 后续子项目准入规则

任何后续项目在开工前必须回答：

1. 它增强世界模型的哪个组成部分？
2. 它更新、读取或维护哪些实体、关系、事件或概率状态？
3. 它怎样与统一地图和事件 schema 兼容？
4. 它解决了哪种长期家庭任务中的真实失败？
5. 它相对同信息、同预算基线增加了什么能力？
6. 它产生的代码、数据、模型或评价能否被总系统复用？
7. 它是否错误地把某个下游模块重新包装成整个项目中心？

符合下列任一情况的项目不得直接成为主线：

- 与长期个性化语义世界模型没有清楚接口；
- 只更换更大的模型但不增加地图或长期记忆能力；
- 只优化单个代理指标，无法解释对世界模型或机器人能力的意义；
- 创建与总纲不兼容的独立地图、实体 ID 或事件格式；
- 因历史代码已经存在而反向改变研究方向；
- 把普通模块集成包装为创新，却没有新任务、新能力或可测改进。

---

## 13. 预期长期成果

### 科学成果

- 长期、个性化、事件化和关系化语义地图表示；
- 跨时间物体实例保持与人物归属推断；
- 未观测物体转移的关系/因果推断方法；
- 面向家庭习惯变化的持续学习方法；
- LLM 与可追溯概率世界模型结合的查询方法；
- 任务执行与地图维护共同演化的具身学习机制。

### 工程成果

- 统一的长期语义世界模型 API；
- 人—物—地点—时间事件数据库；
- 实例级三维地图；
- 习惯学习和隐藏状态推断模块；
- 自然语言查询接口；
- 搜索、导航、操作和地图更新闭环；
- 仿真、半真实与真实机器人适配器。

### 数据与评价成果

- 多人物、多物体实例、跨 session 的家庭事件数据；
- 时间化、人物条件化和功能化查询任务；
- 物体身份、关系、习惯、事件和地图更新评价协议；
- 长期部署中的隐私、证据谱系和可解释性规范。

---

## 14. 项目最终定义

本项目最终要构建的机器人是：

> 一个能够长期生活在家庭中，持续观察并学习主人及家庭成员的习惯，维护包含具体物体实例、三维位置、时间历史、人物归属、使用事件、空间关系、活动关系、隐藏状态和不确定性的个性化语义世界模型；它能够利用视觉、几何、事件和语言证据推断未被直接观察的物体转移，通过 LLM 理解模糊、时间化和功能化指令，完成搜索、导航、抓取和递送，并在执行任务的过程中持续更新、纠错、巩固和遗忘长期记忆，使语义地图随着生活经验不断成长。

本项目不是单一的“找东西”系统，也不是单一的“是否重新观察”决策器。它研究的是：

> **家庭机器人如何通过长期生活经验，形成一个持续演化、个性化、可推理、可查询、可行动的语义世界模型。**
