# 结构二窗口三 R5：公开修订、冻结参考、语义摘要与主干工程

本轮工作树：`/private/tmp/s2-w3-native`。真实起点是 `281e88894fca527df1d54018b5053469e8c422d5` **加全部已交付 R4 未提交修改**。未 reset、提交、合并或推送。未改原独立审核脚本、旧证据、方法选择/授权状态、科学阈值或窗口二比较政策。

证据目录：[`w3_repair_r5_20260912T055846Z`](data/w3_repair_r5_20260912T055846Z/)，下称 E5。**三条 R4 独立反例的行为已经修复并有新增正反检查；完整主干、全场景延期生命周期和七算子合作尚未整体验收。** 本报告不把测试总数解释为完整语义正确、方法创新或基线优势。

## 1. 起点、复现与源码绑定

- `E5/start_manifest.json`：HEAD、767 个相关源/测试/配置/应用文件哈希、解释器、PYTHONPATH、原独立目录 20 个文件哈希；`tracked.patch` 保存起点已有修改。
- `head.tar.gz`、`current_source_start.tar.gz`、`untracked_start.tar.gz` 分别保存 HEAD、真正执行的源码以及起点新增文件。R4 新增 oracle、semantic identity、测试和证据没有丢失。
- 原样运行 `w3_new_boundary_probes.py /private/tmp/s2-w3-native`，`before_boundary.json` 与 `before_boundary.execution.json` 保留修前结果和源码一致性。该脚本不因发现反例而返回非零，不能用退出码 0 宣称验收。
- Python：主仓库 `.venv/bin/python`，CPython 3.13.5；起点 `pip freeze` 因没有 pip 模块失败，保留错误并用 `importlib.metadata` 保存实际发行包；没有安装或更换环境。
- 最后交付以 **`E5/delivery_*` 和 `E5/delivery_source/`** 为准。`verified_*`、`final_*`、`final_source/` 属于两次扩展缺陷修复前的中间源码，原样保留，不能混用。

| 原反例 | 修前 | 修后证据 |
|---|---|---|
| 直接 CORRECT 虚假成功 | 返回 correct，old/new 都不在长期集合，new 也不在隔离集合 | 原脚本现在得到明确 ValueError；额外检查整个执行状态恢复、旧记录仍存活、新记录无残留。另有三条历史入口的非空成功纠正 |
| 纠正后再冻结的 oracle | 实际 2 条、参考 1 条，合法状态被拒绝 | 实际/参考均为 2；检查不改变生产状态；增加多代纠正和再撤回等矩阵 |
| 修订语义摘要 | 同语义仅 authorization `basis_sha256` 不同 | 两次语义相同、差异路径为空；原始哈希/谱系完整性检查仍开启 |

修后原脚本的 `direct_revision.unexpected_error` 字段是脚本自己的异常分类，**不能改写为它输出了“通过”**。新 API 将原来虚假 CORRECT 改为明确拒绝；是否原子恢复由独立的公开入口状态断言补证。

## 2. A：公开修订结果与原子性

`core.apply_event_revision_outcome` 自己验证 CORRECT 的新版本确实进入长期集合；否则抛错，并以 `BaseException` 捕获恢复整个事务。删除上层专有的重复补偿检查，确保直接调用也有相同后置条件。历史写入资格门、父资格摘要、正式纠正证据和资格缺失拒绝保留。

ProjectOne 请求保留独立的明确延期合同，不能让同步 CORRECT 的成功断言把合法延期全部变成拒绝。公开请求及延期重试均有完整事务保护；外来 owner/object/location、错误父事件或 owner delta 在进入队列前拒绝。多轴反馈使用真实共享引擎和消息传递器；任一统计请求 REJECTED 时，恢复反馈去重、引擎、历史、资格与统计。

新增覆盖见 `test_structure_two_w3_round5_boundaries.py`：

- legacy/direct-P5/debt-replay × 同/异位置 × owner mass 0/0.25/1，直接公开纠正非空成功；新旧集合、资格、来源、账本及独立统计参考核对。
- 8 个历史目标 × 2 种 owner 质量 × direct/ProjectOne，区分实际成功、明确拒绝与真实延期；没有把历史末段的合法成功强行改成“必须拒绝”。
- Hybrid/Dirichlet/RLS 三个阶段 × RuntimeError/KeyboardInterrupt × direct/request 的失败恢复和同请求重试。
- 同步连续纠正、纠正后撤回、重复/乱序反馈、原十条受阻观测不得通过重建间接提交，继承并重跑 R4 矩阵。
- 多轴公开入口：成功、原始绑定重放、同主张换 ID 重投拒绝、中断恢复。换 ID 不会获得独立性证书；没有自行签发证书来制造第二次更新。

**本轮额外发现并修复**：REJECTED 请求此前写入 `action_scoped_negatives`，外来 owner/object 也能留下动作副作用；DEFERRED 的同一 owner delta 同时写入队列与该字典，被计算两次。`refusal_action_before` 保存 4 个真实失败；`refusal_action_after` 保存修后正反和行动邻接回归。现在初次拒绝只追加拒绝审计，整个执行状态其他字段必须逐键相同；延期增量只从队列读取一次。没有改行动阈值或把拒绝当成负观测。

重复 ProjectOne 请求可追加明确 REPLAY_NOOP 回执：新增测试逐键证明除了 `project_one_application_receipts` 外执行状态不变，并检查只增加预期数量的回执。审计记录的变化不被藏在“完全不变”断言中。

**延期覆盖边界**：真实公共入口已证明原先隔离 → 合法 CCRR 晋升 → 延期请求应用恰好一次的非空正例；也证明纠正后进入 `promotion_finalizes` 的非空待处理状态、重复请求及后续 CCRR 丢弃的终结。后者仍沿用既有合同：纠正先被接受，后来终止的是统计晋升；没有实现取消已接受纠正并重放恢复父贡献。若这类终结也要求“拒绝即恢复父贡献”，A 的这一子项仍不通过。该模式下最终成功晋升、任意交错请求和进程崩溃后 durable 恢复的全矩阵仍未完成。不能据上述正例宣布所有延期分支都已验收。

## 3. B：已纠正初态的独立参考

core 在正式事务中保留 `_revision_parent_events` 原始父输入，随失败回滚。oracle 冻结原观察、完整父链、历史资格和操作；将原始输入依次应用已校验历史操作及新操作，再以自己的存活遍历和存储规则计算预期。

预期集合不从最终 `_committed_events` 建立；无参考不读取被测值补缺口；不调用被测重建作为参考。最终集合只出现在比较一侧。增加源记录、观测证据链、位置、owner 质量、Hybrid 计数/账本多重性、Dirichlet 和各 RLS 阶段核对。

正例包括纠正后冻结且不操作、1/2/3 代后冻结、继续纠正/撤回、授权人撤回。反例包括伪父资格、错 basis、漏操作、漏父输入、循环和错引用；保留原八类检出：漏纠正、额外阻断提交、错权重、错位置、重复记账、漏撤回、回滚残留、漏操作日志。每类先运行合法正控制，再要求真实断言失败。

**独立性边界**：操作验证、输入重构、成员遍历与 Hybrid 标量求和独立；CCRR、Dirichlet、RLS 使用共享叶算法的新实例。没有证明共享数学算法的正确性、冻结前外部托管真实性、感知真值或完整粒子联合模型正确性。合法来源格式不等于外部证据真实性证明。

## 4. C / G6：保留原始完整性的语义对应

原始 Hybrid 日志先恢复验证完整链；资格先验证原始 basis、父资格摘要、操作摘要、完整父关系。然后对已知内部 correction/evidence/snapshot/ledger 标识作保引用的同构重命名，并重算**对应完整正文**的语义哈希。不是删掉 authorization 字段。

重建授权增加 `observation_log_revision_ids`，让原 `observation_log_sha256` 有可核对的原始正文。该正文、basis_json、grantor、来源、父版本均保留。执行摘要与语义摘要继续分别返回；原始快照绑定、重放防护和源码绑定没有关闭。

额外多轴探索发现内部合成证据 ID 还进入 CHEH 历史摘要，见 `explore_remaining.txt`。修复先重新验证全部原始 CHEH 历史内容哈希、派生 UUID 和父链，再对应内部反馈证据/簇和派生历史哈希；外部反馈来源、端点、语义/主张指纹仍保留。新增公开多轴正例、重复、拒绝、中断恢复跨运行对照及原始历史哈希破坏拒绝。

同/异位置纠正、连续纠正、撤回、重放、恢复与源/位置/权重改变分别验证。显式粒子来源正文、输入日志和账本前缀也先核验后对应。这个摘要是定义的记忆/修订/统计/行动读出范围，**不是完整进程、所有延期审计或所有控制器私有状态的跨运行等价证明**。

## 5. 全部既有缺口的状态

| 项目 | 状态 | 本轮实际进展 | 未覆盖或剩余工作 |
|---|---|---|---|
| P1 / A | 已修并验证原实证缺陷；广义生命周期部分完成 | 公开成功后置条件、全事务异常/中断恢复、资格父输入、拒绝动作副作用和延期双计数修复 | 上述延期终结/交错/跨进程恢复边界，不报全路径完成 |
| P2：独立参考 / B | 已修并验证有界矩阵 | 纠正初态、完整必要父链、历史授权与八类故障检出 | 共享叶算法与外部原始证据托管边界 |
| P2：非空解禁 | 已修并验证已构造公共路径 | 保留 R4 非空解禁、失败恢复/撤销授权；重跑 | 不能代替 adaptive-only 同位置/负观测全部可达性 |
| G1 | 部分完成 | 同/异位置、负观测、延期重放分别记录；真实后续 CCRR 解禁正例保留 | adaptive-only 同位置/负观测完整固化与证据独立性/寿命规则待定 |
| G2 | 已修并验证已声明反馈连接 | 正式 ingest 调用、共享 engine/passer、真实后验/统计后果与原子恢复 | 不把没有反馈输入的 P0 冒充调用；完整粒子反馈回路属 G5 未完成 |
| G4 | 部分完成 | 生产 identity-switch 输入不再恒零；0/0.95 公共干预改变晋升，direct/debt 传递验证 | 身份概率的上游估计与校准未完成 |
| G5 | 部分完成，部分设计待决 | native 显式跨步全字段粒子、真实来源/父/统计/账本引用、log-q 消费、条件容器、生产 RGRC 原子束、真实解析回退 | 默认完整联合生成、神经工件、条件统计推导/聚合、完整后验 CIAV/行动、完整粒子回放未完成 |
| G6 | 部分完成；本轮摘要反例已修 | 修订/授权派生哈希与多轴内部证据可复现，原始完整性检查开启 | 语义范围之外的控制器/所有延期/完整恢复状态未证明 |
| G7及非 P0 身份 | 部分完成 | 原额外实例/成员替换矩阵保留；新增 native workspace 原实例/源码约束及替换拒绝 | 所有正式连接的所有因果、时序和中断攻击未穷尽 |
| G9 | 未完成，校准设计待决 | 保留默认保守策略，显式调用方和多轴修订可运行 | 默认自主纠正/撤回未校准；不以 caller-policy 正例替代 |

完整主干工程与科学候选、风险和最小实验分别列在 [`E5/design/remaining_connections.md`](data/w3_repair_r5_20260912T055846Z/design/remaining_connections.md)。保留完整统一框架、隐藏事件、多 actor、开放世界未知、可逆归因和具身反馈。

## 6. 七算子实际连接与因果矩阵

`delivery_collaboration.json` 按真实函数 call/return 当时保存输入/输出 repr、内容摘要、实际对象、源码文件/哈希/行号，另记前后存活集合、资格、统计和动作。不是等全部运行完再序列化可变参数。`delivery_interventions.json` 补充冻结公共身份输入、三种 CIAV 结果和实际 CCRR 调用完成后中断/重试。必要身份回执不是充分的语义证据。

| 算子与真实实例/函数 | 输入 → 输出 → 下游 | 正例/应变证据 | 应不变/拒绝与原子性证据 | 边界 |
|---|---|---|---|---|
| OPCEU：core corrector `weight_for_opportunity` | 实际可见/检测机会 → 权重 → event evidence、统计与行动 | 原 inverse-propensity/可见性公共干预，direct/legacy/debt 实际输入输出 | 不读取字段的 null 对照；P0/普通事务故障回滚 | 不是校准观察模型证明 |
| ORRER：共享 event engine `branch` / `revise_actor_responsibility` | 检测端点、多 actor/机制/角色证据 → 完整事件链与未知支持 → PCHMP / 修订 | 连续学习、真实多轴反馈 owner 权重改变、显式粒子真实链来源 | 外来历史/成员替换、支持不丢失、反馈中断/同主张重投拒绝 | 神经完整候选生成未接入 |
| PCHMP：共享 passer `consume` / `infer` | 真实证据图 → 消息与归一化后验 → 事件统计/反馈引擎 | 证据公共干预到统计/行动、反馈实际 infer 消费 | 证据重排 null、未知支持、同主张防重复；P0维护/反馈故障恢复 | 未证明所有轴间联合校准 |
| CF-BOCPD：实际 online router `observe_online` | 真实时间序列与原因帧 → 阶段/原因概率 → CCRR、CIAV | 真信号变化 vs 常量序列、连续帧和实际 CIAV 输入 | 常量 null、P0维护中断及全事务恢复 | 直接篡改上游输出的旧测试只算接线检查，不算公共因果证据 |
| CCRR：原 reactivator `decide` | 当前候选/阶段与身份概率 → create/stay/拒绝 → 晋升资格/RGRC | 非空公共 HABIT_CHANGE；identity 0/0.95 正反干预 | 未授权不能提升；真实 decide 返回处中断后全状态恢复，原输入重试晋升 | 全生命周期复活/相关验证规则未覆盖完 |
| RGRC：现有 ledger `apply_statistic_bundle` | 唯一来源整簇增量/既有证书 → 原子晋升/撤回 → Hybrid 快照、后续动作 | 生产普通 ingest 真调用；多键全自然参数非空；真实数值抵消触发完整日志重建 | 外域/缺证书/重复键拒绝，三阶段异常/中断、修订回滚、八类 oracle 故障 | 显式粒子条件统计尚未聚合进这条生产账本 |
| CIAV：原 planner `select` / executor `execute_selected_action` | 原因快照/候选行动/实际 likelihood → 选择/验证证据 → 快记忆或不同位置闭包 | likelihood 干预到实际执行器输入与解码动作；同/异位置/未检测分开记录 | 隐私阻断 null、非执行不算负证据、P0暂缓和事务中断 | 负观测完整闭包、完整粒子后验消费、默认自主修订未完成 |

路径分别捕获：P0、direct-P5 同/异位置、debt-replay、12 步稳定/隐藏多 actor 学习后迟到多轴反馈、非空解禁后纠正/撤回及后续行动、显式粒子跨步读出。P0 的维护调用与欠账不等同完整七算子执行；direct 单步也未必触发 `CCRR.decide`，实际确认路径才会触发。

这张矩阵明确了已有正、负、原子证据及缺口，**并未声称每条边的每一种干预和中断均已覆盖**。尤其没有把显式粒子读出接成无授权的默认行动或第二写入权威。

## 7. 回归与证据解释

具体命令、退出码、耗时、JUnit 用例名及执行前后全部 src/tests 哈希保存在对应 `delivery_*.json/.txt/.xml`；最终结果数字见 `E5/delivery_results_summary.json`。保留原 252、新增 105、邻接 82、自动阶段 44，并另跑本轮矩阵与行动读出邻接。

| 最终组 | 结果 | 退出码 / 源码前后 |
|---|---|---|
| `delivery_252` | 252 通过，0 失败/跳过 | 0 / 一致 |
| `delivery_105` | 105 通过，0 失败/跳过 | 0 / 一致 |
| `delivery_82` | 82 通过，0 失败/跳过 | 0 / 一致 |
| `delivery_44` | 44 通过，0 失败/跳过 | 0 / 一致 |
| `delivery_r5` | 134 通过，0 失败/跳过 | 0 / 一致 |
| `delivery_action_readout` | 30 通过，0 失败/跳过 | 0 / 一致 |


R5 没有删改原 252、R4 105、邻接 82、自动 44 的测试断言，没有加 skip/xfail，没有修改科学阈值。相对真实审核起点，既有测试文件保持不变；改的是独立参考辅助实现，新增 R5 测试文件另列。中间新测试的错误预期与修正全部保留：

- `new_boundary_matrix1/2`：保存父事件序列化错误、误把合法 CORRECT 当作拒绝、误把合法延期当作拒绝等探索失败。
- `multi_axis_semantics/2/3`：最初错误地拿第二次运行的随机 target 查第一次运行；又把既有 same-claim 防火墙假定为合法第二次证据、把追加 REPLAY_NOOP 审计假定为完全不改状态。修正的是本轮新测试：保留真实拒绝和严格逐键只准审计变化断言，没有放松生产防火墙。
- `refusal_action_before/after`：四个真实生产缺陷反例先失败，后修复并通过 45 项邻接检查。
- `prepared_preflight` / `verified_r5`：显式恶意 model_construct 输入产生的 Pydantic serializer warnings 保留；它们进入重新校验后被拒绝，不以屏蔽警告或关闭校验获得通过。
- `final_r5` 保留 1 失败/133 通过：拒绝内层用了不保留算子实例的窄检查点。`rejection_identity_before` 再现 CCRR/CF 实例被替换；修复为完整检查点，`rejection_identity_after` 的 134 项通过。没有靠放宽实例或状态断言关闭。
- 原 `run_suite.py` 已把 pytest 退出码写入 JSON，但其父进程本身退出 0；最后交付使用 `run_suite_checked.py` 传播真实失败码，`run_delivery.py` 再核对各组 JSON，防止编排状态掩盖子测试失败。
- `run_final_v2_path_failure.json`：最终编排脚本错误替换了旧结果文件名，启动即失败，未执行测试；保留脚本修前版本和错误说明。修正后重新运行，没有修改被引用的旧证据。

未完成工程与设计待决项不会因所有这些回归通过而自动关闭。
