# 完整支持资源缺口接续（运行中，未签收）

修复PR41固定第三段四帧视频的216→783候选超限。实际图为7,969→37,241节点，密集四头float32分数张量单独需22,190,273,296字节；旧32,768节点配置因此拒绝。此处处理计算能力，不改科学门、候选集、权重或研究范围。

分支codex/pc-a-complete-support-resume-20260926，base PR42 3095a2ac112f7934192177a5ef4725b83188e148。当前受测源码7c19637791e45dbf28e041beb3b98bc9acd92038，替代65aa8b35050b0eb85b04200e867e9dc00d746307；初版1a094b06因新旧审查文件同名导致pytest收集失败，未执行首审，日志保留。重命名审查文件后从头复验。

## 变化与边界

显式派生配置上限65,536节点；原密集默认4,096、原派生默认32,768均保持，必须显式--max-nodes 65536。每次注意力仍读取全部键值，仅减少查询行数。临时容量按keys×heads×(3×元素字节＋2个布尔掩码)×query_rows估算，上限256MiB；不把它冒充整个Python/PyTorch进程的内存保证，也不声称无限可扩展。参数逐字复制，新增训练步0。

浮点容差固定atol=rtol=1e-5。首审针对37,241与65,536键，以独立float64全键求和比对；另外验证强制细分查询行时的完整掩码与数值等价、旧限额外的最后一个键确实贡献全局归一化。二审针对完整重封资源配置/历史、权限伪造、合法重用及注意力故障后的后续随机数。继承的完整缓存/结构/因子关联攻击同时复跑。

## 当前结果

旧65aa8b3的112/33和三臂303数值通过保留，但追加完整正路径攻击发现加载后辅助函数替换仍被接受，不能再作为最终验收。总质量1而最大log因子差0.001152332253，三种实现漂移首测均失败。已停止旧长测、将实际模块函数/导入别名/自有评分方法及资源常量纳入模型结构绑定，修后7c19637从第一审重新执行。final-03命令资料路径拼写错误中断，完整复验使用final-04，不复用旧派生检查点。详见FINDINGS.md。

本轮全部为A自审，独立审核者0、B未复核。原自然路径仍是bootstrap未决候选；不新增自然发布、账本、动作、校准或训练。作者完整HFD下载仅作为原始资料准备，二审与真实验收在本轮完整矩阵关闭后推进。完整统一范围与尚未选择的架构/重采样/先验/自然训练日程保持。

修后7c19637当前结果：第一审112（1既有警告）、第二审36、mypy366/Ruff通过；三个原权重303完整支持均数值/恢复通过，最大memory差1.20e-6、因子log差1.70e-7。原失败回滚与四视频矩阵仍在运行，尚不签收。作者全训练原始归档已下载并匹配9185706983字节和MD5，SHA256 d515b35e9c6cc2a9af717395247eda0a914118d368a09d5b00b7b8da9aded5a4；仅原始获取完成，试次验收仍0。
