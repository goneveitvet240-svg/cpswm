# 语义生产、训练消费与后验行动：阶段交付

日期：2026-09-15。结论：本批组件交付，四项完整能力仍未全部完成。完整连续运行目标不变。

## 源码身份与交接

- 分支：`codex/pc-a-semantic-production-20260915`；base：`d041adca826023f65cc3ac6af696e55862bb3f51`，接续草稿 PR23。
- 本批共同审核生产／工具／测试源码：`0e3848f48f75cfed3d685e7b9a3e16856c277b98`。之后的文档提交不改变这批代码。
- 开工已 fetch 核对共享集成 `19ddf26830348a2f0b33f0af54d6ba702c5cfb1c`、B `fd4ca6ef5e81c90d7cf7987b42c0b2810d8a810a`。未合并共享集成，未改 B 专属比较实现。本机两位独立审核者不替代 B Windows 验收。
- 保留 Route C、Architecture A、P5_FIRST、H/R/I/C/Z/r/V、三个 RB blocks、七功能算子、六修订操作及 C 位姿／本地小预算。未训练、未付费计算、未修改科学阈值。

## 已实现的能力及接线边界

1. `src/cpswm/perception_mapping/pose_observation_model.py`：具备来源标识的位姿测量与独立标签接口；六维局部误差偏差和完整协方差拟合、按序列／输入分离的留出评价。`PoseConditionalHistory` 将校正测量连到现有三块条件统计，纠正／撤销均以保留输入重算；其他两块的特征、目标和质量仍须由自己的生产者提供。自然视觉生产者尚未调用该历史接口。
2. `src/cpswm/data_preflight/proposal_learning.py`：完整可见输入和监督分离，复用原生 proposal sample 合同；operation、parent、H/R/I/C/Z/r/V 九因子的兼容完整提议概率损失可反向传播，涵盖六操作。没有截断历史或只对真值候选重新归一化。此为训练准备接口，未实现三架构训练器、解码器或预算执行器。
3. `src/cpswm/system/joint_camera_policy.py` 与连续入口：读取当前经过原生校验的完整联合后验，经既有 CIAV 选相机观察或停止。动作执行前重算选择并绑定方向、角度、原始输入与后验；问题随命令保存，可恢复待执行命令。当前可由调用方使用，默认 Unity 采集脚本未改为自动连续策略。动作观测似然／效用仍由调用方提供，未独立校准。
4. `tools/fit_pose_observation_model.py` 可读入独立拟合／留出文件，输出模型与评价；本批实际 CLI 验证仅用明确命名的合成夹具，不能作为真实测量标定证据。

详细输入／监督／消费者与六操作映射见[生产连接与监督映射](../../../结构二/04_结构二生产连接与监督映射_2026-09-15.md)。

## 同一源码验证

| 验证 | 结果 | 证据及范围 |
|---|---|---|
| 最终专项／相邻回归 | 239 passed，0 skipped，0 xfail，退出0 | [运行记录](regression_final.json)、[日志](regression_final.log)、[JUnit](regression_final.xml)；9测试文件 |
| 源码与工具／测试清单 | 前后哈希相同 | source_manifest_before.json / source_manifest_after.json；独立新 PYTHONPYCACHEPREFIX |
| 静态检查 | mypy 338源码文件通过；本批9文件 ruff及格式通过 | quality_checks.json |
| 第一轮独立复验 | 23永久回归＋3独立探针＝26 passed，5.70秒 | [第一轮报告](round1/REVIEW.md)，独立 git archive 同一SHA、新缓存 |
| 第二轮独立复验 | 23永久回归＋6独立探针＝29 passed，6.44秒 | [第二轮报告](round2/REVIEW.md)，独立 git archive 同一SHA、新缓存 |
| 位姿 CLI | 合成12拟合／12留出样本执行成功 | synthetic_pose_cli_input.json、synthetic_pose_cli_output；仅数值工程正例 |

相邻回归工程耗时32.548秒，子进程CPU32.039秒，macOS峰值RSS481656832字节。它们不是采集—训练—修订—行动—恢复的全生命周期科学成本。不同审核计数有重叠，不相加为独立覆盖总数。

相机正例使用实际P5-first核心与原生后验，但候选／观测似然和执行环境为测试夹具；证明选动作→执行→新原始输入→恢复，不证明自然人物语义→记忆修订→下一动作变化。

## 审核发现和修复

第二轮初始源码 `269b87374474f8f10eb9f54816b1f948fc199436` 的六独立探针3通过3失败，发现：

- P1：可借用完整模型理由执行相反动作。修复为执行前在核心锁内重算且匹配选项。
- P2：物理等价的q/-q参考被拒。修复为按物理位姿残差比较参考。
- P2：有限极大输入导致评价返回无限值。修复为捕获溢出且验证最终统计有限。

三项永久回归已进入最终共同源码；两位独立审核者均在该源码复验通过。原始失败保留在第二轮报告。第一轮额外覆盖相关协方差、完整概率梯度、READY恢复执行；第二轮额外覆盖更新失败原子性与高成本停止。不存在整条真实闭环签收。

## 自然输入与模型来源核查

- HOH访问申请的本次只读查信仅发现原已发记录，未发现作者回复；申请发出不等于取得可用视频／独立标签。没有重发邮件。
- [100DOH官方手物体模型](https://github.com/ddshan/hand_object_detector)两条官方checkpoint链接本机返回HTTP404；大学下载页证书校验失败，未绕过TLS。其旧CUDA扩展也未在本机部署。
- [HOI-DETR官方实现](https://github.com/AhmadDarKhalil/HOI-DETR)已核查，公开checkpoint约5.86GB且官方推断依赖CUDA，未作为本地小预算可运行模型。
- 因此本批真实新增角色候选、真实测量标定、训练运行和模型驱动真实语义修订均为0；不得把接口／合成标签标成真实输入生产完成。

## 尚缺的交付与下一条运行链

| 用户优先项 | 状态 | 后续具体工作 |
|---|---|---|
| 自然语义候选 | 未完成 | 取得合格小规模自然多人／物体片段及独立标签，接像素接触／释放、实例／世界位姿、角色事件似然；输出未检出／歧义并使 NaturalVisionEvidenceProducer 合法返回 GroundedTransition |
| 完整主干训练消费 | 部分 | 将实测模型和其他条件项接到同一生产历史；实现完整六操作解码／采样与原生监督导出、三架构训练及既定预算执行；不能以当前损失函数代替训练 |
| 同次模型动作闭环 | 部分工程连接 | 独立校准动作—观测模型，实际环境执行模型请求，反馈在同一状态重算解释／账本；迟到反证撤错记忆并改变下一模型动作 |
| 冻结比较与恢复 | 本批恢复／审核完成，正式比较未完成 | 同一实际完整主干上重跑合法强对照、Task7五臂、关键耦合、真实质量与全生命周期成本；由B独立复现／验收 |

这些实施缺口不能全部归因于等待数据。完整验收仍要求单次连续运行贯穿：真实输入→多解释→模型行动→新观测→迟到反证→撤销错误记忆→改变下一动作，并支持中断恢复。各组件的分别通过不能拼成该结论。
