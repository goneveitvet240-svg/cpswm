<?xml version="1.0" encoding="utf-8"?><testsuites name="pytest tests"><testsuite name="pytest" errors="0" failures="1" skipped="0" tests="1" time="2.576" timestamp="2026-09-13T17:12:45.750359+08:00" hostname="MacBook-Air-2.local"><testcase classname="tests.test_structure_two_unified_dependencies" name="test_actual_xdist_workers_use_matching_interpreter_and_modules" time="2.540"><failure message="ValueError: ACTUAL_TEST_RUNTIME_REJECTED: matrix">runtime_tree = PosixPath('/private/var/folders/x2/000r8p0n6s393lj2sg8wk5f40000gn/T/pytest-of-pangwei/pytest-1927/actual_pytest_checkout0')

    def test_actual_xdist_workers_use_matching_interpreter_and_modules(runtime_tree):
        journal, stage = prepare(
            runtime_tree,
            "import pytest\n@pytest.mark.parametrize('n', range(4))\n"
            "def test_live(n):\n import cpswm\n assert cpswm.VALUE == 7\n",
        )
        stage = replace(stage, argv=(*stage.argv, "-n", "2"))
&gt;       execute(runtime_tree, journal, stage)

tests/test_structure_two_unified_dependencies.py:103: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/test_structure_two_unified_dependencies.py:70: in execute
    coordinator.run_stages(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

root = PosixPath('/private/var/folders/x2/000r8p0n6s393lj2sg8wk5f40000gn/T/pytest-of-pangwei/pytest-1927/actual_pytest_checkout0')
journal = &lt;_unified_acceptance_regression.Journal object at 0x10b9b6510&gt;
stages = (Stage(name='matrix', argv=('/private/var/folders/x2/000r8p0n6s393lj2sg8wk5f40000gn/T/pytest-of-pangwei/pytest-1927/ac..., new_outputs=(), test_nodes=('tests/test_fixture_72f4cf2bee5a4c0e8cd608062e858b38.py',), native_plugin_policy=False),)
frozen = {'head': 'c6cee50d182e162b9f2fb68eba05494c26b23d96', 'files': {'.gitignore': '4409b253ac33acb862192f9cad8583ed3fe0bac9...1bcd1e84bb57530eb7f2dfd9fc449f7ed', ...}, 'sha256': '9008a5fa632ac5cca7577c5de8c3e08ffa2f24c9de18c611cf87e61d1c515b9b'}
excluded = ()
environment = {'prefix': '/private/var/folders/x2/000r8p0n6s393lj2sg8wk5f40000gn/T/pytest-of-pangwei/pytest-1927/actual_pytest_check...otated-types', '0.8.0'], ['antlr4-python3-runtime', '4.9.3'], ['ast_serialize', '0.8.0'], ['cffi', '2.1.1'], ...], ...}
before_stage = None

    def run_stages(root, journal, stages, frozen, excluded, *, environment=None, before_stage=None):
        """Shared execution engine. No loaded journal can skip a stage or grant success."""
        journal.value.update(
            status="RUNNING", source_before=frozen, required_stages=[s.name for s in stages]
        )
        journal.save()
        locked = []
        completed = set()
        try:
            for stage in stages:
                if not set(stage.requires).issubset(completed):
                    raise ValueError("REQUIRED_PRODUCER_NOT_COMPLETED: " + stage.name)
                frozen_paths = {p for paths, _ in locked for p in paths}
                if not set(stage.required_artifacts).issubset(frozen_paths):
                    raise ValueError("REQUIRED_VERIFIED_ARTIFACT_ABSENT: " + stage.name)
                for name in stage.new_outputs:
                    path = root / name
                    if path.exists() or path.is_symlink():
                        raise ValueError("generation requires new output: " + name)
                require_artifacts(root, locked)
                require_snapshot(root, frozen, excluded)
                if environment is not None and execution_environment(root) != environment:
                    raise ValueError("execution environment changed before stage")
                if before_stage is not None:
                    before_stage(stage.name)
                if not stage.name.replace("_", "").isalnum():
                    raise ValueError("unsafe stage name")
                row = {
                    "name": stage.name,
                    "argv": list(stage.argv),
                    "cwd": str(root),
                    "status": "RUNNING",
                    "environment_overrides": dict(stage.environment),
                    "started_unix": time.time(),
                }
                journal.value["stages"].append(row)
                journal.save()
                output = journal.path / (stage.name + ".stdout.log")
                error = journal.path / (stage.name + ".stderr.log")
                proc = None
                runtime = None
                if stage.test_nodes:
                    runtime = prepare_test_runtime(root, journal, stage, frozen, environment)
                elif stage.junit and environment is not None:
                    raise ValueError("native test matrix requires actual runtime observations")
                try:
                    with output.open("xb") as stdout, error.open("xb") as stderr:
                        proc = subprocess.Popen(
                            stage.argv,
                            cwd=root,
                            stdout=stdout,
                            stderr=stderr,
                            start_new_session=True,
                            env={**os.environ, **dict(stage.environment)},
                        )
                        row["pid"] = proc.pid
                        journal.save()
                        if runtime is not None:
                            while proc.poll() is None:
                                rejections = list(runtime[0].glob("*.reject.json"))
                                if rejections:
                                    row["runtime_rejections"] = [
                                        json.loads(p.read_text()) for p in rejections
                                    ]
                                    # Most rejecting pytest processes exit immediately. Let
                                    # them flush real diagnostics; early worker failures may
                                    # instead wait for xdist, so bound this grace period.
                                    with suppress(subprocess.TimeoutExpired):
                                        proc.wait(timeout=1)
&gt;                                   raise ValueError("ACTUAL_TEST_RUNTIME_REJECTED: " + stage.name)
E                                   ValueError: ACTUAL_TEST_RUNTIME_REJECTED: matrix

tools/structure_two_unified_acceptance.py:594: ValueError</failure></testcase></testsuite></testsuites>