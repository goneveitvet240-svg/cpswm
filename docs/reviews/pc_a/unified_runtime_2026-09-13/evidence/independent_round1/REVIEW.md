# 第一轮独立审核 — CHANGES_REQUIRED

审核对象：`01d44d58a691d4144ffaaa3c23b9aa414115d977`，目录 `/private/tmp/cpswm-pc-a-unified-runtime-20260913`。2026-09-13。本审核者独立读取已提交新增代码、既有传感校验依赖、合作约束和执行计划；没有编辑候选源码，没有复用作者 probe 结论。审查的是此批输入入口和启动/检查工具，不是全项目完成验收。远端进度核验由主任务执行；本审核只对上述固定本地提交负责。

## R1-01 [P2] 不同释放引用可以重用同一捕获身份并静默覆盖输出

位置：`tools/structure_two_rgbd_ingress_replay.py:48-81`，尤其 59-61、79-81；计数 97-98。

CLI 只要求 release.capture_ref 不重复，却没有检查该引用的编号与收据 step_index/sensor_file 一致。制作一个完整 Git 提交：两个唯一引用 `000000.json`/`000001.json`；内部均为 run=probe、step_index=0、sensor_file=000000.npz；两份合法 RGB-D 的 SHA 与收据一致，时钟合法，manifest.run_id 一致。两个输入都被接受，产生完全相同的两个 observation UUID，后一批无提示覆盖第一批。

实际命令返回 0，回执 `selected_capture_count=2, observation_count=4`；实际只有 2 个 observation JSON 和 2 个 NPY。输入、输出数量和身份的证据链不成立。问题不依赖伪造哈希或未来时间，也不要求信任真实摄像头来源；即使将输入当普通固定归档，内部自洽检查仍漏掉引用身份。

可复现脚本：`probe.py`；输出 `probe_result.json` 和 `duplicate_output/`；完整输入 Git 提交 `b500704498eede5a567f0fed7a92091c5067d81e`，保留在 `fixture_repo/.git`（当前工作树包含后续第二例，原提交永久可 git show）。脚本需在全新输出目录运行，避免覆盖既有证据。

建议：绑定 release 文件名、收据 step_index、sensor_file；显式拒绝重复 observation ID；独占创建输出文件；最终从输出清单核对计数及身份。回归必须保留双不同合法捕获正例、重复内部身份、不同外部引用相同身份三种情况。

## 已执行检查与非缺陷边界

- 独立重跑现有原始输入和独立启动测试：24 passed in 2.50s，`targeted.xml`。合法 RGB-D 可重建数组，元数据不直接成为像素负载，payload 哈希及三时刻、严格数组格式、防对象数组/额外字段、不可变副本检查有效。
- 真正 CLI 完整归档攻击：1 个已复现漏洞，见上。
- 部分失败 probe：第一捕获成功、第二捕获摘要错误，退出 1，无 receipt.json，保留 4 个第一捕获文件。没有观测到“部分失败被正式宣布成功”。后续消费者必须要求完成回执/清单，不能只扫描 observations/ 目录；当前无完整消费者因此不将其夸大为已存在的行动漏洞。
- 同一 run/index/trace 更改 sensor_id/frame_id/session_id/household_id，观测 ID 保持相同；更改 trace_id 则不同。适配器明确将调用者负责的会话和恰好一次分发作为前提，当前 CLI 又固定唯一相机和 namespace，不能直接称为跨会话攻击已成功。真正多相机/会话运行前需明确定义这些 ID 与认证收据的绑定，加入相应测试。
- 完整重封装的合法像素+收据及其新摘要被适配器接受符合其前提：expected_receipt_sha256 必须来自独立可信来源。这段纯函数不能证明相机真实性。CLI 当前从已固定 Git 输入计算摘要，证明归档一致性，不是独立现场认证。没有把这种诚实声明的前提当成算法缺陷。
- lazy exports 明确保留 388 个导出，原定义对象及 fresh-process（全新进程）生产入口测试通过。本轮未发现具体兼容性退化。冻结导出测试依赖仓库包含历史提交 6b97e41，源码快照/浅克隆运行需要另行保障该前提。
- 证据运行器记录选定源码树前后摘要、退出码、命令和有限环境字段；native 模式校验 .venv 和顶级 cpswm 来源。它不证明所有子模块实际加载路径，也不检测运行中改动后恢复、依赖/锁文件变化或独立执行托管。现有文档没有把它叫全面运行证明，故本轮将此列为覆盖边界，不能替代 W1 正式执行身份工具。

## 用户目标的完成边界

当前提交提供“来源可追溯的已存 RGB-D → M05 原始观察”及生产首次导入修复，归档回放标志明确为 new_simulator_run=false、semantic_detector_verified=false、default_joint_runtime_verified=false。本轮未见该 CLI 产生合法语义/角色检测、六操作原生监督、完整联合提议、三个解析块更新、唯一账本撤回及动作反馈串联；当前报告也没有声称存在。因此不能据此判定“能连续修订并影响行动的统一运行版本”已经完成。

全链合法正路径、完整伪造传播至后验/账本/实际动作、中断恢复、连续迟到证据后缀重算、多人物真实感知和默认神经模型均不在本次入口测试覆盖范围。科学配置待决与真实监督缺口不能用本轮通过数抵消。

候选当前缺 docs/collaboration/START_HERE.md 与 TASK_BOARD.md，已通知主任务；主任务计划合入共享分支既有 docs-only 提交后补齐。此项应在最终交付查验。

## 复审要求

修复 R1-01 并在新固定 SHA 复验正例与攻击，再进行第二轮独立审核。此报告只是第一轮结果，不能标为两轮通过，也不能替代电脑 B 对最终生产 SHA 的跨机复核。
