# 自然事件覆盖、手部候选与连续恢复：部分交付

2026-09-20。**用户要求的五项完整任务尚未完成；本批不签收自然P5闭环或科学收益。**

- 独立A分支：`codex/pc-a-natural-event-loop-20260920`；base `769a558bc3deb2c4133f509722a748fa37bdc990`，接续PR28。
- 共享集成 `19ddf26830348a2f0b33f0af54d6ba702c5cfb1c`、B分支 `fd4ca6ef5e81c90d7cf7987b42c0b2810d8a810a` 已fetch核验；不覆盖B修改，不自动合并。
- 初始生产源码 `c8fe8d5152c6713f889a19aee4f4756dfa1de49c`。恢复修复及工具最终源码 `288b429a7d1e1d7f22f282ded333eedcf41957bc`。360帧完整推断仍属于初始源码，不自动升级为修复版完整运行。

## 实际新增

1. 从作者官方HO-Cap档案新增360帧RGB-D及配对标签，保留原始／评价分离、逐文件SHA256、ZIP CRC、下载来源。
   加上此前180帧共540帧。采样只用序列长度选择中段与末段固定窗口，未根据方法结果挑选；全部为开发输入。
2. 新增可执行监督覆盖核查：验证raw/label/receipt配对，识别-1缺失手部与缺失位姿；刚体姿态检查；输出逐帧监督盘点与端点位移。
   没有把有限值当有效手部，也没有把位移当独立抓取／释放标签。
3. 接入可选官方MediaPipe手部模型。通过同一个NaturalVisionEvidenceProducer及ContinuousEvidenceInput，
   将21个2D关键点和左右手候选与既有Faster R-CNN物体候选一起保留。IMAGE模式便于明确恢复边界；CPU模型本地运行。
   不输出伪造人物身份、接触概率、物体位姿或记忆权限。
4. 新增独立评价入口：先固定模型输出，再读取作者关键点，以不依赖左右手预测的几何匹配报告像素误差及未匹配数量。
   匹配数量不是正确检出率；作者半自动标签不是无误金标准；不声称角色校准。
5. 为新增手部状态提供实际模型重建、六帧SQLite中断恢复对照，以及独立审查后的损坏检查点修复。

## 数据核查结论

540帧中有900个可见作者手部实例、2160个物体位姿标签。
112303为单手pick-and-place任务，113202/113535为同一人的双手handover任务；没有独立多人交接、迟到纠正事件标签。
手部／物体标注只供监督盘点和固定预测后的评价，不进入自然生产模型。
原始来源、选帧、模型来源和后续依赖详见 [DATA_AND_DEPENDENCIES.md](DATA_AND_DEPENDENCIES.md)。

## 两轮独立审核边界

第一轮独立审查绑定c8fe8d5，在17项组件测试通过的同时复现两项P2：
缺失或身份不匹配的手部检查点被接受；恢复中途失败会部分改写状态。
见 [review1/REPORT.md](review1/REPORT.md) 与原始反例。本批补充13项修前失败反例，保留失败日志。
第二轮独立审核绑定最终288b429：78项相关测试通过；独立构造6种畸形恢复原子拒绝，合法恢复通过；360帧评价独立复现，并验证交换预测帧即使重设pin仍被拒绝。见 [review2/REPORT.md](review2/REPORT.md)，组件范围无阻断发现。两轮组件审核不能抵扣完整五任务最终统一审核，更不能冒充Windows/B验收。

## 五项任务状态

| 用户任务 | 本批推进 | 未闭合依赖 |
|---|---|---|
| 真实事件片段 | 540帧来源／监督核查和360帧实际补数 | 多人物、接触释放与纠正事件独立标注 |
| 语义证据生产 | 真实像素手部候选、现有实例几何关联、作者关键点评价 | 完整实例／人物角色／接触模型、6D物体估计及误差校准 |
| 同一P5主干 | 自然候选保留在同一连续输入，权限边界保持 | 合法GroundedTransition、全轴提议模型与完整运行工厂 |
| 反证与恢复 | 真实候选SQLite恢复；修复检查点漏洞 | 同次自然反证撤记忆、物理下一动作、完整重跑对照 |
| 固定源码独立审核与B复核 | 本批独立组件审查与源码交接 | 完整闭环最终两轮审核、B实际复跑 |

不能将未完成项全部归因于用户数据：完整位姿、角色／事件模型、真实概率校准及运行工厂仍需实现。
同时，离线视频不响应机器人动作，不能当作具有干预反馈的环境。已询问可接入执行环境及已有多人数据目录。

## 固定预测与最终工程结果

360帧实际本地推断经同一连续入口消费720条RGB／深度观察，产生463个手部候选。
三个序列均为120帧；作者可见手分别120／240／240，模型候选97／163／203，
几何匹配后的21关节平均像素误差中位数分别2.9439／3.5318／3.6132 px。
共137个作者可见手未分配到模型候选；不按左右手预测匹配、不设置事后“正确”阈值，
不把匹配数作为准确率或把该开发评价当新留出确认。全量逐帧输出以无损gzip保留。

修复版154项定向／相邻测试通过（0失败、0跳过），mypy346源码文件通过。
13个新检查点反例在修前全部失败，修后全部通过；保留原始日志。
原有跨进程测试曾直接注入没有原始前缀的视觉帧，修后改为合法raw→检测→保存，
不通过放松检查使非法检查点变成正例。
修复版实际6帧RGB-D，在第3帧后关闭SQLite并重新构造两个真实模型；继续后手部及物体候选
与完整6帧运行逐项相等。该实验验证候选持久性，不验证语义撤销、错误记忆或物理执行。

本批所有真实运行：GroundedTransition=0，语义记忆更新=0，physical actions=0。
工程修复通过、自然闭环未成立、科学收益未验证分别记录。

评价JSON中的predictions_fitted_on_labels=false只描述本次固定预训练推断流程；摘要字段和自给哈希不能证明任意外部JSON的训练来源。审核保留了这一信任边界。

首轮审核者也在最终288b429独立复验原始反例、66相关测试和六真实帧恢复，全部通过。其独立实际运行重新构造模型并关闭/重开SQLite，候选与不中断相等；见 [review1/FINAL_RECHECK.md](review1/FINAL_RECHECK.md)。两位最终组件复验绑定同一代码；仍不是两轮完整自然闭环验收。
