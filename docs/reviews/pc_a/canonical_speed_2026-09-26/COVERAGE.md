# 规范化快路径审查边界

生产改动仅在 `_canonical_value` 开头为**精确内置类型** str/int/bool/None/float 增加等价短路径。没有候选缓存、摘要缓存、校验抽样或跳过；PreparedScorer、模型/图缓存指纹、九因子解码和候选语法源码不变。子类继续使用原有BaseModel、dataclass、datetime、UUID、Enum等处理顺序。

第一审新增差分以原提交60453f212c7a9435b272cbd4aece3cf8db4be4a8的完整规范化文件为参考，原文件SHA256为bdfcaddd9866b7c1f014b079e7732ac04dc27d28a8e0befc4e1798c437fb4506。400组递归JSON数据逐一比较字节、SHA与UUID；另外覆盖结构对象、时区、集合、负零、Unicode、大整数、枚举、键冲突、NaN/Inf和不支持类型的异常。差分测试不把实现拷贝改写成另一套同样的新逻辑。

第二审直接攻击易被宽泛isinstance快路径误处理的dataclass字符串子类、float枚举负零和普通float负零，跨三个独立PYTHONHASHSEED进程检查集合身份；在三臂真实推理器的六操作支持上比较旧/新完整目标与因子回执、首次采样、恢复、下一次采样和快照字节。沿用已存在的完整缓存伪造、原地tensor.data篡改、模型方法替换、执行绑定重封、q完整伪造与实际采样不一致等审查；合法正路径、拒绝后重试及权限后果一起保留。

真实诊断单独使用固定HFD首个四帧窗口的完整243候选，对三臂原权重分别执行原规范化和新规范化，要求全部target_json/trace_json完全相同，评分不改变请求和RNG。这是3个固定真实比较组合，不补齐第三轮96组合矩阵，也不冒充全数据集等价证明或准确率提升。新旧顺序交替，但与其他矩阵共享本机，计时仅作定位与性能诊断，不是隔离硬件的公平架构速度比较。

所有审查均为A本机自审，B尚未复核。独立人工接触/释放/身份真值、位姿误差校准、自然提议训练及完整具身反馈科学证据仍缺。完整统一研究范围与科学阈值不变。
