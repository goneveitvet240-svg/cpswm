# 第二轮发现后的独立复验

审核对象：`06366579a1a72a46b465194b6e1bc2b6c27a3e87`。旧 `fd516c33532a3fb61a978d0a0c0bd403a3f3794a` 失败报告 REVIEW.md、原 Git 仓库、replacement.bundle 和 results.json 保留不变。

结论：**本轮确认的 P1 Git replace 来源绕过已关闭；第一轮身份覆盖仍关闭。有限范围复验通过，不代表完整统一运行完成。** 没有在本轮覆盖内确认其他未关闭实质缺陷；不作“无其他漏洞”声明。

## 实际执行

- 独立脚本 `retest.py`，输出 `retest_0636657.json`：直接对旧失败原仓库（原 commit 的 replace ref 仍存在）调用新 CLI，以原 SHA 获得原2捕获/4观测，逐文件摘要吻合；另独立构建 journal blob 替换引用，原 SHA 仍获得原2捕获/4观测；合法迟到顺序双捕获仍输出4唯一观测/8文件。
- 使用第一轮碰撞完整仓库再次调用，仍退出1，错误 released reference differs from capture identity；后到坏hash仍退出1，无成功回执，证据 `reject_retest_0636657.json`。
- 独立运行固定归档 `05836759cd96b89011efca71d3e9dc3ab393ce1b:docs/reviews/pc_a/proposal_scheduler_2026-09-13/procthor_run_07`，36捕获/72唯一观测/144输出文件；逐文件 SHA-256 和字节数全部验证。回执 real_archive_retest/receipt.json、独立核对结果 real_archive_manifest_verified.json。仍是既有真实仿真归档回放，无新仿真、无语义模型、无连续行动运行。
- 三份新增测试独立重跑：32 passed / 3.67 秒，见 retest_targeted.xml。命令同旧报告，新增 commit/blob 两项。
- 比较旧新源码：只有上述 CLI 两处禁用替换引用及对应 CLI 测试变化；src/apps/configs 无变化。复验结束 HEAD 仍为所列完整 SHA。

## 来源及签收边界

此修复禁止 Git 常规替换引用改变所指定对象；仍假定 Git 对象存储和执行进程环境受信。工具自己计算的 receipt 摘要仅表达该归档字节的一致性，不能认证摄像头、人物感知、监督合法性或默认推断行为。已有明确 false 标志应继续保留。

前一报告列出的完整目标缺口保持：人物/语义及标定、原生六操作监督、全轴神经模型、明确连续状态与噪声、默认提议—联合后验—条件块—唯一账本—行动反馈。两名审核者对本批有限边界各完成一轮审查与相应修复复验；用户要求的最终完整运行完成后的两轮审核，不能因此宣称已完成。
