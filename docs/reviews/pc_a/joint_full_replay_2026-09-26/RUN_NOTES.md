# 本轮过程记录（尚未最终交付）

基线为 PR39 `1c1281b9d8ed5d535a335149ebfe622150d6bf0f`。开工 `cf4eabd` 已推远端；初版实际实现 `5564370`，后果追踪/测试入口修正 `1aad655e73aa2da54fe557cf31f8ad270e1a469c`。后者生产代码与初版相同，新增独立运行工件与旧负例诊断适配。

开发日志全部保留在 `output/joint-full-replay-20260926/development/`：

- first：合法源重放失败，暴露只靠已消费联合批次不足以覆盖所有七算子产出的有效观测；增加实际源发布时的核心接受绑定。
- second：旧工作区对象不能直接通用 JSON 哈希；改为完整字段字典参与代际归档哈希，类型与原始接受绑定另校验。
- third：首个合法完整重算正例通过。
- four：开发过程中源码被更新，运行中源码绑定明确拒绝；本轮输出不可当固定源码验收。
- five：65 passed / 7 failed。5 个旧负例在捕获故意破坏状态的公开诊断快照时提前被新增源绑定拒绝；1 个环境缺 torch；1 个新增测试读错状态所属对象。旧负例改为捕获原始破坏状态且仍明确断言公开诊断拒绝，没有弱化保护。后续定向 5 passed。
- first formal attempt (`round1`)：专项 5 passed，但回归收集缺 OpenCV、全量 mypy 缺 cv2/mediapipe，整组不通过。补到与前轮一致的锁定环境：`uv sync --frozen --extra dev --extra perception --extra hand-perception`，88 包。
- `round1-retest`：提交钩子因工具一行过长而拒绝后，误启动了尚未冻结的运行；已中断（130），不计审查通过。已补审查入口的源目录干净检查，避免再发生。
- `round1-complete`：固定 `1aad655...`，646 工程回归通过（11 个既有故意坏类型序列化警告），771.90 秒；其余命令仍以最终 manifest 为准。

两轮均 A 本机自审，独立审核者 0。自然监督/校准、正式神经架构与修订内核选择、B 独立复核没有由这些工程结果替代。重算保持七变量/三解析块/七算子的完整研究范围，当前可执行范围是显式 prepared 模型的有效语义源历史；新生纠正来源缺完整推理记录时仍拒绝，不能称全部六操作已经闭合。

第一轮补验：`round1-complete` 的 646 回归、5 专项、受控连续工具、Ruff 均通过；mypy 使用了安装新可选依赖前的缺包缓存，仍报 mediapipe 缺失。模块实际可发现；同源码 `mypy --no-incremental src` 重新检查 364 文件通过。原失败 manifest 保留，补验记录 `round1-recheck.json` 不改写原结果。连续工具实际重算 23 个仍有效源、形成 46 个新记录和 2 个活跃原子；旧 24 记录归档，账本无新增，六维位姿和 23 步历史保持，恢复后受控相机命令 1 次。原源中 11 个有效观测此前没有被联合批次消费，此次补进完整重算，不能把前后统计差全部归因于撤回一个来源。

## 第二轮发现与当前修复（尚未最终通过）

固定 `83d908422281c3188e5f5bc7f8cf79df1386b97c` 首次第二审 `2 failed / 2 passed`：完整替代模型在相同自报 artifact hash 下恢复并发布伪统计；另一失败是审查脚本错误按粒子输出顺序对应 actor，已改按状态中的角色键比对。磁盘失败恢复与新进程恢复原路径通过。

在生产代码未变的 `523c4c7` 上补验，明确复现两个生产问题（`2 failed`）：

1. 重放错误加入未被联合 kernel 实际消费的中间语义结果。原 12 个联合输入撤回一个应剩 11 个，而不是 23 个。P5 的 primary actor posterior 本就进入后续 CIAV prior；改变更新序列不等于补齐漏观测，会引入未注册的更新/重复计算风险。**上文 23 源、46 记录的原始结果保留为失败实验，不沿用其通过字样。**
2. 旧 READY 联合相机命令在纠错后永久留在队列，collector 每次都重试这个已经过时的决定，阻塞下一动作。物理执行前的拒绝仍有效，但没有完成继续运行。

已实现、正做定向复验：绑定实际模型类型、声明方法、源文件字节与已加载代码；按已经接受的粒子父依赖还原实际输入顺序（不信任字典插入顺序），重放其有效子序列；只取消尚未派发且已经过时的联合命令，保留命令/原因，不动未知执行结果。原子失败回滚包含命令状态。测试故障注入改为 profile hook，不再用不同模型类冒充原检查点的模型；模型夹具模块禁止 pytest 重写，以保证实际加载字节码与新 Python 进程一致。

这些修复不证明观测模型/似然/校准本身真实，也不把 prepared 模型变成已选神经六操作模型。修后仍须冻结并重新执行两轮审查。

## 修后固定版的追加反证

`92e1960a815ac6ae0afa2e0a7516ab764628ba25` 的 651 工程回归通过后，手工后果追踪又发现第四项实际问题：实现绑定只检查 produce/checkpoint_state/restore_state，未绑定其调用的类内辅助方法。直接改写同一 `JointFixture.build_statistics.__code__`，类名、原工件摘要与三个入口不变；完整检查点恢复与重放仍发布了两个原子，alpha 达 1101 以上，账本没有新写入。见 `helper-complete-forgery/result.json` 和 `development/helper-complete-forgery.log`，退出码 1。

这不是被提前拦住的命名攻击；完整伪造实际到达原生发布。本版的回归通过不能覆盖该遗漏。待当前冻结运行结束后，将绑定扩展到模型类/继承链的实际 Python 方法及属性访问器，增加回归和第二审完整路径，再冻结复审。可变外部配置/测量真实性仍归原工件与科学校准，不由代码身份自动证明。

`fixed-two-round` 在 92e1960 的原矩阵最终 651 回归 / 5 第一审 / 6 第二审及工具、364 文件 mypy、Ruff 全部通过且源码不变；但追加完整 helper 攻击否决该版本，不能用原矩阵 all_checks_passed 代替范围外反证。已按上述方向修复，新增 3 个辅助方法检查和 1 个完整发布攻击/合法重试，等待新冻结两轮复验。
