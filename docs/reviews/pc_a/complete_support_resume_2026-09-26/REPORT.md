# 完整支持资源缺口接续：双审与固定真实矩阵完成

原PR41第三段固定四帧输入216→783候选形成37,241节点，超过旧32,768节点预算。该原失败案例现已在三网络全部完成，预定四视频×三网络12组合全部通过。此处关闭特定资源及执行绑定缺口，属于A本机工程验证；B未复核，完整自然闭环与科学收益仍未成立。

分支 `codex/pc-a-complete-support-resume-20260926`，base PR42 `3095a2ac112f7934192177a5ef4725b83188e148`，受测生产/测试/工具Python源码 `7c19637791e45dbf28e041beb3b98bc9acd92038`。final-04全部14条命令exit0，总执行器exit0；877个受控Python文件运行前后相同且匹配Git对象。后续提交仅交付文档。原始命令及源码清单见evidence/commands.json和source-before/after.json。

## 实际变化

显式派生65,536节点执行配置；密集默认4,096及原派生默认32,768保持，必须显式指定 `--max-nodes 65536`。注意力每个查询仍读取全部键值，仅减少同批查询行数；以keys×heads×(3×元素字节＋2布尔掩码)×query_rows估算注意力临时张量，预算256MiB。原37,241节点密集四头float32分数张量单独需22,190,273,296字节。保留全部候选、同一全键softmax、原参数字节和1e-5数值容差，没有新增训练。

审查实际发现：合法完整网络在加载后替换自有辅助函数，仍能返回质量1但概率改变的完整分布。最大log因子差0.001152332253。现将实际加载的自有函数、导入别名、评分方法和资源常量纳入结构绑定。旧112/33的局部通过被该反例推翻，未作为最终验收；修后从首审重新执行，详见FINDINGS.md。

## 两轮审查与完整结果

- 第一审及相关回归112通过，保留1项既有畸形夹具警告；覆盖37,241/65,536键独立float64数值参考、全键归一化、最后一个键的实际影响和资源边界。
- 第二审36通过，包含18项与首审重叠的既有谱系测试，不能合计为148个独立新案例。覆盖完整重封的资源配置/历史/权限、已加载实现漂移、非空历史失败及后续随机数，均含合法恢复路径。
- mypy366源文件、Ruff通过。三种原权重303候选/10,246节点密集—分块对照通过：最大memory绝对差1.1921e-6，最大log因子差1.6913e-7，固定atol=rtol=1e-5。此303完整对照不冒充37,241节点密集全图对照。
- 原第三段783候选在旧32,768配置明确拒绝；三臂均保持已有合法快照和下一RNG，同请求合法重试通过，耗时391.1秒。

| 固定视频 | 迟到前→后候选 | 迟到后节点 | 全三网络及恢复 | 实际耗时 |
|---|---:|---:|---|---:|
| 0001 | 108→324 | 15,588 | 通过 | 980.0秒 |
| 0002 | 117→378 | 18,098 | 通过 | 1021.5秒 |
| 0003，原失败 | 216→783 | 37,241 | 通过 | 4004.9秒 |
| 0004 | 189→594 | 28,434 | 通过 | 2099.6秒 |

每段仍使用原固定四帧、1920×960去答案裁剪、全部三种未选择网络及先前夹具权重。在线与恢复后结果/快照相同；前端和语义核心身份不变。新增自然粒子发布、动作和优化器步数均0。完整结果与输入SHA见evidence/MATRIX_RESULTS.json、observed-graph-sizes.json及各视频摘要。

## 性能、证据与后续

查询分块不保证整个进程RSS，也不解决候选枚举和全图计算的长期扩展。实际长耗时明确保留；一秒OS采样另外观察到Python候选序列化与完整性校验，但不是整段性能剖析，详见PERFORMANCE_SCOPE.md。不能以能完成宣称实时可用。

所有成功、早期失败/中断及运行日志已逐文件SHA校验复制到A主仓 `output/complete-support-resume-20260926/closed/`，与此前部分备份分开；完整字节清单见ARTIFACTS.json。原1a094b0收集冲突、65aa8b3完整正路径反例、final-03命令路径拼写中断均保留。

下一轮才推进HFD证据入口：作者9,185,706,983字节训练归档现已匹配MD5；SHA256为d515b35e9c6cc2a9af717395247eda0a914118d368a09d5b00b7b8da9aded5a4。此时仅原始获取完成，337试次验收尚未运行；作者90个成功标签不等于已验收90段视频。需要第二审、必要修复后重做双审、全试次解码/配对与新进程全源重建。

完整H/R/I/C/Z/r/V、三个解析块、七算子及原科学门保持。自然校准/独立接触身份监督、完整自然修订内核及科学对照仍开放；不替用户选择具体神经架构、重采样/焕新内核或自然训练日程。B复核入口见B_HANDOFF.md，独立审核者0，未合并共享集成。
