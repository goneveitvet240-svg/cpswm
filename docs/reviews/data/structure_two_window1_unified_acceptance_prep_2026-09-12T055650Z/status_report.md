# 窗口一统一验收准备交付（2026-09-12）

**本窗口准备任务完成；统一版本为 WAITING_UNIFIED_SOURCE；科学门未通过。** 没有自动合并、推送、签发消融授权或运行统一版本长实验。

## 真实起点与保全

已完整阅读第四轮独立审核，副本保存在 `reviewed_round4.md`。本工作树起点实际为 `2a5f45947952e8d725009141716b42ac879434f3`，分支 `codex/s2-evidence-repair-window1-20260911`，开始时干净。原 `.venv` Python 3.13.5，依赖通过 importlib.metadata 实读，原生环境指纹前后均为 `bba78a7a15b6c7549995038d89482580b105b702aeaa1775b5e5118eb60b563d`。

v0.4、历史报告、P0、回执、checkpoint、已有原始日志与相关历史证据共 **312 个现存文件**逐项读字节并与起点 Git 对象比较，零差异；原文另封存在 `preserved_reviewed_evidence.tar.gz`，312 个成员再与原路径逐项相等。该包 SHA-256 为 `fdc537c72d50588337e6284d4c98abd4ea6da0fd5a6580037db98cca7c88a3f0`。原有 **768 份源码/测试/配置输入未改动**，前后清单为 `source_before.json`、`source_after_final40.json`。本轮仅新增编排工具与对应测试，保留已通过入口、历史覆盖与安全发布实现。

起点旧checkpoint快速当前性通过。新增准备测试改变了 P0 的 test_contract，之后旧checkpoint对当前准备工作树按预期报 `P0 content manifest is not current`。这不推翻旧源码对应的限定审核，也不要求改旧passed或摘要；旧文件原字节保持。本轮没有重签分支工程链。tools 本体不在旧P0 code scope，本轮用独立前后文件清单和执行代码核对绑定，不能拿旧P0为它作传递背书。

## 新入口与拒绝边界

基础编排提交：`6ae8dc2f06ba4a8e16fbac9b6cfe2e6b05fee604`；最终路径边界及测试提交：`ba816cb7c2a885f743069f6b091962b295249cb9`。入口为 `tools/structure_two_unified_acceptance.py`；用法见 `acceptance_entry_usage.md`。

- 文件与模块入口核对实际执行代码对象；拒绝旧unchecked入口缓存、声明root与执行checkout不符，以及不匹配的原生环境，并拒绝运行目录和生成工件的 `..` 逃逸别名。
- 固定既有原生阶段，不接受替代命令、外来通过回执、resume或跳过。五类真实生成、重算、原生工程回执、fresh生成与fresh验证必须分别发生。
- 全输入清单包含源码、tests、配置、锁、静态数据/工件与文档，以及tools本体；不只凭HEAD或顶层字段。每阶段前后检查来源与实际环境。
- 生成/验证后的文件与比较包单独冻结，后续阶段持续比对文件内容与目录成员；输出区被排除于输入清单不等于其包可任意替换。
- 关键矩阵要求真实非空JUnit，failure/error/skip/xfail拒绝；拒绝会偷偷deselect/-k或替换插件的环境。
- 缺少任一必需矩阵文件、重复闭环节点、五类布局缺项、消费者不一致、旧正式输出/日志目录存在均拒绝。窗口三三项新边界的CLOSURE_TESTS目前为空，不能拿现有相似测试或调用者报告补成通过。
- SIGINT/SIGTERM终止整个进程组，失败停止下游并保留原始流。SIGKILL未写终态只可报告INCOMPLETE_RUN；重新执行须新目录从头开始，部分正式产物保留且要求新证据版本。

它是本地命令编排，不是新签名/授权协议。`COMPLETED_REQUIRED_RECOMPUTATION`也只能表示某统一源码下固定命令实际完成，不表达科学门或独立认证。

## 已完成验证

最终 **40 passed，0 failed/skipped/xfail**，35.86秒，exit0。此前38项/22.22秒的记录原样保留；封存前新增两项非规范路径拒绝后重新完整执行40项，不把两轮计数相加。工具与测试执行前后哈希相同。覆盖真实子进程顺序/日志、预置绿色文件、源码与HEAD变化、阶段失败、完整新目录重试、信号及残存后代清理、SIGKILL状态、JUnit空/跳过/失败、重哈希及成员增删、真实CLI root/WAITING/缓存、环境缩减、重复闭环节点，以及真实原生布局五类正例和删除D0负例。

新增工具源码SHA-256为 `cf003e23d7984ba9f616854b146ed9c942fa404e00c36432b8ba27fdb38e8e95`；测试源码为 `7c7f1126151ae81c68670a903b82e48870dbce1197345b04e2a9a0149429077c`。Ruff lint/format、原生冻结离线环境检查及差异检查通过。

真实准备CLI exit0，`stages=[]`，只写WAITING_UNIFIED_SOURCE；声明本分支旧封存输出为新验收输出的负例exit1且`stages=[]`，没有进行统一指定或任何长重算。旧checkpoint当前性负例exit1。准确命令、完整stdout/stderr和退出码见 `commands.md` / `validation/`；最终共享引擎114份fixture文件及47份CLI诊断文件另有归档与逐文件清单，前一轮归档保留。最终准备状态原文与负例状态分别保存在 `preparation_final40_state.json`、`sealed_outputs_rejection_final40_state.json`；它们只记录准备当时的输入，不是之后统一源码的可复用pin。11条已结束命令的22个完整原始流及4个fixture归档已核验，见 final_seal_checks.json。

没有重跑上一轮五类实验、3944项核心或完整fresh checkpoint；第四轮独立限定通过与旧封存记录作为对应旧源码的证据保留。这里的40项不能冒充统一三窗口实验矩阵。

## 交接与尚待条件

`impact_and_integration.md`列出实际窗口二/三快照、变更文件→结果→必需命令，以及重叠和信息权利待决事项。窗口二实际观察HEAD为45850dc；窗口三为281e888加既有未提交工作并继续R5，均是观察时快照，未形成统一来源。

唯一直接源码交叉是W1/W3 `structure_two_production_system.py`：整合需同时保留传递路径/符号链接保护与主干装配/事务改动。不同名文件也通过全src绑定影响所有当前包，不能拼接三树绿色结果。W1新证据目录及所有消费者、原生P0排除项必须在统一源码冻结前一并调整；W2来源guard在独立新进程运行；W3公开低层修订后置条件、已纠正初态重冻结、纠正后语义身份须提供真实新增闭环节点，不能拿旧矩阵计数替代。

后续只有在用户明确指定统一工作树、实际源码及依赖固定后，才运行入口完整固定阶段：W1保护、W2比较归因与伪造、W3正常/异常修订和主干协作；五类真实生成与重算；历史失败重放/跨根；P0/完整原生工程矩阵；新checkpoint强制fresh生成和fresh验证。后续每次输入改变都须重跑受影响层，不自动继承本分支通过。

没有本轮新增的科学决策被擅自选择。原审核列出的地图权限、共同缺省先验、搜索/预算/信息权利及长期记忆任务设计仍由用户决定；本轮准备不以等待它们为由停止工程工作。

未覆盖任意恶意解释器/整个协调器替换、进程内篡改、全部OS/存储竞态、测试语义的自动完备性或独立托管认证。对不可捕获中断/存储故障只承认不完整状态；不宣称CPSWM已穷尽审核，也不将科学门FAIL改成通过。
