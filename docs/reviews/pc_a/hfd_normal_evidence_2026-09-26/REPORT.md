# 完整 HFD 训练证据入口：未完成草稿

当前代码只完成实现、第一轮问题修复及其 21 项复验。**第二轮对抗审查、完整真实归档解码与独立重建尚未运行，本轮不能签收。** 八小时授权结束后未重新启动下载、测试或新实验；只保存既有工件并交接。

## 版本与任务

- 分支 `codex/pc-a-hfd-normal-evidence-20260926`，base PR41 `768b2578c1805ec09f073fd8b6a0369bd28bf91b`。
- 初版源码 `b692ef185590c8f472dff710a95f0cd36b9379d2`，修后源码 `cd687f92a3cf23922d94ac24b4b1f62b75ce85a9`。随后合入本人前轮的仅文档最终结果，不改受测 Python。四个新增 Python 文件与该源码 Git 字节在收尾时完全一致，见 evidence/source-closeout.json。
- 读取作者完整训练分区的 RGB、时间、人工阶段、力传感器和结果标签；按全部试次核验并隔离无效数据，不只挑成功案例。代码不接入在线模型、原生记忆或动作权限。

## 已完成代码与局部验证

新增完整归档摘要核查、成员白名单与全覆盖验证、视频实际逐帧解码、标签/时钟/力数组检查、来源重建验证和原子接受清单。完整来源/配对依据来自已登记元数据，不接受调用者自行声明的证明。保留无效试次及分母；所有标签保持单作者标注层级。

开发夹具 17 passed；首审注入四个失败：额外文件出现后仍留下清单、构建过程中原始标签漂移、验证过程中标签漂移、符号链接指向包外。修后检查成员字节及文件身份、拒绝别名、最后才写入接受清单；17 原测试＋4 首审共 **21 passed in 0.80s**。初版 mypy 单文件检查通过不能冒充修后全仓类型验证；修后第二轮及全仓检查均未完成。修前 4 failed 原始日志完整保留。

测试使用显式隔离的两个试次小夹具，测试时替换可信输入根；这验证算法行为，不能替代作者真实归档或外部根托管。没有把 21 项夹具通过写成真实训练证据已接入。

## 真实数据状态

既有已核验的作者训练标签包列出 337 试次，其中 outcome=0 共 90（人→机 35、机→人 55）。这只是标签目录统计。作者完整训练归档应为 9,185,706,983 字节、MD5 `14232fcd1b34030b77db48d8c771710d`。

下载在字节段 6,241,124,352–6,257,901,568 经过重试后因 SSL unexpected EOF 失败，保留 373 个完整长度分片、6,257,901,568 字节。分片尚未获得全包 MD5 验证，不能称为已取得可用数据。此前顺序下载的约47MiB片段也保留且未接受。**新增已验收真实试次 0，完整训练包不完整，未真实解码337试次。** 原始文件位于主仓 output/datasets/hfd-full-training-20260926/，未上传 Git。

原作者记录：[Zenodo 10708763](https://zenodo.org/records/10708763)。该来源及训练标签此前已读取，当前仅整理既有下载结果。单人阶段标注不等于独立双人复核、精确接触/释放误差界、全局人物身份或本项目前端位姿误差校准。

## 下轮接续门

1. 先处理 PR41 第三段真实视频资源失败；保留全部支持与事先数值门。不能直接靠删候选或提高科学阈值掩盖。
2. 本草稿必须完成第二轮不同攻击：完整重封后的下游声明、来源/成员错配、正常与隔离路径、后写漂移及失败后的安全重试；保存原始正路径和后果。第一轮复跑不算第二轮。
3. 有新执行授权后可恢复已有分片下载，整包匹配作者 MD5 后才解码全部337试次；独立进程从原始归档重新构建核验。真实兼容性或新源码修复须重新冻结并完成双审。
4. 保留精确释放/身份、运行前端校准、六操作活跃后验核、自然训练日程、真实纠正后 Unity 反馈以及公平强基线/B复核缺口。该入口不替用户选择模型、重采样、先验或训练日程。

A 自审，独立审核者 0，B 未签收。完整统一框架保持；新增训练、自然发布和自然动作均 0。不得合并本草稿作为已通过证据入口。
