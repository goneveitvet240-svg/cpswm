# HFD完整训练证据接续：双审、337试次及全源重建完成

此前只有两段失败交接资料和337项作者标签目录；本轮取得并接入完整训练归档，实际核验337试次、141,462帧，其中90试次为作者成功交接。新的独立进程重新读取原归档、逐帧解码并重建标签/清单，结果完全一致。此处完成来源与结构验收，不把作者标签提升为精确接触、人物身份或本项目前端校准真值。

## 版本及顺序

分支 `codex/pc-a-complete-hfd-resume-20260926`，base PR43 `f72413fdd55bb3324ca064c02ba1fd8507aba54b`，受测Python源码 `c10a311430ff68ecce4aea0eaa6b3039e2f88974`。上一资源轮完整12组合封存后才启动本轮。final-02先顺序双审，再全源验收；全部6条命令及总执行器exit0，879个受控Python文件前后与Git一致，后续提交仅文档。新进程是同实现的重新计算，不冒充另一审核人的独立实现。

## 对抗审查、修复与失败保留

- 修前固定eb64e65287660f5366ac7d48e6cd42c33cfccfe6在能够创建本地socket的环境实际12失败/7通过。发现特殊文件漏检、末次内容检查后漂移仍接受、复数时间丢虚部、无符号时钟倒退错判，以及不合法类型绕过隔离。修复后从第一审重新执行。
- 第一审32通过（原21包/漂移案例＋11公共交接证据回归）；第二审19通过。后者包含完整重封的隔离提升、不同有效视频互换、所有科学权限提升及合法原包恢复，另覆盖数值、真实解码、重复JSON键和失败重试。mypy366/Ruff通过。
- 首次Unix socket绝对路径过长、短路径在沙箱内EPERM、final-01因此停止的结果全部单列，不当作产品漏洞或通过。没有为通过而跳过该测试；允许创建该本地临时文件后，修前与修后均实际执行。详见FINDINGS.md与原始日志。

修复以lstat拒绝特殊文件/别名，最终内容哈希后复查文件身份清单；实数dtype与直接相邻比较避免隐式数值损失/无符号回绕。坏数据仍保留原文件、完整分母并隔离，不对作者数组做修正。合法源恢复/新目录重试可用。该合同不认证恶意操作系统，也不保证返回之后外部任意改写；使用前应再次从源核验。

## 真实资料结果

来源为[作者Zenodo记录10708763](https://zenodo.org/records/10708763)，CC BY 4.0，仅training分区。原归档9,185,706,983字节，MD5 `14232fcd1b34030b77db48d8c771710d`；全包SHA256 `d515b35e9c6cc2a9af717395247eda0a914118d368a09d5b00b7b8da9aded5a4`。训练标签、记录、类别表、datasheet均按原登记SHA核验，未改旧来源根以迁就数据。

| 方向 | 成功 | 未接近 | 未释放／未抓握 | 掉落 | 全分母 |
|---|---:|---:|---:|---:|---:|
| 人→机 | 35 | 34 | 45（未释放） | 55 | 169 |
| 机→人 | 55 | 35 | 34（未抓握） | 44 | 168 |
| 合计 | 90 | 69 | 79 | 99 | 337 |

全部337试次接受，0隔离；141,462帧均实际解码为640×480×3，与作者逐帧标签/时间长度一致；610,420条原始腕力记录、956个阶段边界保留。机器人为276试次Toyota HSR、61试次Kinova Gen3。2,696个原始成员共2,366,082,001字节；337份隔离evaluator行共43,117,642字节。每段完整8模态保留，未只选择成功样本或容易窗口。

首次全源接入84.80秒；新进程从同一原归档重新核验84.89秒。清单SHA256 `e4362769c9f44f02b5a6e055897ef487322b832101a79018034f2d9d8fbe6502`。结果及来源摘要见evidence/REAL_DATA_SUMMARY.json，实际命令和双重源码清单见evidence/。

## 已知质量限制与权限

77试次共89帧在原始力时间范围之外；98,625条原始全零力记录、23,987条重采样全零力记录原样保留。范围外帧不是已校准测量；不把这些数目本身解释成接触失败或传感器坏点。Toyota HSR记录为作者腕部力矩传感器，Kinova为关节力矩推算腕力，不能视为相同真值来源。

作者阶段仍是单人外部标注、有边界歧义；阶段间采样括号不是人工标注误差界。现接入包没有授予全局人物身份、对象六维位姿、本前端位姿校准或精确接触/释放资格。视频已实际解码，但未完成全像素答案文字泄漏的人工审查，也未投入在线网络。作者标签仅在evaluator；新增优化步数、自然发布和动作0；完整提议训练就绪False。没有访问作者validation/test，当前337试次全部属于已读取的作者training开发资料。

全部成功/失败、原始模态和evaluator工件已逐文件SHA校验备份主仓 `output/complete-hfd-resume-20260926/closed/`：3,052文件、2,410,627,477字节，见ARTIFACTS.json。9.2GB完整作者归档与下载失败分片另外保留在原dataset目录，不重复放入Git。B复核命令、平台要求与资料依赖见COMMANDS.md；两轮均A自审，独立审核者0、B未复核，草稿PR44不合并共享集成。

## 主要任务的下一依赖

现在有真实成功、未释放/未抓握、未接近及掉落的完整开发资料，可用于明确边界的阶段/结果监督与前端诊断；不能直接充当完整六操作目标或统一后验的自然校准。下一主项应将这些可用监督与运行像素、角色/接触未决性及当前提议损失逐项对齐，同时验证自然视觉质量和原生修订/行动消费者。具体神经模型、自然训练日程、重采样/焕新内核与新科学指标仍留给项目所有者，完整H/R/I/C/Z/r/V、三解析块、七算子不缩改。资源轮另已定位逐前缀整组序列化的性能成本，后续优化须保留完整分布与篡改/失败后果检查。
