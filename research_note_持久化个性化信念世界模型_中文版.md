# 面向长期具身智能体的持久化个性化信念世界模型

**逄玮 — 青岛大学**
**面向远程科研合作的一页研究简介**
**邮箱：** [your email] · **GitHub：** [project link]

## 研究动机

长期运行的具身智能体所需要的，不只是检索相关历史观测的能力。它还必须显式地维护：自己当前对一个不断变化的环境**相信什么**、**为什么这样相信**、**哪些部分仍不确定**，以及新的观测或它自身的行动**应当如何修正这一信念**。*Enter the Mind Palace* 已经证明，基于多情景场景图的世界实例，可以通过定向记忆检索与引导式探索，支撑长期主动具身问答。本项目研究一个与之互补的问题：**分散在各个情景中的证据，如何被转化为一个持久的、具备不确定性表达能力的、持续更新的信念状态？**

目标场景是一个在家庭环境中跨天甚至跨月运行的机器人。在这一场景下：观测可能在其所描述的事件发生之后才到达；外观相似的物体可能被混淆；**没有检测到某个物体**与**确认该物体不存在**是两回事；家庭生活习惯可能发生改变；机器人自身也会在完成任务的过程中移动物体。一个有用的世界模型必须**表达**这些歧义，而不是把它们悄无声息地坍缩成单一的场景快照。

## 核心研究假设

本项目检验以下假设：在**感知条件与观测预算相匹配**的前提下，一个**事件溯源式的概率信念投影（event-sourced probabilistic belief projection）**，能否在正确实例检索、主动搜索效率、以及对环境变化的适应能力上，超越情景检索（episodic retrieval）或最新快照记忆（latest-snapshot memory）。

形式化地，系统维护一个派生信念状态：

\[
B_t = P(S_t \mid E_{\leq t}, A_{\leq t}, H_{\leq t}),
\]

其中 \(E\) 表示带时间戳的证据，\(A\) 表示机器人行动，\(H\) 表示与人物和生活规律相关的上下文。该信念状态是一个**可重建的派生投影**，而非独立的事实源。

## 拟采用的技术路线

1. **事件溯源式证据层。** 追加式（append-only）的 `RelationAssertion`（关系主张）与 `EventRecord`（事件记录）对象，保留 `valid_time`（有效时间）、`observed_time`（观测时间）与 `recorded_time`（记录时间），并同时保存证据谱系（provenance）、来源类型与证据可靠度。

2. **多假设信念投影。** 一个派生投影维护关于物体身份、位置、归属、状态与关系的多个相互竞争的后验假设。**证据可靠度（evidence reliability）与后验概率（posterior probability）严格区分。**

3. **观测似然与负证据。** 一个共享的 \(p(z\mid s,a)\) 模型，同时支撑两件事：检测失败后的信念更新，以及主动搜索所需的信息增益估计。

4. **个性化转移先验。** 以人物、活动、时间与上下文为条件的生活规律模型提供先验，但不覆盖当前观测。变化检测负责区分稳定习惯、一次性异常与习惯漂移。

5. **具身更新闭环。** 结构化查询 → 信念检索 → 主动搜索 → 观测或任务执行 → 证据写回 → 信念重投影与重规划。

## 首个限定范围的研究

第一项研究将在长时程合成家庭生活规律上，评测**正确实例物体检索**。一个符号式仿真器将生成可复现的事件历史与部分观测，并可控地注入：延迟报告、遮挡、漏检、相似物体歧义、客人行为，以及突变或渐变的生活规律变化。

基线将包括：最新观测（latest observation）、类别级记忆（category-level memory）、情景场景图检索（episodic scene-graph retrieval），以及完整的事件溯源式信念模型。评价将报告：正确实例成功率、Recall@k、搜索代价、错误物体率、状态 NLL 与校准度、生活规律改变后的适应延迟，以及下游行动效用。消融实验将分别隔离以下因素的作用：负证据、三时间语义、个性化先验，以及多假设投影。

## 当前进展与合作范围

本项目目前已具备完整的模块化架构，以及一个**可执行的 Python schema / 契约层**，覆盖：时态主张、事件记录、概率信念快照、观测似然、证据谱系、查询预算、纠正权限，以及真值隔离。当前契约测试套件包含 **33 个通过的测试**。下一个纵向切片是：符号式仿真器、追加式关系/事件存储、派生信念更新，以及一个可复现的端到端检索实验。

我正在寻求围绕这项限定范围研究的**远程科研合作**。我可以承担实现、实验、文档撰写与异步协调的主要责任。合作可以聚焦于：打磨问题的形式化表述、选取公平的长期记忆与主动具身问答（active-EQA）基线，以及设计一套能够把**持久信念维护**与**主动具身行为**联系起来的评价方案。如果双方均有兴趣且能形成实质性贡献，这项研究可以发展为一个联合研究项目。

**相关起点文献：** Ginting et al., [*Enter the Mind Palace: Reasoning and Planning for Long-term Active Embodied Question Answering*](https://proceedings.mlr.press/v305/ginting25a.html), CoRL 2025.
