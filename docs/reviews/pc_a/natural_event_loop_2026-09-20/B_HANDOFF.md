# B：固定源码复核请求

A分支 `codex/pc-a-natural-event-loop-20260920`，base `769a558bc3deb2c4133f509722a748fa37bdc990`，
待复核代码 `288b429a7d1e1d7f22f282ded333eedcf41957bc`。叠加PR28，不改变共享集成。
本文件是复核交接，不代表B已收到、开始或通过；A没有修改STATUS_B和B专属实现。

1. 独立检出固定SHA并按COMMANDS重建环境，不复制macOS虚拟环境。
2. 核验原始／作者标签／下载回执成员配对、缺失手-1哨兵和窗口选择；
   事件真值缺失必须保留，不能用物体位移生成“抓取／释放真值”。
3. 核验MediaPipe模型SHA，原始RGB生产与评价标签分離；检测/左右手分数不可进入角色后验。
4. 重跑154项工程案例及修前13项反例在最终版的拒绝与全状态不变行为；测试原始前缀和每帧模型绑定。
5. 使用真实模型重建重跑六帧SQLite中断／完整运行比较。Windows模型依赖与差异单列，不把本机结果视为跨机结果。
6. 独立检查固定360帧预测与作者关键点评价。初始360帧运行绑定c8fe8d5；最终288b429只新增恢复修复，
   不能把两次实验拼成同次自然P5。压缩逐帧产物可解压并与summary内原始SHA复核。

请按AGENTS更新自己的STATUS_B，记录源码SHA、环境、命令/通过/失败/跳过、原始证据及结论，
通过独立B分支/PR交接A。完整自然语义、角色/位姿误差、同次反证/行动/恢复和科学比较仍未交付，
本轮仅申请组件复核，不能关闭五任务最终验收。
