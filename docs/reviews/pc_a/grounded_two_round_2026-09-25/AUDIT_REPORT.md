# PR34 二轮对抗审核（2026-09-25）

按用户要求，先完成两轮本机审核，再启动后续连接实现。两轮分别绑定版本，并非两位独立审核者；B 跨机复核仍未完成。

首轮基线：`baa37172c35f442419ae3aea048060d01c9101c2`。新反例首次运行 **3 failed / 1 passed**，合法正例保留。发现三个 P2 问题：

1. **标注时序错绑**：程序起始读取 A 标注，结束时重新读取路径计算哈希；中途换成 B 后输出错误绑定 B。修复为固定实际读取的字节及其哈希，并在发布结果前拒绝发生改变的标注。
2. **跨文件像素错绑**：仅分别比较标注中的结果文件/像素哈希，未验证原结果的逐帧像素引用。替换像素并重写对应标注哈希后，仍可声称来自未改变的原归档。修复为核对原归档的逐帧 payload 摘要、顺序和完整覆盖。
3. **截去失败尾部**：标注行删到前缀后仍能导出片段结果，易把失败尾部漏掉。修复为要求全片段帧数和归档闭合；本工具未声明支持任意子采样，因此不改变研究协议。

另补输出中的源码文件哈希、运行依赖版本及编码说明。RGB 数组字节摘要与原 NPY payload 摘要分别记录。以上只建立本地依赖一致性，不赋予外部真实性或物理语义权限。

修后固定源码 `1e6e0373248d3881be67f00922cc7c330b6f1bba`，首轮七项修复/原跟踪用例通过。第二轮在该固定版本扩展路径逃逸、空输入、重复片段、顺序错乱、布尔索引、后续标签干预，以及**全部字段和哈希自洽的完整伪造正路径**。后者可得到像素测量，但仍无法取得接触校准、世界身份/位姿或自然语义记忆权限；未把自洽哈希当独立信任。

第二轮 **114 项定向/相邻测试通过，350 个源码 mypy 通过**。真实 64 帧重跑留存量仍为 17/17、15/15、3/17、14/15；三个受控种子实际各撤销 1 条，恢复/从头语义重放一致，动作读出变化仍为 false/true/true。全部复跑 source_unchanged=true。

没有新增阻断发现，三个首轮问题在该版本关闭。范围仍是 PR34 新增的跟踪/受控比较及相邻连接；不覆盖完整统一框架的所有伪造链、全部账本生命周期或真实动作。默认联合粒子生产缺口及自然语义/强基线缺口继续保留，不宣布整体验收。

原始失败和修后日志、第二轮日志及结构化结果见本目录。后续新连接另有源码 SHA 与验证记录，不能继承本报告的固定源码审核。
