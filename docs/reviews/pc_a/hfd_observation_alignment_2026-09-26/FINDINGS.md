# 实际反例和修复

本轮仅A本机两次不同审视，不冒充独立B或独立人工标签。真实执行源码 `1c180cfdecc35bed0233d89646201398c5b1217d`；最终双审 `dc2c23183d244a443fae05494912b6cd7619d81a`。两者生产、tools和tests逐字相同，唯一Python差异是加强第二审脚本。

| 问题 | 实际后果 | 修复与覆盖 |
|---|---|---|
| 重复视频与时钟被两个作者试次引用 | 初始构造覆盖同名帧并输出重复index；运行入口才拒绝。初始首审13失败多由该共同夹具问题触发，不能算13种生产漏洞。 | 导出前拒绝重复传感器来源，不生成重复训练样本；正常夹具改为不同传感器时钟。旧21135ff独立目录复现不抛错，修后抛错。 |
| 作者结果排序影响运行导入时间 | 初版按全批记录数递增时间；置换结果导致相同像素的capture/arrival发生变化，可能形成标签侧信道。 | 各序列独立导入编号，runtime索引按无答案的传感器来源键排序；置换结果后运行字节相同，仅评价侧和父来源摘要不同。旧21135ff对时钟单独比较确实失败。 |
| 元数据默认随机record_id | 固定相同输入和导入时刻，合法新进程重建仍无法逐字一致。final-01首审实报envelope不一致。 | record_id从原帧观测身份确定性派生，final-02合法重建通过。 |

其他未通过记录也保留：final-01的Infinity攻击在构造攻击摘要时先被JSON拒绝，是测试安排错误；修为向入口提交包含Infinity的坏回执后检查拒绝。初次旧版本复现实验由仓库conftest重新绑定当前源码，未证明旧行为；随后提前导入旧模块又被bootstrap拒绝。最终使用旧目录自己的正式bootstrap，再以 `--noconftest` 避免第二次导入另一个工作目录的bootstrap；日志明确打印并断言旧模块路径，两个目标反例均失败。该隔离命令仅用于旧版本反证，不用于正式验收；全部失败/配置日志保留，不混作最终通过。

`before-regressions-isolated.log`还有pytest试图在/dev创建缓存的两条警告；反例断言本身已执行。首轮正式79通过只有既有runtime候选伪造夹具的Pydantic序列化警告。第二轮14通过，含合法恢复、全量摘要重封、标签/像素/索引/时钟交叉配对、权限升级、遗漏、附加内容、硬链接/符号链接/FIFO和末次读取漂移。审查不覆盖任意OS攻击者修改进程内存或外部标注真实性。

交付前补充：最初第二审只重算文件清单摘要，尚不足以证明像素/回执/索引全部内部一致，因此未用它宣称最终完整伪造覆盖。加强版先修复攻击包所有内部链接并以自签摘要通过6帧结构准入，再由原归档校验拒绝。c9a4fb9上时钟攻击因浮点差值未重算而提前拒绝（1失败/13通过），属攻击构造不完整；最后同步改变全序列时钟、评价时刻和区间，重算相对秒与全部摘要，在dc2c231上首轮138、二审14、mypy367/Ruff通过。完整前后状态与失败日志均保存。
