# 手物观测进入三类提议网络：第二轮局部闭合

base PR47 `74db008222979e4ae508a7c6fded524ae62e19ae`；分支 `codex/pc-a-hand-proposal-input-20260926`；实际受测源码 `b55867d0138c2af2c39175cba18123d99c8bd60b`。A两次自审，B未复核，不合并集成。首审224、第二审44（10新项、34前轮回归）、mypy368/Ruff通过；9条命令及总执行器exit0，898个Python逐条前后同Git。

原始手检测与视觉帧严格连接。模型现在实际接收二维关键点、所有区域候选、手物距离/框内点数、未决人物归属、人物对身份两种解释和条件角色；关联与几何均从同一可见前缀重算。actor/instance键与既有支持共享相同标识。手部没运行与运行后没检出不同，图像外关键点保留未知归属。来源哈希、文件名、模型版本和作者答案未进入神经特征，来源依据另存。

使用第一轮原归档重建且外部固定摘要的原输入，本轮128帧/32窗全部完成：378区域手候选、2,530手物测量、34未决人物对进入提议模型输入，8,012完整离散语法候选无截断。手候选可能区域重复，不是378只独立物理手。64角色仍为条件假设，不是识别人际交接成功。

实际网络矩阵为8试次首窗前2帧 × 三种原网络 = 24组合；16唯一原帧、50区域手候选，每臂完整候选296个。固定同一目标集合及所有UUID，对比原手、去手、x加1像素。24/24去手与位移对照都有非零概率差异，完整质量归一、恢复及下一次采样24/24完全相同。三臂权重来自既有微型夹具训练，新增优化步骤0、架构未选择。

概率变化范围（每例最大候选概率差）：

|网络|去手|x+1像素|
|---|---:|---:|
|类型因子图 Transformer|6.70e-5–1.81e-4|7.20e-9–2.19e-7|
|槽条件 Perceiver|5.61e-8–1.60e-7|2.14e-9–5.18e-9|
|自回归类型图策略|1.02e-6–4.91e-6|7.52e-9–9.00e-8|

这些数值只证明代码存在输入响应，量级很小；不证明有效理解手物关系、准确率、校准或任务收益。两帧只能覆盖短输入消费，尚未验证四帧完整交接候选的模型分布。下一轮扩大到全部既定32窗的四帧评分，并对各试次首窗执行采样恢复，其余窗分别记录评分/状态检查，不冒称都完成采样恢复。

完整伪造但内部一致的手坐标可生成q并恢复，仍无语义权限；同一请求换输入、局部重封与嵌套强制篡改均被拒绝且可合法重试。自然前端实际core/snapshot/ledger保持、execution traces0；新自然发布、记忆、动作0。自然GroundedTransition、可靠接触/释放/人物身份与位姿误差模型仍缺，不把本轮称为完整自然闭环。

136文件、28,101,313字节完整本轮产物已逐文件校验备份主仓 `output/hand-proposal-input-20260926/closed/`。原帧来源仍在 `output/person-ambiguity-20260926/closed/final-02/aligned/runtime`，固定pin为 `77f02b3dbf093243033f232ce4721d54f1ca35189b1a963d6552b92dd199279f`。本轮前端结果摘要 `677505fa2891aab0b2602a5ec002ea3fb3612d5e219ad788437b5b03feaf3c48`。
