# 本批自然数据与完整闭环依赖

核对日期：2026-09-20。原始输入、模型输出、作者监督和受控机制分开保存。

## 已取得的自然输入

官方 HO-Cap：<https://irvlutd.github.io/HOCap/>；官方工具仓库读取版本
`576c63ebf3b84dfec8744ba0f021234213bf0dab`。

在三个既有 subject_5 序列的前60帧之外，本轮从作者原始档案补取每序列中段与末段各60帧。
窗口只依赖 meta.yaml 的 num_frames，不根据模型正确率或作者姿态挑选；全部是开发数据，
不是新的独立确认集。原始JPEG/PNG与作者NPZ／姿态数组放在不同目录；每个成员保留来源URL、
ZIP CRC与SHA256。未下载完整档案，不能声称验证整包。

| 序列 | 完整序列帧数 | 已有窗口 | 新增窗口（均含端点） |
|---|---:|---|---|
| 112303 | 702 | 0–59 | 321–380、642–701 |
| 113202 | 791 | 0–59 | 365–424、731–790 |
| 113535 | 854 | 0–59 | 397–456、794–853 |

三序列共540帧、900个有可见关键点的作者手部实例、2160个物体姿态标签。
这些计数是监督可用性，不是模型召回率。标注是作者半自动方法产物，不能宣称无误的人类金标准。
只有单人和左右手；task_id=2的双手传递不能作为两个人交接。
NPZ未提供pickup/move/release/interperson-handoff/correction事件标签。
物体姿态的端点位移和关节接近只可作为诊断量，不能伪造独立事件标签。
公开图像抽查321/365帧有自然手物体外观及背景标记，未把背景标记或作者姿态送入模型。

## 手部像素生产

MediaPipe官方Hand Landmarker版本1模型：
<https://storage.googleapis.com/mediapipe-models/hand_landmarker/hand_landmarker/float16/1/hand_landmarker.task>

SHA256 `fbc2a30080c3c557093b5ddfc334698132eb341044ccee322ccf8bcf3607cde1`；
依赖锁MediaPipe 0.10.35。CPU delegate、IMAGE模式逐帧运行，最多4个手部候选、官方0.5检测与presence默认值。
这些只是开发前端设置，不是科学阈值或角色校准。输入只有经来源验证的RGB像素。
输出只保留21个图像关键点、左右手分数及来源，不把model-local world landmarks当相机／世界位姿。
同一ContinuousEvidenceInput保存物体与手部候选，未经校准时仍返回INSUFFICIENT_SEMANTIC_EVIDENCE。

## 仍未取得／未实现的依赖

1. **真实多人角色、接触/释放与纠正监督**：HOH官方页
   <https://tars-home.github.io/hohdataset/> 明确完整数据需作者密码；网页列有抓取、传递和释放时间标签。
   本轮没有完整数据，没有绕过访问，也未代发申请邮件。
2. **CORE4D候选补充**：<https://github.com/leolyliu/CORE4D-Instructions> 与
   <https://raw.githubusercontent.com/leolyliu/CORE4D-Instructions/main/docs/file_definitions.md>
   可读，方向性pass1/pass2为序列动作标注，不能自动当帧级接触／释放。官方HF目录可通过网页核查，
   本机对 `https://huggingface.co/api/datasets/leolyliu/CORE4D/tree/main/CORE4D_Real`
   30秒请求超时，未取得新增原始视频／标签。不要把网页列表当已下载数据。
3. **自然实例与完整位置+朝向估计**：现有类别框、表面点、手部关键点都不是稳定物体实例和完整6D物体位姿。
   需要像素估计器、合法物体模型/参考系、独立误差拟合与开发外验证。不得用作者obj_poses填充运行输入。
4. **接触／角色概率和观察机会模型**：检测与左右手分数不等于抓取者／放置者责任概率；缺少相应监督和校准。
5. **完整P5工厂和全轴提议模型**：合同／损失／解码组件已经存在，但合法运行支持生成、全轴训练模型与完整运行工厂仍缺。
   缺口不全是数据访问；尚需实现与实验验证，不能重命名旧原因阶段MLP或接入oracle替身。
6. **可执行行动环境**：需要明确可控制的环境和返回新观测的接口。离线视频只能回放，不能响应机器人决策；
   对外部视频执行假动作或把后续既有帧当干预结果不满足要求。已询问用户可接入的仿真／实机环境。

## 五项任务签收规则

- 本批数据补充和手部组件交付不签收任务1的完整多人／纠正覆盖，也不签收任务2的完整语义生产。
- 任务3必须由自然证据生成合法GroundedTransition，驱动全轴后验、三解析块、唯一账本及真实动作。
- 任务4必须同次运行收到反证、撤销贡献、改变并执行下一动作，并与完整重跑及恢复对照。
- 本批两轮独立审核仅是已改组件审核；完整五任务的最终同源码审核和B验收继续保持未完成。
