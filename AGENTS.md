# CPSWM 双机协作规则

用户于 2026-09-12 要求：本机与另一台 Windows 电脑、两个 Codex 账号共同推进项目；GitHub 为最新共享进度依据。

1. 开工先执行 `git fetch origin --prune`，读取远端交接/进度文件和任务对应分支的完整 commit SHA（提交哈希），再读该版本代码。联网失败时标明“远端未验证”，不能把本地缓存称为最新。
2. 仓库为 https://github.com/goneveitvet240-svg/cpswm 。当前双机集成分支为 `codex/dual-pc-handoff-20260912`；默认 `main` 和旧 checkpoint 分支不是当前集成进度。更换集成分支必须在 GitHub 的协作文件中留下明确记录。
3. 先读 `docs/collaboration/START_HERE.md`、`TASK_BOARD.md`、`STATUS_A.md`、`STATUS_B.md`，再读 `docs/结构二/README.md` 和对应最新审计/修复报告。历史报告不能覆盖更新且绑定实际源码的结果。
4. 电脑 A（原 macOS）负责主干/七算子修复与集成，电脑 B（新 Windows）负责独立复现、比较协议与验收。具体边界见任务板。两个账号必须各自拥有 GitHub 仓库权限；Codex 账号不自动赋予 GitHub 权限。
5. 每项任务使用独立 `codex/pc-a-*` 或 `codex/pc-b-*` 分支及工作目录。不要两台机器直接在共享集成分支并行写代码；不要覆盖、丢弃或自动合并另一方未完成修改。旧活动窗口也须先查状态，避免重复开工。
6. 开工/交付更新本人 STATUS 文件，写明任务、分支、base SHA、实际代码 SHA、测试命令/结果、证据位置、阻塞和下一步，并推送到 GitHub。未推送的工作只能标为本地进行中。电脑 A 汇总任务板，电脑 B 不同时修改该文件。
7. 跨机器通过 GitHub 分支及 Pull Request（拉取请求）交接；审查绑定具体 SHA。新提交使旧审查过期。A 集成 B 的已审查成果，B 复核 A 的生产修复；接收方记录审查依据后由 A 集成。禁止 force push（强制推送）覆盖共享分支。
8. 传输包只是某一时刻的完整备份，不能覆盖从 GitHub 新获取的工作树。归档中的 macOS 虚拟环境、绝对路径、工作树 .git 指针不可直接用于 Windows，须按交接说明重新克隆并重建环境。
9. 保留完整统一研究框架，隐藏事件、多人物、开放世界、可逆归因、具身反馈及 H/R/I/C/Z/r/V、三个 RB blocks、七算子范围均不擅自缩小。既有用户科学决策保持有效；新科研路线、权限/先验或指标选择交用户决定。
10. 工程测试通过、局部诊断、独立复核、统一验收、科学收益分开记录。不得因负例/命名攻击通过而宣称全面验收；覆盖必须含合法正路径、完整伪造路径、依赖/状态机、账本与动作后果。尚未完成的矩阵明确标部分覆盖。
