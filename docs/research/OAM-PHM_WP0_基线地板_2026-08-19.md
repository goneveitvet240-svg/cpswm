# OAM-PHM WP0：基线地板已建立

日期：2026-08-19
工作包：**WP0**（`OAM-PHM §14`：O-STaR、STREAK、HOMER 等最强邻近基线的忠实适配和死亡测试，主要模块 M31/M32）
状态：`floor_established / external_replication_contract_frozen_partial`
（**不是** `§9.1 comparison complete`）

2026-08-26 协议硬化更新：baseline adapter（基线适配器）、内容绑定的
tuning/evaluation seal、头部构造类型、F0 精确双射 / B1-D2 时变多对多身份契约、
簇绑定查询网格与显式计算单位已进入代码和回归测试。当前 F0 没有不相交的
evaluation dataset，因此 runner **不签发正式独立调参协议**。四项外部复现仍保持
`external_reimplementation_required`，因此状态仍是 `floor_established`，不构成方法证据。

2026-08-26 外部证据门更新：O-STaR 公式级 reference core（参考核心）、O-STaR/STREAK
机器可检查 fidelity manifest（忠实度清单）、正式比赛 fail-closed gate（失败关闭门）、
顺路观察 paired intervention（配对干预）和抗污染 matched ablation（匹配消融）协议已经建立。
这只关闭了规则与证据格式，不代表两个外部方法、完整 OAM-PHM 或任何效用主张已经完成；详见
`OAM-PHM_WP0_外部复现与效用证据门_2026-08-26.md`。

---

## 0. 为什么先做 WP0

F0 复审门当前是 `BLOCK / review_blockers_only`，明文暂停的是**多人物、访客污染、交接和变化检测扩展**——对应 WP3、WP5 和部分 WP4。

**WP0 不在暂停清单里**，而且它是编号第 0 的工作包。更重要的是，不做它，OAM-PHM 自己的两个门槛都无法判定：

- `§12.1` 完成条件 8：*"相对独立调优的 O-STaR 及强组合基线产生行动层或效用层收益"*
- `§12.2` 停止条件 1：*"强组合基线经过独立调优后已经匹配候选方法"*

**没有基线，你既不能宣布成功，也不能宣布该停。**

顺带说明：这同时补上了结构三 §13.4 那个洞（11 条基线全是内部消融）。WP0 的协议可以直接复用过去。

---

## 1. 交付物

| 文件 | 内容 |
|---|---|
| `src/cpswm/system/evaluation_operations/oam_phm_baselines.py` | 契约 + 3 个可运行基线 + §9.1 注册表 + adapter / tuning / identity / 双头评分 / 查询网格 / 预算 harness |
| `tests/test_oam_phm_wp0_baselines.py` | **28 个测试** |
| `apps/evaluation_runner/run_oam_phm_wp0_baselines.py` | 可复现产物入口，默认 1 小时查询网格，带 provenance 和性能预算绑定 |

2026-08-26 定向回归：**28 passed**。上一次全量回归结果为：
**2203 passed, 1 xfailed, 6 failed**；6 项中 5 项是工作区既有 Project Two
`source_evidence_maturity` 失败现已在定向复核中消失，P0 content manifest 当时仍失败。
由于共享工作树继续漂移，本文不把旧全量数字写成当前全仓结论。

---

## 2. 地板数字（F0 固定样例，书桌 → 沙发异常 → 书桌）

查询设计：每次放置后 1 小时问"它在哪"，而检测历史严格早于查询时刻——**基线必须预测，不能读答案**。

| 基线 | top-1 | NLL | n |
|---|---:|---:|---:|
| `oam-phm-last-seen@0.1` | **0.333** | 5.0674 | 3 |
| `oam-phm-household-frequency@0.1` | **0.667** | 2.7648 | 3 |
| `oam-phm-markov-transition@0.1` | **0.333** | 5.0674 | 3 |

两次运行字节一致，`sha256(artifact) = 48a86633…`。

### 这三个数字讲的正是 OAM-PHM 的核心论点

**last-seen 0.333** —— 第 2 天书出现在沙发（`temporary_exception`），last-seen 从此永远相信沙发，第 3 天错。这就是 `§2.2` 记录的边界"检测命中直接增加位置习惯证据"的最直接后果：**一次偶然异常永久污染当前状态**。

**household-frequency 0.667** —— 池化抗住了第 2 天的异常，第 3 天答对。但它第 2 天错，因为池化分布**追不上当前状态**。

**关键在于两者的互补失败**：

> 单一位置分布赢了习惯就输了当前状态，赢了当前状态就输了习惯。**没有更好的池化估计器能同时赢**。

这正是 `§2.2` 那一行——*"一个位置分布同时承担当前状态和长期规律"*——第一次变成可测量的东西。要突破它必须**分离当前信念与长期习惯**，也就是 WP2，而不是把频率先验调得更好。

**markov 0.333** —— 3 次稀疏检测只产生 1 次转移，一阶估计退化到与 last-seen 相同。这量化了 `§1.1` 场景里"选择性观察"的代价。

**地板留有余量**：没有任何可运行基线解决这个固定样例（测试 `test_no_runnable_baseline_solves_the_fixture` 固定了这一点），所以 WP2–WP6 的增益是可测的。

---

## 3. 两条结构性保证

### 3.1 基线不可能看到真值

`§5.1` 不变量 8 规定只有 M29/M31/M32 可读 `gt.*`。基线是模型不是评价器，所以：

- 基线的输入类型是 `SymbolicSimulationResult`（机器人可见流），**不是** `PrivilegedSymbolicSimulationView`
- 真值只以 `tuple[UUID, ...]` 形式传给打分函数，永不传给基线
- 测试 `test_baseline_module_never_imports_ground_truth` **解析模块的 AST**，断言它不 import 任何 `cpswm_gt`

最后一条是结构性的：不是靠约定，是靠导入图。

### 3.2 输入必须完全相同

`§9.4` 规则 1 与 6 要求同感知输入、同预算，禁止靠给候选方法更多观察制造收益。`compare_baselines` 把同一个 run 和同一个查询集喂给每个条目，测试 `test_every_baseline_sees_identical_inputs` 用一个记录型子类验证每个基线看到的检测数与机会数完全一致。

---

## 4. §9.1 注册表：显示缺什么，而不只显示有什么

10 条全部登记，**不可运行的也登记**，避免"没比过"被读成"比过并赢了"。

| 状态 | 数量 | 条目 |
|---|---:|---|
| `implemented` | 3 | last-seen、household frequency、Markov transition |
| `gated_by_review` | 3 | person-conditioned frequency prior、full OAM-PHM、per-dimension oracle 上界 |
| `external_reimplementation_required` | 4 | O-STaR 忠实复现、O-STaR 独立重调参、O-STaR + 匹配增强感知、STREAK |

契约层强制：**非 implemented 条目不允许声明 `baseline_version`**（测试 `test_a_non_implemented_entry_cannot_claim_a_version`）。这样注册表不可能出现"看起来实现了其实没有"的条目。

产物 JSON 里带 `scope` 字段明写：

> Floor only. No OAM-PHM method is compared here … this artifact must never be read as a completed §9.1 comparison.

---

## 5. 过程中的一个发现

F0 固定样例里，`gt_entity_id` 与机器人可见的 `object_instance_id` / `location_id` **是同一个 UUID 命名空间**（评价器 line 789–790 直接 `==` 比较）。

在 F0-L0 符号 oracle 轨道上这是合理选择，但需要记录两点：

1. 泄漏检测器因此只能针对 `gt_event_id`，**不能**针对实体 ID——因为实体 ID 本来就共享。这与文档 §4 "精确特权引用检测器"的限定一致，但值得在文档里说明白共享的是哪一层。
2. F0 评分现显式使用 `F0ExactBijectionIdentityMapping`。B1/D2 另有
   `TemporalIdentityAssociationContract`，允许 fragment、merge、unmatched/unknown 和有效期；
   评价器不再把 F0 双射当成真实 tracker 的唯一契约。

---

## 6. 下一步（按门禁分）

**门内可做：**

| 项 | 说明 |
|---|---|
| 扩展查询集 | **已完成脚手架**；默认 1 小时网格在 F0 产生 62 个对齐查询，同时生成 current truth 与 empirical habitual truth |
| `§10.2` 固定观察轨道的其余指标 | verified-absence calibration、evidence traceability 等不依赖多人物 |
| 把 WP0 协议迁移到结构三 | 结构三 §13.4 缺的正是这个，模板已经有了 |

**需要 F0 验收后才能做：**

| 项 | 阻塞原因 |
|---|---|
| person-conditioned frequency prior | 多人物轨道被暂停 |
| 各维度 oracle 上界 | S3-1/F0 未完成交付项 |
| WP3 / WP5 | 明文暂停 |

**需要额外投入：**

O-STaR 忠实复现是 WP0 的主体。当前已经核对原论文并实现 `w_hit` / `w_miss` /
`gamma` 所在的公式级参考核心，但 LLM prior、3D 几何、relaxed transition inference、
成本感知主动搜索和机会式多目标感知仍未形成端到端复现。当前实现仍只是**基线地板加公式核心**，
不是 WP0 完成；注册表和 fidelity manifest 均保持 BLOCK。

---

## 7. 边界

1. 本轮**没有触碰**任何被 F0 复审门暂停的能力
2. 62 个小时点是 time-grid queries，不是 62 个独立样本；产物同时报告
   episode / household / object / event cluster count，并明写 `statistical_independence_claim=false`。
3. 当前 habitual truth 只是 `cumulative_empirical_placement_distribution`，包含
   temporary exception，不是 latent routine truth。契约为后续 latent / regime-conditioned /
   exception-excluded 轨道保留独立类型。
4. 三条单头基线的 habitual 输出标为 `derived_empirical_reference_head`，不是各自
   native habitual head；派生头不得用于 habitual/joint 调参目标。
5. 平滑常数 `1e-3` 是任意选的，只为保证 NLL 有限；它会影响 NLL 绝对值，不影响 top-1
6. 没有 commit、没有 push
7. WP0 状态是 `floor_established`，**不是** `§9.1 comparison complete`

---

## 8. 全量回归和性能预算

- 预算协议不再使用含混的 `prediction_calls`。可选单位是 adapter invocation、
  component-model invocation 或 processed observation；若 adapter 不报告被选单位，评分失败。
- 单头派生路径每个 query 报告 1 次 adapter invocation 和 2 次 component-model
  invocation；当前 runner 开发轨只选用 adapter invocation，不替正式对比选定最终口径。
- **5 秒是事后 wall-time compliance check，不是 hard timeout**。正式外部复现仍需
  process/worker 级 timeout 与 hardware profile。
- 正式协议绑定 dataset content hash、truth manifest hash、identity mapping hash、
  episode/household/object/event groups 和 frozen candidate-grid hash。authority 必须先通过
  disjointness audit，再对 evaluation data 和 tuning selection 分别签名。
- 同一 query set 用 `model_copy` 改 split 名会因 group/sample overlap 被拒绝；
  `SealedEvaluationQuerySet` 不接受普通直接构造。
